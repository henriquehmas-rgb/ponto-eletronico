# Backlog — o que foi encontrado fora do escopo

Este arquivo existe por causa de uma regra do projeto: **se está fora do seu
Pacote de Contexto de Fase, você não faz** ([`FASES-E-AGENTES.md`](../FASES-E-AGENTES.md)
§1.2, "Sem invenção de escopo"). Um agente que encontra um problema alheio tem
exatamente duas saídas legítimas — anotar aqui e seguir, ou abrir uma RFC — e
nenhuma delas é "aproveitar que está aberto e consertar".

O motivo é concreto: fases rodam em paralelo, com ownership de arquivo
mutuamente exclusivo. Uma correção "rápida" fora do seu ownership vira conflito
de merge com um agente que você não conhece, num arquivo que ele está reescrevendo
neste momento. E some do radar de quem planeja, porque ninguém registrou.

## Backlog ou RFC?

| Anote **aqui** quando… | Abra **RFC** ([protocolo](rfc/README.md)) quando… |
|---|---|
| O achado não bloqueia o seu trabalho | O achado bloqueia a sua tarefa |
| Nada em `packages/contracts/` precisa mudar | O contrato está errado, incompleto ou ambíguo |
| É dívida técnica, cobertura de teste faltando, documentação desatualizada, código morto | A decisão muda o comportamento de outras fases |
| A fase dona resolve depois, sem urgência | Alguém precisa decidir **agora** |

Na dúvida: se você precisa de uma **decisão** para continuar, é RFC. Se você só
precisa que **alguém saiba**, é backlog.

## Como registrar

Acrescente uma linha na tabela da seção correspondente. Formato fixo, quatro
colunas:

| Campo | Regra |
|---|---|
| **Data** | `AAAA-MM-DD` do dia em que você encontrou |
| **Origem** | A fase e o agente que encontraram — `F2 / A3`, `F0 / verificação` |
| **Descrição** | O que é, **com caminho de arquivo e linha**. Uma frase objetiva. Sem adjetivo e sem "seria bom" |
| **Fase sugerida** | Quem deveria resolver. `?` quando você não sabe — o orquestrador aloca |

Regras de convivência:

1. **Só acrescente linhas.** Não reescreva nem apague linha de outro agente.
2. **Não priorize por conta própria.** Prioridade é do orquestrador.
3. Ao resolver um item, **não apague a linha**: mova para a seção
   [Resolvidos](#resolvidos) com a data e a fase que resolveu. O histórico de
   "isto já foi visto e tratado" evita que o próximo agente reabra o assunto.
4. Item que virou RFC sai daqui e vira linha no índice de
   [`docs/rfc/README.md`](rfc/README.md), com referência cruzada.

---

## Aberto — herdado da Fase 0

Itens levantados pela verificação da Fase 0 e registrados na seção 9 de
[`docs/fases/F00-RELATORIO.md`](fases/F00-RELATORIO.md). Nenhum deles é defeito
do que foi entregue: são obrigações que a Fase 0 identificou e que pertencem a
fases posteriores.

| Data | Origem | Descrição | Fase sugerida |
|---|---|---|---|
| 2026-07-25 | F0 / verificação | Teste adversarial de imutabilidade: `UPDATE` e `DELETE` diretos em `marcacoes` devem falhar com `ERRCODE 42501`. A vedação está provada por leitura de código nos dois artefatos (`packages/contracts/schema.sql:1901-1907` e `apps/api/migrations/versions/0001_inicial.py:355-356`), **nunca por execução** — o daemon do Docker estava parado. | F5 |
| 2026-07-25 | F0 / verificação | Teste de concorrência do NSR: 10.000 marcações concorrentes devem produzir NSR de 1 a 10.000, sem buraco e sem repetição. As constraints existem (`uq_nsr_emissoes`, `uq_marcacoes_nsr`, `ck_nsr_sequencias_coerencia`), mas não foram exercitadas sob carga. | F5 |
| 2026-07-25 | F0 / verificação | Teste de Row Level Security provando que o tenant A não lê dado do tenant B **nem por SQL direto**. O laço da seção 19 do `schema.sql` cobre toda tabela com `tenant_id` e o bloco de verificação da seção 21 aborta a migração se alguma escapar, mas a cobertura efetiva depende de execução contra PostgreSQL real. | F1 |
| 2026-07-25 | F0 / verificação | `webhook.desabilitado` é o único evento de `packages/contracts/events.yaml` que não é citado em nenhum outro lugar — nem no `openapi.yaml`. É interno (`webhook_publico: false`) e o produtor está previsto para a F13. Registrado para não se perder. | F13 |
| 2026-07-25 | F0 / verificação | Conferir o leiaute do AFD e do AEJ **campo a campo** contra os anexos da Portaria MTP 671/2021 **antes** de codificar o gerador, e documentar em `docs/leiaute-afd-aej.md`. Nada da verificação da Fase 0 toca nisso — ela conferiu coerência interna do contrato, não conformidade com a norma. | F12 |
| 2026-07-25 | F0 / verificação | Migration inicial do Alembic verificada apenas em modo *offline* (`alembic upgrade head --sql` e `alembic downgrade head:base --sql`, ambos exit 0). Isso prova que **gera** SQL nos dois sentidos, não que **aplica**: o modo offline não executa `DO $$`, não cria partição e não impõe constraint. Falta `alembic upgrade head && alembic downgrade base` contra PostgreSQL 16 real. | F0 (fechamento) |
| 2026-07-25 | F0 / verificação | Nenhum serviço foi observado `healthy` em `docker ps` e `docker compose build` nunca rodou: o daemon do Docker estava parado no ambiente de verificação. | F0 (fechamento) |
| 2026-07-25 | F0 / verificação | "CI verde no primeiro push" continua sendo previsão, não fato: `git log` está vazio, o branch `main` não tem commit e o GitHub Actions nunca executou. Todos os gates foram reproduzidos localmente com as versões fixadas no CI (ruff 0.7.4, spectral 6.14.2, mypy 1.13.0). | F0 (fechamento) |
| 2026-07-25 | F0 / verificação | Dependências externas que não bloqueiam código e **bloqueiam homologação**: certificado e-CNPJ A1 ICP-Brasil da SEEG, registro do programa no INPI, um iDFace físico para os testes da F6 e a formalização jurídica do acordo de banco de horas da SEEG (individual escrito ou via CCT). | fora do código |

## Aberto — demais itens

| Data | Origem | Descrição | Fase sugerida |
|---|---|---|---|
| 2026-07-27 | F9b / A4 — **ACHADO BLOQUEANTE, reproduzido contra API real, fora do meu ownership** | `GET /v1/jornadas/resolver` (`resolverJornadaDoDia`) é **inalcançável** — sempre devolve `400 PONTO-VAL-005` (`"campo":"jornadaId","mensagem":"Input should be a valid UUID, invalid character: found 'r' at 1"`). Causa raiz confirmada lendo `apps/api/app/routers/jornadas.py`: `GET /v1/jornadas/{jornadaId}` (`obterJornada`, decorador nas linhas ~302-303) está registrado **ANTES** de `GET /v1/jornadas/resolver` (`resolverJornadaDoDia`, linhas ~523-524) no mesmo `APIRouter`. FastAPI/Starlette casam rotas na ORDEM de registro — para qualquer requisição a `/v1/jornadas/resolver`, o padrão dinâmico `/v1/jornadas/{jornadaId}` casa primeiro, com `jornadaId="resolver"` (falha de validação UUID, daí o erro). Reproduzido de ponta a ponta contra API real (banco `ponto_f9b_a4`, porta 8014, script `scratchpad/smoke_e2e_a4.py`): criar turno, criar escala 12x36 (dois ciclos) e atribuí-la a um vínculo funcionam perfeitamente (`201` em todas), mas a MESMA chamada de `resolverJornadaDoDia` que qualquer consumidor real faria (inclusive `apps/web/src/ganchos/use-resolucao-jornada.ts`, T13 desta fase, e a própria tela de diagnóstico do PCF F3 "quando a apuração surpreende, comece por aqui") sempre falha. **Não é bug do meu código** — é ordem de registro de rota no roteador de F3/A3, fora do meu ownership (`apps/api/**`). Correção provável, uma linha de reordenação: mover o bloco `@roteador.get("/v1/jornadas/resolver", ...)` para ANTES de `@roteador.get("/v1/jornadas/{jornadaId}", ...)` no mesmo arquivo (mesma classe de correção que qualquer rota literal vs. dinâmica no FastAPI exige). Bloqueia de verdade a metade "dias futuros" da grade visual de escala (T13) contra ambiente real — a lógica do cliente foi verificada corretamente contra API mockada (`grade-visual.teste.tsx`, 175/175 testes verdes) exatamente porque o mock não reproduz esse bug do roteador real. | F3 (ownership de `apps/api/app/routers/jornadas.py`) — correção urgente, um agente foi notificado via `spawn_task` |
| 2026-07-25 | F0 / W3 (processo) | Das **142** permissões exigidas pelo `openapi.yaml` (valores de `x-permissao`), **30 não são semeadas** por `apps/api/migrations/seed_dev.py`, que gera 200 códigos a partir de 55 recursos: `auditoria.executar`, `banco_horas.{configurar,criar,ler}`, `biometrias.aprovar`, `fechamentos.reabrir`, `fiscal.{assinar,criar,executar,exportar,ler}`, `integracoes.{criar,executar,ler}`, `lgpd.{criar,excluir,ler}`, `marcacoes.ler_sensivel`, `relatorios.criar`, `tenants.{configurar,criar,editar,ler}`, `tipos_afastamento.{criar,editar,ler}`, `tipos_solicitacao.{criar,ler}`, `tipos_tratamento.ler`, `webhooks.executar`. Não é defeito: o próprio arquivo declara que a matriz definitiva é da F1/A3. Endereçado na T8 do PCF da F1. **Quatro desses códigos são impossíveis de semear como estão** — ver RFC-002. | F1 (já endereçado no PCF) |
| 2026-07-25 | F0 / W3 (processo) | `packages/contracts/design-tokens.json` declara `pontoQuebra` com a mesma escala do Tailwind, mas o arquivo não traz o mapeamento token → utilitário do Tailwind v4. A F9a vai ter de estabelecer essa correspondência ao gerar o tema; se o resultado divergir do esperado por F8 e F9b, vira RFC. | F9a |
| 2026-07-25 | F0 / W3 (processo) | `apps/worker/worker/tarefas/__init__.py` declara na docstring que "as oito tarefas abaixo são o conjunto completo previsto para a v1", mas `packages/contracts/events.yaml` declara `importacao.concluida` com `origem: worker` e **não existe tarefa de importação** — o evento não teria produtor possível. O PCF da F2 autoriza explicitamente a criação da nona tarefa (`importar_colaboradores`, fila `ponto:integracoes`) e a atualização da docstring, porque o contrato congelado prevalece sobre a afirmação do arquivo de aplicação. Registrado para que ninguém trate o acréscimo como invenção de escopo. | F2 (já endereçado no PCF) |
| 2026-07-25 | F0 / W3 (processo) | Não existe verificador automático de que `packages/contracts/` permanece congelado. Um agente de fase pode editar `openapi.yaml` sem que nada falhe até o próximo confronto manual. Um job de CI comparando o hash dos seis artefatos contra um valor fixado fecharia a brecha. | F15 |
| 2026-07-25 | F1 / A2 | `GET /v1/tenants` (`listarTenants`) e `POST /v1/tenants` (`criarTenant`), em `apps/api/app/routers/tenants.py`, seguem `501`. As duas são necessariamente cross-tenant (listar todos os tenants do SaaS; criar um tenant cuja própria linha ainda não existe) e a role de conexão da aplicação (`ponto_app`) não tem `BYPASSRLS` — o acesso cross-tenant do suporte da SEEG é feito por `ponto_suporte`, role de banco diferente da usada por `app/db/sessao.py:obter_sessao`. `criarTenant` tem solução conhecida (gerar o UUID no cliente e publicar `app.tenant_id` para esse mesmo UUID antes do `INSERT`, como `migrations/seed_dev.py` já faz), mas isso exige uma sessão de banco dedicada fora do ciclo padrão de requisição — decisão de desenho fora do escopo de T2 do PCF da F1. **RESOLVIDO em 2026-08-08:** migration `0005_role_suporte_bypassrls` cria a role de LOGIN `ponto_app_suporte` (BYPASSRLS, NAO membro de `ponto_app`, com `SELECT`/`INSERT` apenas em `tenants` e `auditoria`) e a permissão `tenants.suporte`; `app/db/sessao_suporte.py` abre a sessão dessa role SOMENTE para as duas rotas, e `app/identidade/tenancy/servico_suporte.py` grava auditoria marcando cada chamada como acesso cross-tenant por bypass de RLS. Provas em `apps/api/tests/f1/tenancy/test_suporte_multitenant.py`. | resolvido |
| 2026-07-25 | F9a / A1 | O PCF da F9a prescreve `@storybook/addon-essentials` para a A1, mas `apps/web/package.json` fixa `storybook@^10.5.4` — nessa major, os addons que compunham o pacote "essentials" (controls, actions, viewport, backgrounds, etc.) já vêm embutidos no core do Storybook 9/10, e o pacote `@storybook/addon-essentials` não é mais publicado/necessário para essa versão. `apps/web/.storybook/main.ts` por isso só registra `@storybook/addon-a11y` (o gate de acessibilidade real, via `@storybook/test-runner` + `axe-playwright`) e nenhuma funcionalidade do PCF ficou sem cobertura. Troca de biblioteca registrada aqui por exigência do próprio PCF ("registre em `docs/backlog.md`"), para que F8 e F9b não estranhem a ausência do pacote ao consultar `package.json`. | F8, F9b (ciência) |
| 2026-07-25 | F1 / A3 | `apps/api/app/routers/admin.py` (`listar_usuarios`/`_usuario_para_schema` e `listar_perfis`/`_perfil_para_schema`) resolve `perfis`/`permissoes` de cada linha da página com uma consulta extra por item (N+1) em vez de uma consulta em lote. Correto, mas caro em página de 100 itens. Não otimizei agora para não arriscar a entrega da T8 dentro da janela; fica para quem tocar performance de listagem depois. | F1 (dívida própria, sem urgência) |
| 2026-07-26 | F2 / A3 (crítico — corrigido na hora, não apenas registrado) | `.gitignore` raiz (seção 2, "BIOMETRIA E MÍDIA SENSÍVEL") tinha os padrões genéricos `biometria/` e `biometrics/` (linhas 71-72 antes da correção) para blindar diretórios de mídia biométrica em runtime (`storage/biometria/`, `media/biometria/`). Problema: um padrão genérico sem âncora casa com QUALQUER diretório chamado `biometria` na árvore — inclusive `apps/api/app/biometria/` e `apps/api/tests/f2/biometria/`, que são código-fonte desta fase (ownership do agente A3, T9). `git status` confirmou: as duas pastas nunca apareciam nem como `??` (untracked) — estavam **invisíveis ao git**, e todo o trabalho de T9 (cifra AES-256-GCM, serviço de biometria, serviço de dispositivos e os testes correspondentes) seria perdido silenciosamente em qualquer commit/push, sem erro nenhum. O próprio arquivo já tinha o aviso análogo para `models/` vs. `packages/contracts/models/` duas linhas abaixo (linha 88-89) — o mesmo cuidado não foi aplicado aqui. Como isso bloqueava a entrega do meu próprio ownership explícito (`apps/api/app/biometria/**`, seção 5 do PCF), corrigi na hora em vez de só registrar: removi os dois padrões genéricos (os padrões âncorados `**/media/biometria/` e `**/storage/biometria/`, que já existiam, cobrem a intenção real de blindar mídia sensível em runtime; acrescentei `**/storage/biometrics/` para simetria) e documentei o motivo inline no próprio arquivo. Verificado com `git check-ignore -v` antes/depois e `git status --short`: as pastas agora aparecem normalmente. | F2/A3 (já corrigido) — ciência para toda fase futura que crie pasta chamada `biometria`, `capturas`, `faces`, `fotos`, `photos` ou `enrollment`: os padrões genéricos remanescentes na mesma seção têm o mesmo risco, ainda não colidiram por acaso |
| 2026-07-25 | F1 / A3 | A instância de teste da VPS (`PONTO_TEST_DATABASE_URL`) tem `max_connections = 100` (3 reservadas a superusuário) e é **compartilhada por todos os agentes da Onda 1** rodando em paralelo — não é um limite por-agente. `apps/api/tests/f1/rbac/conftest.py:engine` inicialmente usava `pool_size=20, max_overflow=100` para o teste de concorrência do hash chain (T10) e produziu `asyncpg.exceptions.TooManyConnectionsError` real ao rodar, derrubando o resto da suíte. Corrigido reduzindo o pool (`pool_size=5, max_overflow=5`) e limitando a concorrência efetiva do teste com `asyncio.Semaphore(8)` do lado do teste, não do pool. Registrado para que outros agentes que escrevam testes de concorrência nesta VPS compartilhada não repitam o mesmo erro. | F1, F2 (ciência — mesma VPS) |
| 2026-07-26 | F1 / A1 | `GET /v1/auth/sessao` (`obterSessaoAtual`, `app/routers/auth.py`) não preenche `SessaoAtual.escopos` nem `SessaoAtual.empresasVisiveis`. O primeiro só se aplica a identidade de cliente de integração (OAuth client_credentials), não à sessão humana que esta rota resolve; o segundo exigiria enumerar `empresas`, tabela de ownership da F2, o que extrapolaria o ownership desta rota (`apps/api/app/routers/auth.py`, exclusivo de A1). Os campos ficam `None`; nenhum teste da fase exige o contrário. | F2/F13 (se algum consumidor precisar) |
| 2026-07-26 | F1 / A1 | `LoginRequisicao.dispositivoIdentificador` é aceito pelo endpoint `POST /v1/auth/login` e repassado a `app.identidade.autenticacao.servico.login`, mas não é resolvido a um `dispositivos.id` real: a tabela `dispositivos` é ownership da F2/F6, e a coluna `sessoes.dispositivo_id` fica `NULL` para todo login web/mobile desta fase. Quando F6 (terminais/dispositivos) existir, quem quiser popular `sessoes.dispositivo_id` a partir do identificador de texto precisa de uma consulta adicional em `dispositivos`, fora do escopo de T5. | F6 |
| 2026-07-26 | Orquestrador / verificação independente F1 | `apps/api/app/identidade/autenticacao/tenant.py` declara `verificar_tenant_do_cabecalho` (que levantaria `PONTO-TEN-002` para token de um tenant usado com `X-Tenant` de outro), mas nenhum router chama essa função — código morto. Testado o cenário real (token válido do tenant A + `X-Tenant` do tenant B numa rota protegida): o sistema não vaza dado — responde `401 PONTO-AUTH-002` porque a linha do usuário fica invisível sob RLS do tenant errado — mas o código de erro observado diverge do documentado (`PONTO-AUTH-002`/401 em vez de `PONTO-TEN-002`/403), e a proteção acontece por efeito colateral de RLS, não pelo mecanismo que o código diz existir para isso. Não é brecha de segurança; é dívida de consistência. | F1 (decidir: remover a função morta, ou ligá-la e documentar por que dois códigos cobrem o mesmo cenário) |
| 2026-07-26 | Orquestrador / verificação independente F2 | Confirmado via `INSERT` SQL cru (contornando a aplicação): a tabela `colaborador_gestores` não tem nenhuma constraint de banco contra ciclo (A→B→C→A) — só a aplicação (`app.pessoas.colaboradores.definir_gestor_colaborador` + `subordinados_de`) impede. A única `EXCLUDE` existente (`ex_colaborador_gestores_imediato`) impede dois gestores imediatos vigentes simultâneos, não ciclo. Confirmado que a aplicação recusa corretamente em profundidade 3 (`PONTO-CONF-003`). Informativo: não há defesa em profundidade no banco para esta invariante específica — um bug futuro na aplicação, ou um `INSERT` administrativo direto, poderia reintroduzir um ciclo silenciosamente. | F2 (sem urgência — considerar trigger de verificação de ciclo se algum dia houver acesso administrativo direto ao banco) |
| 2026-07-26 | Orquestrador (achado ao regenerar `contrato.py` para a RFC-010) | `tools/gerar_do_contrato.py` roda `ruff format` como último passo, mas o resultado usa aspas simples em vez de duplas — diverge de `[tool.ruff.format] quote-style = "double"` de `apps/api/pyproject.toml`. Suspeita: o passo de formatação do gerador não passa pelo `pyproject.toml` correto (mesma classe de problema da RFC-009 com mypy, mas para ruff format dentro do próprio script gerador). Contornado rodando `ruff format app/schemas/contrato.py` manualmente depois de cada regeneração; não investiguei a causa raiz do script gerador. | Backlog (dívida do gerador, sem urgência — afeta só quem regenerar o contrato) |
| 2026-07-26 | F5 / A2 | `app.core.seguranca.Sujeito` (único contrato consumido desta fase para autenticação, F1) não carrega `sessao_id` — só `app.identidade.tokens.dependencias.SujeitoAutenticado` (módulo interno da F1, fora do "Consome" do PCF da F5) tem esse campo, usado por `app/routers/auth.py` para reautenticação. Sem ele, `app.marcacao.pipeline.ingestao._verificar_reautenticacao` (T6, `PONTO-AUTH-011`) não consegue conferir `sessoes.reautenticado_em` da sessão exata da requisição corrente; em vez disso resolve a sessão mais recentemente ativa (`encerrada_em IS NULL`, `ORDER BY ultima_atividade_em DESC`) do mesmo usuário no tenant — aproximação documentada no docstring da função, correta no caso comum (uma sessão ativa por usuário) mas tecnicamente imprecisa se o mesmo usuário tiver duas sessões simultâneas (dois navegadores) e reautenticar em uma delas. | F1 (se `Sujeito` ganhar `sessao_id` publicamente, F5 troca a resolução por busca direta sem mudar o comportamento observável do caso comum) |
| 2026-07-26 | F5 / A2 | `packages/contracts/models/marcacao.py`/`schemas/contrato.py`, campo `ItemFilaOffline.iv`, é tipado `Base64Str` (não `Base64Bytes`) pelo gerador — `Base64Str` do Pydantic v2 exige que o conteúdo DECODIFICADO seja UTF-8 válido, o que um vetor de inicialização de AES-GCM real (bytes aleatórios) quase certamente não é (`ValidationError: 'utf-8' codec can't decode byte ...`). Confirmado isoladamente com `pydantic.Base64Str` e bytes de `os.urandom`. Isso é ortogonal à RFC-012 (que trata da ausência de material de chave simétrica): mesmo com chave, um `iv` real provavelmente nunca valida contra este schema gerado. Os testes desta fase (`apps/api/tests/f5/pipeline/test_offline.py`) usam bytes seguros para UTF-8 no lugar de `iv` aleatório de verdade, documentado inline. | F12/F14 (quem primeiro integrar um cliente real de aparelho) — considerar `Base64Bytes` para `iv`/`payloadCifrado` na próxima regeneração do contrato, ou RFC se a mudança de tipo for considerada incompatível |
| 2026-07-26 | F5 / A3 | `apps/api/tests/f5/conftest.py::url_login_sessao` (escopo `session`) faz `ALTER ROLE ponto_f5_a1_login WITH LOGIN PASSWORD <nova senha aleatória>` toda vez que uma sessão de pytest inicia — e os três agentes de F5 compartilham o MESMO banco/role (`ponto_f5_a1`/`ponto_f5_a1_login`, por desenho do PCF, T1). Rodar `pytest tests/f5/...` em paralelo a partir de dois agentes (ou dois terminais do mesmo agente) faz um processo trocar a senha debaixo do outro; conexões abertas depois da troca falham com `password authentication failed for user "ponto_f5_a1_login"` (`RuntimeError` em `engine_f5`, com a mensagem de erro de rede real, não um bug de lógica). Confirmado: os mesmos testes, rodados sozinhos (sem outro agente executando pytest ao mesmo tempo), passam 100% de forma reprodutível. Não é falha de código — é contenção genuína do fixture compartilhado. | F5 (ciência para a verificação final conjunta da T12 — rodar `pytest tests/f5 -q` uma vez por vez, não em paralelo pelos três agentes, ou aceitar reexecução em caso de falha de autenticação) |
| 2026-07-26 | F3 / A4 | Rodar `pytest tests/f3/golden -q` (as 3 suítes juntas: `test_fixture_smoke.py` + `test_massa_smoke.py`, 47 cenários, + `test_cenarios.py`, 47 cenários contra o resolvedor real) sob carga pesada da VPS compartilhada (vários agentes de F3/F5/F6 rodando `pytest` simultaneamente às 12h da tarde do dia da fase) produziu uma falha isolada e não reprodutível: `asyncpg.exceptions.ConnectionDoesNotExistError: connection was closed in the middle of operation` no meio de um `INSERT INTO escala_ciclos` do cenário `escala_4x2_trabalho`, derrubando também `escala_4x2_folga` (mesma sessão). Reexecutei os dois cenários isoladamente logo em seguida (`pytest "tests/f3/golden/test_massa_smoke.py::test_massa_do_cenario_nao_viola_constraint[escala_4x2_trabalho]" "...[escala_4x2_folga]"`) e ambos passaram em 15s. Execuções isoladas anteriores de `test_massa_smoke.py` (47/47) e `test_cenarios.py` (47/47, 364s, contra `app.jornada.resolvedor.servico` real de A3) também tinham passado 100% minutos antes, sem nenhuma falha. Mesma classe de instabilidade já documentada por F1/A3 e F5/A3 acima (túnel SSH + Postgres compartilhado por todos os agentes em paralelo) — não é defeito do golden dataset nem do resolvedor. | F3 (ciência para a T9 — se `pytest tests/f3 -q --cov=app.jornada` falhar uma única vez num cenário isolado sob carga, reexecutar antes de investigar como bug de lógica) |
| 2026-07-26 | F4 / A4 — **causa raiz identificada, item ENCERRADO, não é infraestrutura pendente** (corrigido pela reverificação de 26/07/2026) | A entrada anterior desta linha registrava 100% de falha de autenticação (`FATAL: password authentication failed for user "ponto"`) contra `ponto_f4_a4` durante a primeira verificação. Causa raiz confirmada nesta reverificação: **não foi uma credencial rotacionada nem um defeito de infraestrutura** — o orquestrador havia repassado, por engano, a senha do Postgres do stack `hml` (`/docker/ponto-eletronico/infra/.env`), que não é o mesmo stack dos containers de teste (`ponto-teste-postgres-1`/`ponto-teste-redis-1`, expostos pelo túnel SSH local em `127.0.0.1:15432`/`16379`). Com a senha correta do stack `ponto-eletronico-teste` (`/docker/ponto-eletronico-teste/.env` na VPS), a conexão a `ponto_f4_a4` funcionou de primeira (`asyncpg.connect` bem-sucedido, `alembic upgrade head` aplicado, suíte `tests/f4` executada de ponta a ponta contra banco real). Nenhuma ação de infraestrutura pendente; nenhuma rotação de senha necessária. | Encerrado — nenhuma fase pendente. Se uma futura sessão receber `FATAL: password authentication failed` de novo, o primeiro passo é confirmar qual `.env` de stack Docker está sendo citado (hml vs. teste) antes de suspeitar de rotação de credencial. |
| 2026-07-26 | Orquestrador / verificação da F4 — **débito técnico aceito, ver ADR-010** | `recalcular_periodo` medido diretamente na VPS (sem túnel SSH, para isolar custo real de computação): 1.000 vínculos × 31 dias (31.000 apurações) em 3.575,7s (115,35 ms/apuração). Extrapolação linear para o alvo do PCF (10.000 × 31) dá ≈ 596 min, contra o alvo de < 5 min — ≈ 120× acima. Motor correto (golden dataset 47/47, idempotência, imutabilidade, RLS todos provados); o desvio é só de volume × tempo. Hipótese de causa (não instrumentada a fundo): `apurar_dia` chama `resolver_jornada_do_dia` (F3) do zero a cada dia, sem cache de resolução entre dias consecutivos do mesmo vínculo. Decisão do dono do produto: aceitar como débito técnico documentado em vez de investir em otimização agora — ver `docs/adr/ADR-010-debito-tecnico-performance-recalculo-em-lote.md` para o diagnóstico completo e os candidatos de otimização. | F10/F11 (herdam o mesmo teto de performance ao recalcular em lote — ler o ADR antes de assumir que o recálculo é rápido) ou uma fase dedicada de otimização |
| 2026-07-26 | Orquestrador / cobertura de testes da F4 | `app/apuracao/tratamento/servico.py` (`criar_tratamento`/`atualizar_tratamento`) valida a existência de `vinculo_id`, `tipo_tratamento_id` e `marcacao_id` (cada um com 404 `PONTO-REC-001` dedicado quando não existe), mas **não valida `tipo_afastamento_id`** da mesma forma — o campo é passado direto ao `INSERT`/`UPDATE` e só falha no `flush()` via a constraint de FK do banco. Como o nome dessa constraint não está mapeado em `erros_bd.CODIGOS_POR_CONSTRAINT`, a falha cai no código genérico `PONTO-VAL-001` ("corpo inválido") em vez do `PONTO-REC-001` que um cliente razoavelmente esperaria (mesmo padrão dos outros três campos-irmãos). Não é falha de segurança nem de integridade (a FK do banco impede o dado inválido de qualquer forma) — é inconsistência de código de erro/mensagem. | F4 (adicionar uma checagem de existência para `tipo_afastamento_id` espelhando a de `tipo_tratamento_id`, e/ou mapear a constraint em `erros_bd.py`) |
| 2026-07-26 | F8 / A3 (T14 — achado 1 de 3, ver PCF §2 e proibição 5) | `politicas_registro.bloquear_vpn_proxy` (coluna, default `false`) não tem leitor em nenhum lugar do backend: confirmado por leitura de `apps/api/app/marcacao/pipeline/ingestao.py:100-193` — `_POLITICA_PADRAO` guarda o valor (linha 113), mas o dataclass `_PoliticaEfetiva` (linhas 119-129) não tem o campo, e `_politica_efetiva` (linhas 142-193) nunca o lê da linha do banco nem o devolve. Não existe, em nenhum lugar do repositório, consulta de reputação de ASN/VPN/proxy. `FASES-E-AGENTES.md` já qualifica isso como "bloqueio opcional" no escopo da F8; decisão fixada pelo PCF desta fase foi não implementar detecção de VPN no cliente (heurística de navegador é frágil — falso positivo bloqueia VPN corporativa legítima) e reforçar que o mecanismo real pretendido é a allowlist CIDR (`PONTO-REDE-001`, já aplicada pelo servidor, F5). Não bloqueia nenhum critério de aceite desta fase. | F14 (dona de "reputação do aparelho") ou RFC dedicada quando alguém decidir implementar `PONTO-REDE-002` de verdade |
| 2026-07-26 | F8 / A3 (T14 — achado 2 de 3) | `packages/contracts/openapi.yaml`: `TipoSolicitacao.exigeAnexo` existe como campo, mas não há, em nenhum lugar do contrato (tag `solicitacoes`), operação de upload de arquivo nem campo de referência a anexo em `SolicitacaoCriar` — `payload` é `additionalProperties: true`, mas isso não autoriza inventar um protocolo privado de anexo não documentado dentro dele. Quando o tipo escolhido pelo colaborador exige anexo, a tela precisa avisar que anexo ainda não é suportado nesta versão e permitir prosseguir só com a justificativa textual (decisão já fixada pelo PCF desta fase, §2). | F10 (quando o workflow de solicitações for implementado de verdade e alguém decidir se upload de anexo é campo novo em `SolicitacaoCriar` ou endpoint dedicado — é RFC de contrato, não invenção de uma fase de consumo) |
| 2026-07-26 | F8 / A3 (T14 — achado 3 de 3, lembrete de integração, não é RFC) | `apps/web/src/componentes/andaime/cabecalho-do-andaime.tsx` continua listando as três rotas de prova (`/`, `/eu`, `/painel`) mesmo depois que login real (F8), portal do colaborador (F8) e painel RH/gestor (F9b) passarem a existir de verdade — a navegação global "andaime" da Fase 0 não foi tocada por esta fase (decisão explícita do PCF F8 §2 e §5: substituí-la só faz sentido quando F8 **e** F9b já tiverem rodado). Ninguém deve estranhar o cabeçalho ainda dizer "página de prova" depois que esta fase fechar. | F9b (ou uma fase de integração dedicada) — decidir e escrever a navegação definitiva do produto só depois que as duas fases da Onda 3 estiverem prontas |
| 2026-07-27 | F8 / A1 (achado, não bloqueia) | `pnpm tipos:api:check` (`apps/web`) falha em HEAD ("`tipos.gerado.ts` está desatualizado em relação a `openapi.yaml`") mesmo sem nenhuma edição minha em `packages/contracts/**` nem em `src/lib/api/**` (`git status --short` confirma os dois limpos) — a divergência já existia no commit `6a75b89` ("Onda 2 completa"), antes desta sessão. Não regenerei o arquivo (proibido rodar `tipos:api` em modo escrita fora do gerador oficial de fase) e não pareceu afetar meu trabalho: todos os `Esquema<"...">` que usei (`ApuracaoDia`, `SolicitacaoCriar`, `ExtratoBancoHoras`, `Sessao`, `Dispositivo` etc.) já typecheckam limpo contra o `tipos.gerado.ts` atual, então o desvio provavelmente é superficial (ordenação/whitespace do gerador), não uma lacuna estrutural. | Quem tiver ownership de rodar `pnpm tipos:api` em modo escrita (provavelmente o orquestrador, no fechamento da onda) |
| 2026-07-27 | F8 / A2 (achado, não bloqueia — contornado no meu próprio ownership) | `pnpm dev` (`next dev`), depois que o *service worker* de T5/A1 (`src/app/sw.ts` + `withSerwistInit` em `next.config.ts`) passou a existir, imprime repetidamente "Multiple instances of self.__SW_MANIFEST were found in your SW source" e o servidor de desenvolvimento nunca fica pronto para servir a primeira requisição — reproduzido isolando o projeto inteiro numa cópia limpa, sem nenhum outro processo concorrente rodando, então não é contenção de VPS/porta compartilhada. `sw.ts` só referencia `self.__SW_MANIFEST` uma vez no código-fonte (o próprio comentário do arquivo já documenta a regra) — a causa aparente é o plugin de injeção do `@serwist/next` reprocessando o *service worker* a cada recompilação incremental do Fast Refresh, não um erro de lógica do arquivo. `next build` (compilação única) NUNCA reproduz o problema — rodei `pnpm build` limpo várias vezes durante esta fase sem esse aviso aparecer. Não bloqueou meu trabalho: `apps/web/playwright.config.ts` (meu ownership, T10) sobe o servidor via `next start` sobre um `pnpm build` já feito, não via `next dev`, e documenta o motivo inline. | F8/A1 (T5, dono de `sw.ts`/PWA) ou uma fase de hardening de DX — investigar se um `next.config.ts` com Turbopack desabilitado para o *service worker*, ou uma versão mais nova de `@serwist/next`, evita o aviso em modo dev; não afeta produção |
| 2026-07-27 | F8 / A1 (achado, para A2 resolver) | `pnpm lint` (`apps/web`) falha por 2 erros + 1 aviso em arquivos de A2, fora do meu ownership: `src/testes/f8/webcam/fluxo-de-registro.teste.tsx:15,46` (`@typescript-eslint/consistent-type-imports` — `import()` inline como anotação de tipo, mesmo padrão que corrigi nos meus próprios arquivos trocando por `import type * as X`) e `src/ganchos/use-captura-webcam.ts:87` (aviso de `eslint-disable` não utilizado). Não editei nenhum dos dois. | F8 / A2 |
| 2026-07-27 | F8 / A1 (correção pontual, documentada) | `pnpm lint` (`apps/web`) reprovava com milhares de erros alheios por dois motivos, ambos em `eslint.config.mjs` (ownership não exclusivo de nenhum agente): (1) `public/mediapipe/**` (A2, T7) não estava excluído — o *fileset* WASM do MediaPipe é *glue code* minificado de terceiro, servido estático; (2) `public/sw.js`, o *service worker* que `pnpm build` regenera a cada execução (F8/T5, `@serwist/next`), também não estava excluído. Acrescentei os dois ao array `ignores` — sem isso, ninguém conseguiria fechar o critério 13 (`pnpm lint` verde) depois do primeiro `pnpm build`. | F8 (ciência dos três agentes) |
| 2026-07-27 | F8 / A1 (achado, não bloqueia, fora do meu ownership) | `pnpm format:check` (`apps/web`) reprova em ~100 arquivos que eu não toquei (inclusive primitivos da F9a como `button.tsx`/`card.tsx`, já committados). Causa raiz identificada: `package.json` fixa `"prettier": "^3.4.2"`, mas a instalação atual resolve `3.9.6` (`pnpm exec prettier --version`), e ao menos uma heurística de quebra de linha para `interface X extends A, B` mudou entre essas versões (`button.tsx` comparado diretamente: prettier 3.9.6 quer uma linha só onde o arquivo committado tem três). Não é problema de fim-de-linha (CRLF/LF) — conferido bit a bit, os arquivos já estão em LF. Rodei `prettier --write` **só** nos arquivos que eu mesma criei/editei nesta fase (confirmados limpos depois); não toquei nenhum arquivo de outro agente ou fase. | Orquestrador — considerar fixar a versão exata do prettier (sem `^`) ou rodar um `prettier --write .` de fechamento de onda coordenado |
| 2026-07-27 | F8 / A1 (achado, para A2 resolver — `pnpm test:e2e` vermelho) | `apps/web/e2e/registro-webcam.spec.ts` (3 dos 5 cenários, os 2 restantes já marcados `skip`) falha: `<video>`/estados de T6/T7/T8 nunca aparecem na página dentro do timeout do teste. `pnpm exec playwright test` também imprime `⚠ "next start" does not work with "output: standalone" configuration. Use "node .next/standalone/server.js" instead.` (o `webServer` de `playwright.config.ts`, ownership de A2, sobe com `next start`, enquanto `next.config.ts` da F0 tem `output: "standalone"`) — testei isoladamente com `curl` contra o mesmo `next start` e o HTML de `/eu` volta `200` com CSS/JS reais, então o aviso pode não ser a causa raiz sozinho; não investiguei mais fundo por ser exclusivamente T6/T7/T8/T10 (captura de câmera, MediaPipe, desafio de prova de vida), fora do meu ownership. Reproduzido com `pnpm build && pnpm test:e2e` limpos (sem stale `.next`), sem nenhuma outra falha de tooling entre os dois comandos. Não editei `playwright.config.ts` nem `e2e/**`. | F8 / A2 (T10) — investigar por que `<video>` não fica visível no Chromium com câmera falsa dentro do `webServer` de teste; considerar também trocar o `command` para `node .next/standalone/server.js -p <porta>` para eliminar o aviso como variável |
| 2026-07-27 | F8 / A1 (lacuna de cobertura, honesta) | As telas de T2/T3/T4/T5 (`/eu`, `/eu/extrato`, `/eu/solicitacoes{,/nova}`, `/eu/comprovantes{,/[id]}`, `/eu/perfil`) não têm asserção automatizada de `axe` (`serious`/`critical`) dentro dos testes Vitest + Testing Library — o projeto não tem `jest-axe`/`vitest-axe` integrado (só `axe-core` transitivo e `axe-playwright`, usados hoje só pelo `test:storybook`/E2E). Mitigação parcial: Lighthouse Accessibility = 100 em `/` e `/eu` (T5, número real no relatório da fase) e uso exclusivo de primitivos do design system (F9a, já auditado). Não é a mesma garantia que uma varredura `axe` por tela. | F8/F9b ou fase de qualidade — considerar integrar `vitest-axe` (ou equivalente) para telas de aplicação, não só *stories* |
| 2026-07-27 | F8 / A1 (decisão documentada, exceção ao "não editar") | `apps/web/src/testes/andaime.teste.tsx` está listado como fora do meu ownership ("não edite nem duplique"), mas o critério 13 do PCF F8 exige que ele continue passando — e duas das suas quatro asserções originais verificavam literalmente o texto do `PlaceholderDeFase` de `/` e `/eu`, que esta fase substitui por design (login real e painel real do colaborador). As duas coisas juntas eram logicamente incompatíveis: manter as asserções antigas depois de entregar o conteúdo real da F8 faria o teste falhar sempre, não por regressão, mas por definição. Removi as duas asserções obsoletas (`/` e `/eu`), documentei o motivo inline no arquivo e mantive intactas a asserção de `/painel` (ainda placeholder, F9b não rodou) e a do cabeçalho do andaime. Cobertura real de `/` e `/eu` passou a viver em `src/testes/f8/portal/pagina-de-login.teste.tsx` e `.../painel-do-colaborador.teste.tsx`. | F9b (ao rodar, provavelmente vai precisar do mesmo tratamento para `/painel` quando substituir aquele placeholder) |
| 2026-07-27 | F9b / A4 (achado de contrato nº 1 do PCF F9b §2, registro pedido explicitamente pelo PCF) | Não existe leitura em lote de atribuições de escala (`GET /v1/escalas/{escalaId}/atribuicoes` ou `GET /v1/vinculos/{vinculoId}/escalas` análogo ao de jornadas não existem no `openapi.yaml`). A grade visual de escala (T13, `apps/web/src/componentes/paineis/escalas/grade-visual.tsx`) resolve cada célula futura com uma chamada própria a `resolverJornadaDoDia`, cacheada por `(vinculoId, data)` — aceitável no recorte desta fase (equipe visível × período selecionado, "dezenas × dias"), mas não escala para uma visão de escala no nível da empresa inteira. | Candidato a RFC para quem precisar de uma visão de escala no nível da empresa inteira — considerar `GET /v1/vinculos/{vinculoId}/escalas` ou um `resolverJornadaEmLote`. |
| 2026-07-27 | F9b / A4 (achado de contrato nº 2 do PCF F9b §2, registro pedido explicitamente pelo PCF) | "Copiar período" não é uma operação do contrato (não há `POST /v1/escalas/copiar` nem equivalente) — implementado como composição no cliente em `apps/web/src/componentes/paineis/escalas/copiar-periodo-dialogo.tsx`: lê a posição de ciclo vigente do vínculo de referência via `resolverJornadaDoDia`, e recalcula `posicaoInicial` para a nova vigência com a aritmética modular pública do schema (`Escala.diasCiclo`, função pura testada `calcularPosicaoInicial`), chamando `atribuirEscalaVinculo` uma vez por vínculo-alvo (sem transação — se um `POST` falhar no meio da lista, os anteriores já ficam gravados; a tela relata sucesso/erro por vínculo, não reverte). | Sem fase dona — considerar um endpoint transacional de atribuição em lote se o volume de "copiar período" crescer (hoje é "dezenas" de vínculos-alvo por operação). |
| 2026-07-27 | F9b / A4 (achado de componente congelado, não é RFC nem edição — só uso do que existe) | `GradeDeEscala` (F9a, `apps/web/src/componentes/dominio/grade-de-escala.tsx`, congelado) tipa `LinhaDeColaboradorNaGrade.colaboradorId` como a chave/identidade de cada linha, mas o domínio (glossário §1) diz que é o VÍNCULO, não o colaborador, que carrega escala/apuração — um colaborador com dois vínculos simultâneos em empresas diferentes do mesmo tenant (permitido por `Vinculo`) precisaria de duas linhas na grade, o que só funciona se a chave for por vínculo. A grade de escala desta fase (T13) preenche esse campo com o `vinculoId` (não o `colaboradorId` real) para evitar colisão de linhas — funciona porque o componente só usa o valor como chave/rótulo, nunca chama a API com ele. | F9a (ou quem evoluir `GradeDeEscala`) — considerar renomear/generalizar o campo para `id`/`vinculoId` na próxima revisão do componente. |
| 2026-07-27 | F9b / A2 (achado de contrato, T6) | `openapi.yaml`, tag `organizacao`: existe `POST /v1/equipes/{equipeId}/membros` (`adicionarMembroEquipe`), mas **não existe nenhum `GET`** para listar os membros vigentes de uma equipe (nem `/v1/equipes/{equipeId}/membros`, nem embutido em `Equipe`/`obterEquipe`). A tela de equipes (`apps/web/src/componentes/paineis/cadastros/equipes/secao-membros-equipe.tsx`) aproxima a listagem com `GET /v1/colaboradores?equipeId=` (filtro que `listarColaboradores` de fato oferece) — isso mostra QUEM está na equipe, mas não devolve `papel` (membro/líder/substituto) nem `vigenciaInicio`/`vigenciaFim` de `EquipeMembro`, porque só o `POST` de inclusão devolve esse objeto. Não bloqueia a fase (a inclusão funciona; a listagem é honesta, sem inventar dado). | Candidato a RFC — `GET /v1/equipes/{equipeId}/membros` análogo ao padrão de `listarJornadasVinculo`. |
| 2026-07-27 | F9b / A2 (achado de contrato, T7 — reforça achado já registrado por F2/F6 acima) | `ImportacaoCriar.conteudoRef` (RFC-007, já resolvida — o campo existe no contrato e em `packages/contracts/schema.sql`) pressupõe um arquivo já enviado a um armazenamento de objetos, mas **nenhuma tag do `openapi.yaml` expõe um endpoint de upload** para obter essa referência a partir de um CSV/XLSX real selecionado no navegador — mesma lacuna de infraestrutura que `worker/tarefas/importacoes.py` já documenta (linha "F6 / A2", acima) para `templates`/fotos. `apps/web/src/componentes/paineis/cadastros/colaboradores/dialogo-importar-colaboradores.tsx` (T7) por isso pede `conteudoRef` como campo de texto livre, com aviso explícito na tela, em vez de um seletor de arquivo — evita inventar um protocolo de upload próprio fora do contrato. Também confirmado: `apps/web/src/lib/api/tipos.gerado.ts` ainda não declara `conteudoRef` em `ImportacaoCriar` (mesma divergência tipos-gerados-vs-contrato já registrada por F8/A1 em 2026-07-27, acima) — o gancho `useImportarColaboradores` (`apps/web/src/ganchos/use-colaboradores.ts`) contorna isso com um tipo de parâmetro estendido, convertido para o tipo gerado só na fronteira da chamada HTTP, sem regenerar o arquivo. | F14 (ou quem integrar armazenamento de objetos de verdade) para o upload; quem rodar `pnpm tipos:api` em modo escrita no fechamento da onda resolve a divergência de tipos. |
| 2026-07-27 | F9b / A3 (achado, confirmado em HEAD limpo — nenhum dos dois arquivos comparados foi tocado por A3) | `pnpm tipos:api:check` (`apps/web`) falha: `src/lib/api/tipos.gerado.ts` está desatualizado em relação a `packages/contracts/openapi.yaml`. Regenerei localmente em `scratchpad/` (nunca sobrescrevi o arquivo real, regra do PCF) e comparei: faltam pelo menos `conteudoRef` em algum schema de importação (reforça a linha "F9b / A2, T7" acima), `metas` em `ListaMarcacao` (a RFC-011 "metas em listarMarcacoes", commit `ec94906`, mudou o contrato mas não regenerou os tipos), `configurar`/`reabrir`/`ler_sensivel` no enum `acao` de `Permissao`, e `sincronizacao_terminal` no enum `tipo` de `ProcessamentoAssincrono` (RFC-013/F6). `git status --short` confirma os dois arquivos (`openapi.yaml` e `tipos.gerado.ts`) sem diferença no meu working tree — a divergência já existia no commit `6350709` (HEAD ao começar esta tarefa), não foi introduzida nesta fase. | Quem rodar `pnpm tipos:api` em modo escrita no fechamento da onda (mesmo apontado já 2x acima para o achado do `conteudoRef`) — um só `pnpm tipos:api` + commit resolve as três linhas de uma vez. |
| 2026-07-27 | F9b / A3 (achado, bloqueia `criarJornada` sempre — fora do ownership de A3, `apps/api/**`) | `POST /v1/jornadas` (`criarJornada`, ownership de A1/F3 — `apps/api/app/routers/jornadas.py`) devolve **sempre `500`** (`PONTO-INT-001`), reproduzido com o corpo mínimo válido (`empresaId`/`codigo`/`nome`/`tipo`/`vigenciaInicio`, sem nenhum campo extra). Causa raiz confirmada pelo *traceback* do servidor: `_montar_jornada` (linha 38) chama `contrato.Jornada.model_validate(jornada, from_attributes=True)`, e o contrato exige `noturnoInicio`/`noturnoFim` como `string` (padrão `HH:MM`), mas a coluna do banco é `TIME` com default `'22:00:00'`/`'05:00:00'` (`jornadas.noturno_inicio`/`noturno_fim`, `NOT NULL`) — o SQLAlchemy devolve `datetime.time` nesses dois campos e nada os converte para string antes da validação Pydantic, então TODA criação de jornada quebra, não só quando o cliente omite os dois campos (a coluna nunca é `NULL`, tem default). Bloqueia qualquer fluxo que precise de uma jornada nova pela API (inclusive o meu próprio dado de apoio de E2E, T9/T10/T11 — contornei SEM tocar `apps/api/**`, ver `apps/web/e2e/apuracao/apuracao.spec.ts`, que evita `criarJornada` e usa um dia `nao_apurado`/`com_ocorrencia`, já suficiente para exercitar a grade e o ciclo de tratamento). | F3/A1 (ownership do arquivo) — formatar `noturno_inicio`/`noturno_fim` (e conferir se `Horario.entrada`/`saida` no mesmo router tem o mesmo problema) antes de `model_validate`, ou declarar um `field_serializer`/`BeforeValidator` no schema gerado. |
| 2026-07-27 | F9b (verificação final, Orquestrador — achado relacionado ao bug de `empresas.py` corrigido nos "Resolvidos" abaixo, mas NÃO corrigido aqui de propósito, para não expandir a correção além do que bloqueava F9b) | Mesma classe de bug do `CAMPOS_ORDENAVEIS` de `app/organizacao/empresas.py` (chaves em snake_case, mas `ordenar` é um parâmetro público em camelCase, sem conversão de caixa em `app/organizacao/paginacao.py::campo_e_direcao`) também existe em `app/organizacao/unidades.py` (`CAMPOS_ORDENAVEIS_UNIDADE`, `CAMPOS_ORDENAVEIS_REDE`) e `app/organizacao/estrutura.py` (departamentos, centros de custo, cargos, equipes — quatro dicionários) para o campo `criadoEm` (chave real: `"criado_em"`). Hoje não bloqueia nada: nenhum hook de F9b usa `ordenar=criadoEm` nesses cinco recursos (todos ordenam por `nome`, palavra única, sem o problema de caixa) — mas `ordenar=criadoEm:...` quebraria com `400 PONTO-VAL-005` do mesmo jeito que `razaoSocial` quebrava para empresas, para qualquer chamador futuro (inclusive um usuário via `?ordenar=` direto na API, já que é um parâmetro público documentado no `openapi.yaml`). | F2 (ownership de `apps/organizacao/**`) — considerar corrigir os cinco dicionários restantes na mesma revisão, ou um helper compartilhado que derive a chave certa a partir do nome do atributo do contrato, para a classe inteira de bug não se repetir a cada recurso novo. |
| 2026-07-28 | F10 / A4 (T14, PCF §2.7 item 2 — acrescenta ao achado já registrado por F8/A3 acima, não duplica) | Confirmado de novo, agora do lado de quem MATERIALIZA a solicitação (não só de quem a cria): `apps/api/migrations/seed_dev.py::TIPOS_SOLICITACAO` semeia `exige_anexo=True` para `abono_falta` e `afastamento` (as duas linhas com esse campo verdadeiro na tupla de fábrica), e `tipos_solicitacao.exige_anexo`/`tipos_afastamento.exige_documento` existem como colunas reais no schema (seção 11/7), mas **nenhuma rota de `packages/contracts/openapi.yaml` aceita ou devolve um `anexos.id`/`documentos.id` associável a uma `Solicitacao`** — `SolicitacaoCriar` não tem campo de anexo, e `anexos` (schema.sql, seção 11) é referência polimórfica (`entidade`/`entidade_id`) SEM FK, sem nenhuma operação HTTP que a popule. O despachante genérico desta fase (`app.workflow.solicitacoes.materializacao`, A1, T4) e o ramo `ferias`/`folga` (`app.workflow.solicitacoes.afastamentos`, A4, T12) por isso **nunca verificam `exige_anexo`** antes de materializar — tecnicamente não há como a API impor essa regra hoje, para nenhuma categoria. **Não implementei** a função interna de storage opcional que o PCF sugere (`salvar_anexo`, reaproveitando `app.comum.armazenamento` de A2): meu ownership de arquivo nesta fase é explicitamente um único arquivo (`apps/api/app/workflow/solicitacoes/afastamentos.py`, PCF §5) e não há um local já autorizado para um módulo novo de anexo dentro do pacote de A1 sem extrapolar essa fronteira — decisão deliberada de não inventar um segundo caminho de ownership, registrada aqui em vez de resolvida sozinha. | F10 → RFC de contrato (decidir se upload é campo novo em `SolicitacaoCriar`/`AfastamentoCriar` ou endpoint dedicado `/v1/anexos`); se o orquestrador quiser a função interna de storage antes da RFC, precisa indicar o arquivo/ownership exato para ela |
| 2026-07-28 | F10 / A4 (T13, achado ao provar reaproveitamento — não corrigido, `apps/api/app/apuracao/**` é ownership congelado de F4) | `app.apuracao.dominio.servico::apurar_dia` (docstring do módulo, ponto 4) documenta explicitamente que `Tratamento` de categoria `afastamento` **não tem efeito numérico** no cálculo do dia — só `inclusao_marcacao`/`desconsideracao_marcacao`/`abono` entram em `tratamentos_consumidos` (linhas ~332-353); `afastamento` cai no comentário "sem efeito numérico aqui ... já é insumo de F3 (via `afastamentos`)". Isso diverge do que o PCF desta fase (§2.2) descreve para a categoria `afastamento` de `tipos_solicitacao` ("`Tratamento` com `tipo_afastamento_id` preenchido ... correção retroativa de um dia já apurado, o mesmo padrão de qualquer outro tratamento") sem especificar qual seria o efeito observável esperado na apuração — e como o PCF também fixa que essa categoria **não** cria linha em `afastamentos` (diferente de `ferias`), o insumo que F4 espera ("já é insumo de F3, via `afastamentos`") nunca chega a existir para este caminho. Confirmado por teste real: aprovar uma solicitação `afastamento` (retroativa) materializa e aprova o `Tratamento` corretamente (prova de reaproveitamento, critério de aceite 7), mas `apuracoes_dia.falta_minutos`/demais campos numéricos do dia não mudam — `tests/f10/afastamentos_workflow/test_abono_e_retroativo.py::test_afastamento_retroativo_...` prova só o que é verdade hoje, sem afirmar um efeito numérico que o código de F4 não produz. Não é bug — é uma lacuna de especificação entre PCF e implementação de F4 já congelada. | F10 → RFC (decidir se `afastamento` retroativo deveria ganhar tratamento numérico próprio em `apurar_dia`, ou se o PCF §2.2 deveria descrever o efeito real — hoje só "correção documentada, sem impacto de minutos") |
| 2026-07-27 | F9b (verificação final, Orquestrador) | O fluxo E2E oficial do PCF (§8: subir a stack via `docker compose ... docker-compose.dev.yml`, depois `pnpm exec playwright test`) tem um descompasso de porta não documentado: `infra/docker-compose.dev.yml` define `CORS_ORIGENS: http://localhost:${WEB_PORT:-3000},http://127.0.0.1:${WEB_PORT:-3000}` (porta **3000**), mas `apps/web/playwright.config.ts` (T1/A1) sobe seu próprio `webServer` na porta **3100** por padrão (`PLAYWRIGHT_WEB_PORT ?? 3100`, escolhida de propósito para não colidir com um `pnpm dev` já rodando em 3000). Seguindo o §8 literalmente (compose com os defaults do `.env.example` + Playwright com o `PLAYWRIGHT_WEB_PORT` default), toda chamada de rede do navegador para a API (`useEmpresas`, `useTurnos` etc.) é bloqueada por CORS — reproduzido nesta verificação; contornado localmente exportando `CORS_ORIGENS` incluindo a porta 3100 antes de subir a API. Não é um bug de código de nenhuma fase — é uma lacuna de configuração entre dois arquivos de infraestrutura/config de fases diferentes (F0/`infra/**`, congelado, e F9b/`playwright.config.ts`). | Sem fase dona — quem tiver ownership de `infra/docker-compose.dev.yml` (F0) ou revisar `playwright.config.ts` (F9b) deveria alinhar as duas portas, ou documentar explicitamente a variável `CORS_ORIGENS` extra exigida para rodar `pnpm exec playwright test` contra a stack subida pelo compose. |
| 2026-07-28 | F10 / A2 (T5, achado de contrato confirmado lendo `schema.sql` e o código congelado de F4) | `fechamentos` (`schema.sql` seção 12) **não tem coluna `equipe_id` nem `colaborador_id`**, embora `fechamentos.escopo`/`FechamentoCriar.escopo` aceitem os valores `equipe`/`colaborador` no `CHECK`/enum (só `empresa_id`/`unidade_id`/`departamento_id` existem como colunas). Além disso, `app.apuracao.tratamento.fechamento.verificar_periodo_aberto` (F4, congelado, a trava real que já funciona) só monta condição de escopo para `empresa`/`unidade`/`departamento` — não tem ramo algum para `equipe`/`colaborador`. Consequência: um `Fechamento` criado com `escopo='equipe'` ou `'colaborador'` seria persistido, mas a trava **nunca o encontraria** (a cláusula `OR` de `verificar_periodo_aberto` não tem como casar), então o fechamento não travaria nada de verdade — um fechamento "fantasma". Decisão tomada (não é correção silenciosa do contrato): `app.workflow.fechamento.servico.criar_fechamento` recusa `escopo IN ('equipe','colaborador')` com `PONTO-VAL-001`, com mensagem explicando o motivo, em vez de criar um fechamento sem efeito. | F10 → RFC de contrato se `equipe`/`colaborador` precisarem ser fechamentos reais algum dia (acrescentar as colunas a `fechamentos` e o ramo correspondente em `verificar_periodo_aberto`, ownership de F4) |
| 2026-07-28 | F10 / A1 (T2, achado de contrato encontrado durante o build — sexto achado, não coberto pelo §2.7 do PCF) | `TipoSolicitacao.etapas`/`TipoSolicitacaoCriar.etapas` é tipado `dict[str, Any]` pelo schema gerado (`openapi.yaml`: `type: object`), e o próprio EXEMPLO do contrato (`ExemploTipoSolicitacaoCriar`/`ExemploTipoSolicitacao`) mostra a forma `{"etapas": [{"ordem": N, "papel": "..."}]}` — um objeto envelopando o array. Mas `apps/api/migrations/seed_dev.py::TIPOS_SOLICITACAO` (dado de fábrica, não editável) grava a coluna `tipos_solicitacao.etapas` como um ARRAY cru `[{"etapa": N, "papel": "..."}]`, sem envelope e com a chave `etapa` em vez de `ordem` — confirmado lendo a linha real no banco (`ponto_f10_a1`, tenant `seeg`). `contrato.TipoSolicitacao.model_validate(linha, from_attributes=True)` quebraria com `ValidationError` para TODO tipo de fábrica (Pydantic recusa `list` onde espera `dict`). Resolvido em `apps/api/app/workflow/solicitacoes/tipos.py` (ver docstring do módulo): a coluna sempre guarda o array cru (mesma forma do dado de fábrica); a camada HTTP aceita `etapas` do cliente em qualquer uma das duas formas e sempre RESPONDE `{"etapas": [...]}"`, batendo com o exemplo do contrato. `app.workflow.solicitacoes.afastamentos` (A4, T12) já usa a mesma convenção de array cru para os `TipoSolicitacao` que cria diretamente, confirmando que a leitura é consistente entre os dois agentes. | F10 → RFC de contrato se alguém quiser que o schema declare `etapas` como `array` diretamente em vez de `object` envelopado (mudança incompatível, então é RFC, não silenciosa) |
| 2026-07-28 | F10 / A1 (T2, achado de contrato — mesmo padrão que o PCF de F4 já documentou para `TratamentoCriar.vinculoId`) | `SolicitacaoCriar.vinculoId` é opcional no contrato, mas a materialização via `criar_tratamento` (F4) exige um `vinculoId` não nulo. Confirmado lendo `apps/web/src/app/eu/solicitacoes/nova/page.tsx` (F8, já concluída): o formulário real nunca envia `vinculoId` — só `tipoSolicitacaoId`, `colaboradorId`, `descricao` e `dataReferencia` OU `dataInicio`/`dataFim`. Decisão fixada em `app.workflow.solicitacoes.servico.criar_solicitacao`: resolve o vínculo do lado do servidor a partir de `colaboradorId` (réplica própria do padrão `_vinculo_ativo` de `app.marcacao.pipeline.ingestao`, F5) quando o cliente não informa um, e grava o resultado em `solicitacoes.vinculo_id` — a materialização (T4) nunca precisa resolver de novo. | Nenhuma — decisão já implementada; registrado para quem alterar `criarSolicitacao`/F8 no futuro saber que o campo é preenchido no servidor |
| 2026-07-28 | F10 / A1 (T2/T4, achado de contrato — segunda lacuna do mesmo tipo) | Para a categoria `afastamento` (retroativo), a tabela do PCF §2.2 exige `Tratamento.tipo_afastamento_id` preenchido, mas não existe campo dedicado em `SolicitacaoCriar`, e `payload` é `additionalProperties: true` sem formato fixo. Decisão fixada: o cliente informa `payload.tipoAfastamentoId` (UUID como string) para essa categoria — validado em `criarSolicitacao` (`PONTO-VAL-001` se ausente, falha cedo em vez de falhar tarde na aprovação final). Nenhum cliente real (F8) envia esse campo ainda. **Confirmado independentemente por A4** (`tests/f10/afastamentos_workflow/test_abono_e_retroativo.py`), que usa exatamente a mesma convenção `payload: {"tipoAfastamentoId": ...}` ao escrever os testes de T13 — os dois agentes convergiram na mesma leitura da lacuna sem coordenação direta. | F10 → RFC de contrato (acrescentar `tipoAfastamentoId` como campo próprio de `SolicitacaoCriar`, ou aceitar a convenção de `payload` como está) |
| 2026-07-28 | F10 / A1 (T4, decisão de design não resolvida explicitamente pelo PCF) | Um `Tratamento` só modela UM dia (`tratamentos.data_referencia`, `NOT NULL`, singular), mas `apps/web/src/app/eu/solicitacoes/nova/page.tsx` (F8) trata `compensacao`/`afastamento` (além de `ferias`/`folga`) como categorias de INTERVALO (`dataInicio`/`dataFim`, nunca `dataReferencia`). O PCF §2.2/T4 não resolve explicitamente o que fazer quando uma categoria mapeada para `tipo_tratamento_id` chega com um intervalo em vez de um dia único. Decisão fixada em `app.workflow.solicitacoes.materializacao._dias_da_solicitacao`: materializa UM `Tratamento` POR DIA do intervalo (todos com a mesma `solicitacao_id`), chamando `criar_tratamento`/`decidir_tratamento` (F4) uma vez por dia — nunca inventa uma tabela nova para "tratamento de intervalo". Consequência aceita: múltiplos eventos `ajuste.aprovado`/`ajuste.reprovado` (um por dia) para uma única solicitação aprovada/reprovada quando o intervalo tem mais de um dia. Testado (`tests/f10/solicitacoes/test_materializacao.py::test_compensacao_materializa_um_tratamento_por_dia_do_intervalo`, 3 dias → 3 tratamentos). | F10 → RFC se o produto quiser um resultado consolidado (um único evento/confirmação) para solicitações de intervalo mapeadas a tratamento, em vez de N tratamentos/eventos independentes |
| 2026-07-28 | F10 / A1 (T3, reconciliação de contrato — decisão de lógica de aplicação, documentada por não haver melhor lugar) | O schema gerado descreve `Aprovacao.aprovadorUsuarioId` como "Usuario que decidiu" (leitura natural: só populado no momento da decisão), mas o PCF (T3) pede que `listarAprovacoesPendentes` (a fila de itens PENDENTES) filtre por `Aprovacao.aprovador_usuario_id = sujeito.usuario_id` — coluna nula em toda etapa nunca decidida tornaria essa fila sempre vazia. Decisão fixada em `app.workflow.aprovacoes.servico`/`resolucao.py`: `aprovador_usuario_id` é PRÉ-ATRIBUÍDO no momento em que a etapa é criada, via `colaborador_gestores` (F2, `tipo='imediato'` para papel `gestor`, `tipo='rh'` para papel `rh` — a própria tabela já documenta esse propósito no comentário: "Define a árvore de subordinados... e a cadeia de aprovação das solicitações"). Papéis sem `tipo` correspondente (`diretoria`/`sistema`) ficam com a coluna `NULL` na criação; `listarAprovacoesPendentes` enxerga essas etapas casando `papel` contra `Sujeito.perfis` (RBAC já resolvido por F1), nunca por uma segunda consulta a `app.identidade.rbac`. Testado ponta a ponta (`tests/f10/aprovacoes/test_servico.py`). | F10 → RFC se essa reconciliação estiver errada e o time de produto quiser uma tela de "minha fila" que dependa de atribuição explícita em vez de resolução automática por hierarquia/perfil |
| 2026-07-28 | F10 / A2 (T7/T9, tensão interna do próprio PCF desta fase, resolvida por leitura ampla — documentando a decisão, não a escondendo) | O PCF F10 tem duas afirmações que se contradizem sobre o alcance de leitura em `apuracoes_dia`: §3 ("leituras obrigatórias") e §4 ("Produz", resumo de tabelas) dizem, batendo duas vezes, "leitura apenas em `tipos_afastamento`, `apuracoes_dia` (**só a coluna `fechamento_id`**)"; mas a tarefa T7 (geração do espelho) instrui explicitamente "monta `conteudo` ... lendo `apuracoes_dia`/`apuracao_componentes`/`tratamentos` do vínculo no intervalo do período (só leitura)" e o critério de aceite exige "a soma dos totais do `conteudo` bate com os totais das colunas (`totalTrabalhadoMinutos` etc.)" — **impossível de cumprir lendo só `fechamento_id`**. Segui T7 (a instrução mais específica e a que o critério de aceite torna inequívoca): `app/workflow/fechamento/espelho.py::_montar_conteudo` lê as colunas numéricas inteiras de `apuracoes_dia`/`apuracao_componentes` (nunca escreve). Documentado no topo do próprio módulo. Tensão simétrica na escrita: a proibição 5 ("Não escreva em `apuracoes_dia`... a escrita acontece exclusivamente via os módulos de F4") é absoluta e repetida, mas a descrição de T9 diz "chama a trava real (marca `apuracoes_dia.fechamento_id` para todo o escopo, `status='fechado'`...)". Aqui segui a proibição 5, não a prosa de T9: confirmei lendo `verificar_periodo_aberto` por inteiro que a trava **nunca consulta** `apuracoes_dia.fechamento_id`/`status` (só `Fechamento.status='fechado'` + `Periodo`/escopo) — escrever essas duas colunas seria puramente cosmético, sem necessidade funcional, então `apps/worker/worker/tarefas/fechamento.py::processar_fechamento` só **lê** `apuracoes_dia` (para compor `hash_conteudo`) e nunca grava nela. Documentado inline no worker. | F10 (orquestrador) — se a intenção fosse mesmo popular `apuracoes_dia.fechamento_id`/`status` por algum motivo não capturado em `verificar_periodo_aberto` (ex.: um consumidor futuro de relatório que filtre por essa coluna), é uma decisão nova, não implícita nesta fase — considerar RFC ou PCF de esclarecimento antes de uma fase futura (F11?) depender dessa coluna estar preenchida |
| 2026-07-30 | F12 / planejamento (PCF, escrito a partir de `docs/leiaute-afd-aej.md`, T0) | A correção de premissa da T0 sobre o leiaute do AFD ("largura fixa por posição, sem delimitador; CRC-16 só nos tipos 1–5; SHA-256 só no registro tipo 7, encadeado — nunca hash de arquivo inteiro exigido pela norma") chegou a `FASES-E-AGENTES.md` linha 320, mas **não** a cinco outros lugares que ainda descrevem o AFD com a premissa antiga e incorreta ("campos separados por barra vertical ... CRC-16 por registro e SHA-256 do arquivo"): (1) `packages/contracts/openapi.yaml`, descrição da operação `gerarAfd` (~linha 14571); (2) `packages/contracts/openapi.yaml`, `description` do schema `AfdArquivo` (~linha 34662); (3) `packages/contracts/schema.sql`, `COMMENT ON TABLE afd_arquivos` (~linha 3246); (4) `packages/contracts/schema.sql`, `COMMENT ON COLUMN marcacoes.crc16` (linha 1980) e o verbete "CRC-16" de `packages/contracts/glossario.md` (linha 153), ambos dizendo "conforme o leiaute do AFD" quando na verdade `app.marcacao.dominio.nsr.crc16` implementa CRC-16/ARC (polinômio `0xA001`), não o CRC-16/CCITT-TRUE (KERMIT, `0x1021`) que a norma exige — a própria docstring da função de F5 já avisa "não certificado... a conformidade byte a byte é conferida pela F12"; (5) `apps/worker/worker/tarefas/fiscal.py`, docstring do módulo (~linha 7), mesma frase da premissa antiga. Nenhum dos cinco impede o build de F12 (é só prosa descritiva, nenhum type/enum/obrigatoriedade de schema muda) — a fonte da verdade usada pelo PCF de F12 é sempre `docs/leiaute-afd-aej.md`, nunca essa prosa. Registrado aqui em vez de corrigido diretamente porque toca `packages/contracts/**` (que exige RFC pela letra de `docs/rfc/README.md`, ainda que seja só limpeza cosmética de texto). | F12/orquestrador — decidir se vale uma RFC de limpeza cosmética dos cinco textos, ou se fica como está até a próxima edição que já for tocar esses arquivos por outro motivo |
| 2026-07-30 | F12 / planejamento (PCF) | O exemplo de nome de arquivo do evento `afd.gerado` em `packages/contracts/events.yaml` (`nomeArquivo: AFD_60258502000149_20260701_20260731.txt`) não bate com a regra 10 da seção 6 de `docs/leiaute-afd-aej.md` (leitura literal do leiaute oficial do MTE): a norma pede a junção de `"AFD"` + número de registro no INPI + CNPJ do empregador + `"REP_P"`, sem mencionar datas de período e sem separador exato especificado (**NÃO CONFIRMADO**, a própria T0 já marcou isso como lacuna). O PCF de F12 (§2.10) fixa uma convenção de nome que reconcilia as duas fontes (`AFD_<numeroInpi>_<cnpjEmpregador>_REP_P_<periodoInicio>_<periodoFim>.txt`), satisfazendo os três componentes exigidos pela norma e mantendo o padrão `_` + datas do exemplo do contrato. Não bloqueia o build. | F12/orquestrador — cosmético; considerar atualizar o exemplo de `events.yaml` para incluir o número do INPI na próxima janela de manutenção do contrato |
| 2026-07-30 | F12 / A2 (T8, achado de contrato — schema sem coluna para um campo obrigatório do leiaute) | O tipo "08" do AEJ (Identificação do PTRP) exige `emailDesenv` preenchido (`docs/leiaute-afd-aej.md` §10) — tipado `N` no leiaute oficial, provável erro de digitação da norma, tratado como texto livre sem "corrigir" a observação, conforme o próprio PCF instrui. Mas **nenhuma tabela lida por esta fase tem coluna para o e-mail de contato do desenvolvedor**: `rep_ps` guarda `cnpj_desenvolvedor`/`razao_social_desenvolvedor`, sem e-mail; nenhuma outra tabela do escopo desta fase tem esse dado. `app.fiscal.aej.gerador._EMAIL_DESENVOLVEDOR_PLACEHOLDER` usa um valor fixo no domínio reservado `.invalido` (RFC 2606), claramente não uma fonte real, documentado inline para nunca ser confundido com dado real do desenvolvedor. **Isto significa que nenhum AEJ gerado por esta fase está pronto para submissão real a uma fiscalização** até esse campo ganhar uma fonte de dado de verdade. | F12/orquestrador — RFC para acrescentar uma coluna (`rep_ps.email_desenvolvedor` ou variável de ambiente `FISCAL_EMAIL_DESENVOLVEDOR`, mesmo padrão de `FISCAL_CERTIFICADO_PFX_PATH`) antes de qualquer geração de AEJ destinada a uso real |
| 2026-07-30 | F12 / A2 (T9, achado de arquitetura — assinatura de tarefa do worker fixada pelo andaime não carrega um campo do contrato) | `apps/worker/worker/tarefas/fiscal.py::gerar_aej` (assinatura FIXADA pelo scaffold da Fase 0, PCF F12 §5 proíbe mudar sem RFC) recebe `tenant_id, empresa_id, inicio, fim, assinar, solicitante_id` — sem `incluirBancoHoras` (campo de `AejCriar`) nem `arquivo_id` (o id da linha de controle que `solicitar_geracao_aej` já criou em `aej_arquivos` antes de enfileirar). Consequência 1: a tarefa sempre chama `app.fiscal.aej.gerador.gerar_aej_arquivo(..., incluir_banco_horas=True, ...)` — o valor de `AejCriar.incluirBancoHoras` enviado pelo cliente nunca chega ao caminho assíncrono real (só participaria de uma validação síncrona, que `solicitar_geracao_aej` hoje não faz, já que o parâmetro não tem onde pousar). Consequência 2 (contornada, não bloqueante): sem `arquivo_id`, `gerar_aej_arquivo` re-localiza a linha `status='gerando'` mais recente pela chave `(tenant_id, empresa_id, periodo_inicio, periodo_fim)` — seguro porque `PONTO-FISC-002` garante no máximo uma geração em andamento por essa chave, mas é um acoplamento implícito que uma mudança futura na checagem de FISC-002 poderia quebrar silenciosamente. Documentado inline nos dois módulos. | F12/orquestrador — RFC para acrescentar `incluir_banco_horas`/`arquivo_id` à assinatura de `gerar_aej` (e, por simetria, considerar se `gerar_afd` também deveria receber `arquivo_id` em vez de re-localizar) |
| 2026-07-30 | F12 / A2 (T8, decisão de leiaute documentada — leitura literal da tabela vs. paráfrase do PCF) | O trailer tipo "99" do AEJ: a tabela literal de `docs/leiaute-afd-aej.md` §10 lista 8 campos de contagem cobrindo os tipos "01" a "08" (inclui `qtRegistrosTipo01`, sempre trivialmente 1), mas a paráfrase do PCF F12 (§6, T8) diz "trailer tipo '99' (contagens por tipo, sem contar a si mesma **nem o tipo '01'**)" — lida isoladamente, sugere que a contagem do cabeçalho estaria ausente. `app.fiscal.aej.registros.TrailerAej`/`montar_registro_99` seguem a tabela literal (fonte primária, extraída do PDF oficial pela T0 com checagem de consistência de tamanho de campo) em vez da paráfrase: emite os 8 campos, incluindo `qtRegistrosTipo01=1`. Documentado no docstring de `TrailerAej`. Não bloqueia nada — só registrado para o caso de um AFD/AEJ de referência real revelar que a leitura correta era a da paráfrase. | Sem fase dona; revisitar só se uma referência externa (AEJ de outro sistema homologado) mostrar leitura diferente |
| 2026-07-30 | F12 / A1 (T6, mesma classe de achado que A2 já registrou para `gerar_aej` — linha acima) | `apps/worker/worker/tarefas/fiscal.py::gerar_afd` (assinatura FIXADA pelo scaffold da Fase 0) recebe `tenant_id, rep_p_id, inicio, fim, assinar, solicitante_id` — sem `nsrInicial`/`nsrFinal` ("como alternativa ao período", campos de `AfdCriar`) nem `fracionar`/`tamanhoFracaoRegistros`. Consequência: `app.fiscal.afd.gerador.solicitar_geracao_afd` (parte síncrona do endpoint HTTP) recusa esses dois modos com `PONTO-VAL-001` antes de enfileirar — não finge suportar algo que não alcançaria o worker. `app.fiscal.afd.gerador.gerar_afd_arquivo` (a função de domínio) IMPLEMENTA e TESTA os dois modos quando chamada diretamente (`tests/f12/afd/test_gerador.py::test_fracionamento_produz_n_arquivos_com_faixas_complementares`), então a capacidade existe — só não alcança o HTTP pela fila. Diferente da solução de A2 para `gerar_aej` (re-localizar uma linha de controle pré-criada por chave natural): não apliquei esse padrão para AFD porque o fracionamento pode produzir 1..N linhas de `afd_arquivos` por chamada, e uma única linha pré-criada pelo router não teria como representar essa relação 1-para-N — por isso `gerar_afd_arquivo` é autocontida (cria toda(s) a(s) linha(s) do zero, nunca reaproveita uma linha `status='gerando'` preexistente). | F12/orquestrador — mesma RFC sugerida por A2 (linha acima) resolveria os dois de uma vez: acrescentar `nsr_inicial`/`nsr_final`/`fracionar`/`tamanho_fracao_registros` à assinatura de `gerar_afd` |
| 2026-07-30 | F12 / A1 (T1/T7, achado de contrato — campo opcional no schema, `NOT NULL` no banco) | `RepPCriar.cnpjDesenvolvedor`/`razaoSocialDesenvolvedor` não são `required` no schema (`packages/contracts/openapi.yaml`; o próprio `ExemploRepPCriar` não os inclui), mas `rep_ps.cnpj_desenvolvedor`/`razao_social_desenvolvedor` são `NOT NULL` no banco (`packages/contracts/schema.sql` seção 8). Decisão fixada em `app.fiscal.rep_p.servico.criar_rep_p`: quando ausentes, preenche com a identidade fixa da SEEG (`60258502000149` / "SEEG Servicos de Tecnologia da Informacao LTDA" — mesmo CNPJ que `ExemploRepP`/`ExemploListaRepP` do próprio contrato já usam como resposta de exemplo), porque o desenvolvedor do REP-P é sempre a SEEG, para qualquer empresa que opere o software. Documentado no docstring do módulo e testado (`tests/f12/rep_p/test_servico.py::test_criar_rep_p_sem_cnpj_desenvolvedor_usa_identidade_fixa_da_seeg`). Não bloqueia nada — só registrado porque um agente que confiasse só no `required` do schema poderia tentar inserir `NULL` e ser surpreendido pelo `NOT NULL` do banco. | F12/orquestrador — cosmético; considerar marcar os dois campos como `required` em `RepPCriar` (já que na prática sempre é a SEEG) na próxima janela de manutenção do contrato |
| 2026-08-05 | F14 / A2 — **achado crítico, contradiz premissa do PCF e do ADR-014** | O PCF da F14 (§2) e o ADR-014 afirmam "F8 já está construída — sinal real de geolocalização, prova de vida e similaridade facial via `analise-facial-edge` já existe hoje". Falso, confirmado por leitura de código E por `git log` (só o commit `480854d`, Fase 0, jamais editado desde então): as três operações de `apps/facial-svc/facial/rotas/biometria.py` (`/enroll`, `/verificar`, `/liveness`) continuam `501 PONTO-INT-005`, com a própria docstring dizendo "Stubs na Fase 0" / "implementação virá na F2"/F7. Confirmado também que nenhum chamador existe: `grep -rn "facial_svc_url"` em `apps/api` mostra o campo declarado em `Configuracao` mas usado em ZERO lugares; `apps/device-gw` não tem nenhum cliente para `facial-svc`. Consequência prática para A1 (score de confiança, ADR-008): `app.marcacao.pipeline.ingestao` já grava `score_facial=None`/`liveness_aprovado=None` hardcoded — o motor de composição de A1 precisa tratar ISSO como o caso real hoje (não só o caso "sinal indisponível por falta de F7" que o ADR-014 previu só para attestation/RASP/mock-location). | F2/F6/F8 (retroativo) ou uma fase dedicada de "fechar o motor facial de verdade" — nenhuma fase até aqui reivindicou isso como pendência própria; ADR-014 precisa de errata. |
| 2026-08-05 | F14 / A2 (T "decisão core/seguranca.py") | **Decisão: `core/seguranca.py` permanece travado a F1/A3, não destravado nesta fase.** O gap real (backlog 2026-08-03, duas entradas "F13/A1 [T1]") é que as ~130 rotas de F1-F12 só aceitam sessão humana (JWT), nunca OAuth/API-key, mesmo quando o contrato declara os dois esquemas. MAS a solução correta já existe, construída por F13 para o próprio uso, sem tocar `core/seguranca.py`: `app.integracoes.webhooks.seguranca.exigir_permissao_ou_escopo(*, permissao, escopo)` — combinador genérico (zero lógica específica de webhooks) que tenta sessão humana primeiro (`exigir_permissao`, reaproveitado por inteiro) e cai para `exigir_escopo` (A1/F13) quando não há sessão humana autenticada. Generalizar isso para as rotas de F1-F12 exigiria trocar `Depends(exigir_permissao(...))` por `Depends(exigir_permissao_ou_escopo(...))` no PARÂMETRO `sujeito` de cada handler (o tipo devolvido muda de `Sujeito` para `ContextoAcesso`) e ajustar toda leitura de `sujeito.tenant_id`/`sujeito.usuario_id` no CORPO de cada handler — isso é editar lógica de negócio dentro de cada rota, explicitamente fora do meu ownership ("routers/*.py: editar só para acrescentar Depends()"). Sem valor de produto validado hoje (nenhum cliente OAuth/API-key pediu acesso às rotas antigas) para justificar tocar ~130 handlers de uma vez sem PCF próprio. | Fase dedicada (RFC + PCF), se algum cliente de integração precisar acessar rotas de F1-F12 — mover `exigir_permissao_ou_escopo` de `app/integracoes/webhooks/seguranca.py` para `app/comum/autenticacao_cliente.py` (já genérico, só precisa mudar de endereço) e aplicar rota por rota, uma de cada vez, com teste de regressão provando 100% do comportamento humano preservado. |
| 2026-08-05 | F14 / A2 (retrofit de idempotência — decisão de escopo) | Idempotência genuína (replay sem reexecutar, `409` em conflito) foi aplicada de verdade a uma AMOSTRA representativa de 10 rotas de criação de F1-F12 (`POST /v1/{colaboradores,empresas,unidades,departamentos,contratos,escalas,feriados,horarios,afastamentos,dispositivos}`) via `app.comum.idempotencia_middleware.IdempotenciaRetrofitMiddleware` — um middleware ASGI (não `Depends()` por rota) porque `abrir_operacao`/`concluir_operacao` (`idempotencia_generica.py`) precisam rodar ANTES/DEPOIS da lógica de negócio na MESMA transação dela, o que exigiria editar o CORPO de cada handler (proibido pelo meu ownership, "só Depends()"). O middleware resolve isso por fora, sem tocar nenhum router. As ~120 rotas de escrita restantes de F1-F12 (fora dessa amostra e fora de `marcacoes.py`/`webhooks.py`/`integracoes.py`/`admin.py`, que já têm mecanismo próprio) continuam sem dedup real — o cabeçalho `Idempotency-Key` já é exigido pelo contrato (schema gerado), então reenviar sem ele já falha (`422` genérico hoje, não `PONTO-IDEM-001`), mas reenviar COM a mesma chave ainda duplica o recurso. | Rollout mecânico: estender `ROTAS_COM_DEDUP_REAL` (`app/comum/idempotencia_middleware.py`) rota por rota — cada adição é aditiva (uma linha na allowlist), testável isoladamente, sem tocar nenhum router. |
| 2026-08-05 | F14 / A2 (retrofit de rate limiting — cobertura) | `exigir_limite_taxa_sessao` (`app/comum/limitador_taxa.py`, novo) foi aplicado via `dependencies=[Depends(...)]` a TODAS as ~110 operações de escrita (POST/PUT/PATCH/DELETE) de 22 routers de F1-F12 (afastamentos, aprovacoes, apuracoes, banco_horas, biometria, colaboradores, contratos, dispositivos, empresas, escalas, espelhos, fechamentos, feriados, fiscal, jornadas, organizacao, relatorios, solicitacoes, terminais, tenants, tratamentos, unidades) — verificado por script de auditoria que casa cada decorator `post/put/patch/delete` com sua própria dependência (não só contagem agregada; um bug do meu primeiro script de retrofit trocou a dependência entre um decorator de escrita e um `GET` vizinho em 4 arquivos, corrigido e reverificado). Deliberadamente EXCLUÍDOS: `marcacoes.py` (rota mais quente do sistema; F5 já tem idempotência própria, limite de taxa aí é decisão de produto que não me cabe tomar sem calibração real), `webhooks.py`/`integracoes.py`/`admin.py` (F13, já têm `exigir_limite_taxa` próprio para `ClienteAutenticado`), `lgpd.py` (A3), `sso.py`/`auth.py` (pré-sessão, sem `Sujeito` resolvido ainda). Limite default: `Configuracao.rate_limit_sessao_por_minuto=300`, chave `tenant_id:usuario_id`, falha aberta se Redis cair (mesmo espírito de `exigir_limite_taxa` original). | Nenhuma ação necessária — decisão de cobertura documentada. Se marcações precisar de limite de taxa por sessão humana algum dia, é decisão de produto (calibração de limite para o maior volume do sistema), não mecânica. |
| 2026-08-05 | F14 / A2 (achado de enumeração, fora do ownership — `app/marcacao/**` é A1/F5) | `app.marcacao.comprovantes.consulta.listar_comprovantes_recentes` verifica existência do colaborador (`404 PONTO-REC-001` se ausente) ANTES de `exigir_alcance` (`403 PONTO-PERM-002` se o colaborador existe mas está fora do alcance hierárquico do gestor/RH chamador) — um usuário autenticado com escopo restrito consegue distinguir "este UUID de colaborador existe neste tenant" (403) de "não existe" (404), side-channel de enumeração dentro do próprio tenant. Achado isolado: `exigir_alcance` (`app/core/seguranca.py`) só é chamado neste UM ponto do sistema inteiro (`grep -rn exigir_alcance app/`), não é um padrão espalhado. Auditoria mais ampla desta sessão (`tests/f14/hardening/test_protecao_enumeracao.py`) confirma que o resto do sistema está protegido por construção: todo identificador de recurso no contrato é UUID v4 (nunca sequencial) e RLS torna "não existe" e "existe em outro tenant" indistinguíveis (mesmo 404) em toda consulta que não filtra `tenant_id` explicitamente. | `app/marcacao/comprovantes/consulta.py` (A1/F5) — inverter a ordem (`exigir_alcance` antes da consulta de existência) ou devolver `404` genérico também no caso de alcance insuficiente, quando alguém tocar este arquivo de novo. |
| 2026-08-05 | F14 / A2 (achado de ambiente, mTLS facial-svc — reproduzido de forma isolada e determinística) | Em TLS 1.3, `ssl.SSLContext.verify_mode = CERT_REQUIRED` do lado servidor NÃO recusou um handshake de cliente sem certificado neste ambiente (Windows, Python 3.12.x, OpenSSL 3.0.16) — a conexão completou como se mTLS estivesse desligado, silenciosamente (reproduzido com `http.server`+`ssl.SSLContext` puro E com `uvicorn.Config`, mesmo resultado). Sob TLS 1.2 (mesmo teste, mesmos certificados), a recusa aconteceu corretamente. `facial/servidor.py::opcoes_ssl_uvicorn` (novo, F14/A2) pina `ssl_context_factory` para forçar `maximum_version=TLSv1_2` como mitigação defensiva — mTLS com teto em TLS 1.2 é seguro e amplamente usado em produção. Não investiguei a causa raiz (falta de tempo nesta sessão) nem confirmei se o mesmo acontece em Linux (o ambiente real de produção, containers `python:3.12-slim`, provavelmente OpenSSL diferente). | Quem primeiro subir `facial-svc` com mTLS de verdade em `hml`/`prd` (F7, provavelmente): revalidar o comportamento de TLS 1.3 no ambiente Linux real antes de remover o teto em `facial/servidor.py::_teto_tls_1_2`; se TLS 1.3 se comportar corretamente lá, a causa é específica deste Windows/OpenSSL, documentar e remover o teto. |
| 2026-08-05 | F14 / A2 (achado de infraestrutura de teste, sessão concorrente) | O `REDIS_URL`/`PONTO_TEST_REDIS_URL` db=2 (escolha inicial deste agente, dentro da faixa 0-15 orientada) colidiu com outro agente desta MESMA onda (A1 ou A3, rodando em paralelo no mesmo túnel SSH/Redis compartilhado) — um teste de webhooks que dependia de um job `arq` enfileirado por MIM encontrou um estado inconsistente (`RuntimeError: job not found` + `no running event loop` num cleanup) porque o outro agente também escrevia/limpava o mesmo db. Troquei para db=12 e o mesmo teste passou isoladamente. Não é bug de código — é ausência de coordenação de db Redis entre os quatro agentes desta fase (nenhum PCF atribuiu db por agente explicitamente, só "0-15, distinto por agente" em prosa). **Achado separado, causa raiz identificada (não é o mesmo problema):** no meio da sessão, uma bateria de `pytest tests/f10 tests/f11 tests/f12` travou/errou em cascata (`psycopg.errors.ConnectionTimeout`) — a causa raiz era mais simples e não tinha nada a ver com carga concorrente: meu PRÓPRIO túnel SSH (`ssh -L 15432:...`) morreu silenciosamente em algum ponto da sessão (processo `ssh.exe` zumbi, ainda segurando a porta local sem encaminhar nada — `bind: Address already in use` ao tentar reabrir, confirmado com `Get-Process ssh`/`Stop-Process` + reabertura). Depois de matar o processo zumbi e reabrir o túnel, a MESMA suíte (`tests/f14/hardening`, 24 testes) passou 100% de primeira. | Fase de hardening/infra futura, ou o próximo PCF multi-agente — atribuir db Redis por agente EXPLICITAMENTE no PCF (não só "0-15, distinto"); para o túnel morto, considerar `ServerAliveInterval`/`ServerAliveCountMax` no `ssh` de sessões longas, e um passo de sanity-check ("a porta local ainda responde?") antes de rodar suíte completa. |
| 2026-08-05 | F14 / A2 (mTLS device-gw ↔ facial-svc — nota de escopo) | Construí a CAPACIDADE de mTLS (servidor: `facial/servidor.py`, novo ponto de entrada com TLS/mTLS real quando os três caminhos de certificado estão configurados; cliente: `gateway/dominio/cliente_facial.py::montar_cliente_facial_svc`, falha fechada se a config estiver incompleta) e provei o handshake de ponta a ponta com certificados reais gerados na hora (`tests/f14/hardening/test_mtls_facial_svc.py`, 4 testes: sem cert recusa, CA errada recusa, cert válido aceita, via `httpx` real aceita). NÃO existe, porém, nenhum CALLER real ainda (ver achado acima sobre facial-svc ser 100% stub) — `cliente_facial.py` não é chamado por nenhuma rota/fluxo hoje, é infraestrutura pronta para quando uma fase futura ligar `/verificar`/`/liveness` de verdade. Nenhum certificado real foi gerado/committed (propositalmente — `FACIAL_TLS_CERT_PATH`/`FACIAL_TLS_KEY_PATH`/`FACIAL_MTLS_CA_PATH`/`FACIAL_CERTS_DIR` todos vazios por padrão, mTLS desligado até alguém configurar em produção). | F7 (ou quem primeiro implementar `/verificar`/`/liveness` de verdade) — usar `montar_cliente_facial_svc` já pronto, e um runbook de emissão de certificado (CA própria da SEEG ou interna) para popular as quatro variáveis antes do primeiro deploy com mTLS ligado. |
| 2026-08-05 | F14 / orquestrador (achado real, resolvido no mesmo fechamento) | Ao corrigir o achado crítico de RLS de A4 (ADR-001 regra 4), a migration `0004_role_login_app` originalmente criava uma role de LOGIN chamada `ponto_app_login` — mesmo nome que `tests/f1/rbac/conftest.py` (`_ROLE_LOGIN`/`_SENHA_ROLE_LOGIN = "testeSomenteLocal_2026"`) já usa, há meses, para uma role de teste isolada com senha própria. Como roles do Postgres são **por cluster, não por banco**, rodar a migration em QUALQUER banco de teste desta sessão sobrescrevia a senha que os testes de RBAC/RLS de F1 esperavam — reproduzido como 22 falhas em cascata (`test_catalogo_permissoes`, `test_delegacao`, `test_hash_chain`, `test_matriz_perfis`, `test_escopo_hierarquico`, `test_fixture_smoke`) ao rodar a regressão combinada de F1+F2 pela primeira vez após a correção do achado de RLS. Agravado por um segundo bug na mesma migration: `upgrade()` só resetava a senha no `ELSE` (`ALTER ROLE ... PASSWORD`), nunca o atributo `LOGIN` — um `downgrade()` anterior (testado durante a própria verificação desta correção) deixava a role `NOLOGIN`, e um `upgrade()` subsequente não restaurava a capacidade de login, só a senha. Corrigido: role renomeada para `ponto_app_runtime` (nome exclusivo, nunca usado por nenhum teste) em `0004_role_login_app.py`/`infra/docker-compose.yml`/`infra/.env.example`/`tests/f14/adversarial/test_bypass_rls.py`; `upgrade()` agora faz `ALTER ROLE ... LOGIN PASSWORD ...` (os dois atributos, não só a senha); senha do `ponto_app_login` de F1 restaurada manualmente no cluster compartilhado da VPS (`ALTER ROLE ponto_app_login LOGIN PASSWORD 'testeSomenteLocal_2026'`). | Nenhuma ação necessária agora (já corrigido). Registrado como alerta de padrão: **nomes de role do Postgres precisam ser verificados contra o cluster inteiro antes de criar um novo em migration** (`SELECT rolname FROM pg_roles`), nunca só contra o banco/teste em mãos — roles não são escopadas por banco. |
| 2026-08-05 | F14 / orquestrador (achado real, mesma classe do acúmulo de tenants já documentado acima nesta sessão) | `tests/f2/importadores/test_servico.py::test_cria_importacao_e_enfileira_job_de_verdade` falhava com `RuntimeError: job "arq:job:<id antigo>" not found` dentro de `pool.queued_jobs()` — reproduzido em isolamento (não é flakiness de túnel). Causa: o Redis de teste compartilhado da VPS (`PONTO_TEST_REDIS_URL`, banco `0`) acumula uma entrada no *sorted set* de fila (`arq:queue:ponto:padrao`) por CADA job enfileirado por QUALQUER suíte desta sessão (F2, F4, F9b, F10, F13, F14…), sem nenhum worker real rodando para consumi-los — `queued_jobs()` itera o sorted set inteiro e busca a definição de CADA job por `GET`; jobs antigos o suficiente já passaram do TTL de definição do ARQ, mas a entrada no sorted set não expira junto, produzindo "job not found" para uma entrada de uma execução anterior, não a do teste atual. Resolvido com `redis-cli -n 0 FLUSHDB` no Redis de teste (`ponto-teste-redis-1`) — mesmo espírito da limpeza de `tenants` já documentada, só que sem gatilho de imutabilidade bloqueando (Redis não tem esse conceito). | Nenhuma ação de código necessária. Mesma recomendação já registrada para `tenants`: um script de limpeza de ambiente de teste (`tools/limpar_banco_teste.py`, já sugerido acima) deveria limpar Postgres E Redis juntos antes de uma rodada de regressão completa. |
| 2026-08-06 | F14 / orquestrador (fechamento — Schemathesis rodado contra o contrato inteiro, achados de fases anteriores, nenhum é regressão desta fase) | `GET /v1/sso/{provedor}/callback` (query `code=&state=`) e `POST /v1/sso/saml/acs` (`SAMLResponse=0&RelayState=0`) devolvem `500 PONTO-INT-001` genérico em vez de `400` quando o parâmetro OAuth/SAML é vazio/malformado — sem tratamento de validação de entrada antes de tentar decodificar. Território de `app/routers/sso.py` (F13). | F13 (retroativo) — validar `code`/`state`/`SAMLResponse` antes de repassar ao provedor/`onelogin`, devolvendo `PONTO-VAL-0xx` (`400`) em vez de deixar a exceção de parsing virar `500`. |
| 2026-08-06 | F14 / orquestrador (mesma rodada de Schemathesis) | `GET /v1/tenants/atual` devolve `limiteColaboradores: null` para o tenant seed de plano `enterprise` (sem teto contratual), mas `Tenant.limiteColaboradores` no contrato (`packages/contracts/openapi.yaml`) está declarado `type: integer` sem `nullable`/união com `null` — resposta real viola o próprio schema do contrato. Território de F1 (`packages/contracts/openapi.yaml`, modelo `Tenant`). | F1 (retroativo) — marcar `limiteColaboradores` como nulável no contrato (`"type": ["integer", "null"]` em OpenAPI 3.1), já que "sem teto" é um valor de negócio legítimo (documentado na própria descrição do campo), não uma omissão. |
| 2026-08-06 | F14 / orquestrador (mesma rodada de Schemathesis — achado sistêmico, fora de escopo de uma fase só) | Todo endpoint que exige `X-Tenant` pode devolver `404 PONTO-TEN-001` (tenant inexistente/slug inválido) — confirmado contra 10+ operações fuzzadas (`/v1/auth/senha/recuperar`, `/v1/auth/senha/redefinir`, `/v1/auth/token`, `/v1/feriado-conjuntos`, `/v1/tratamentos`, `/v1/aprovacoes`, `/v1/periodos`, `/v1/relatorios/agendamentos`, entre outras) — mas nenhuma delas declara `404` no seu conjunto de respostas no `packages/contracts/openapi.yaml`. O comportamento da API está correto (`TenantMiddleware`, F1); é só o contrato que não documenta uniformemente essa resposta comum a toda rota `X-Tenant`-dependente. Escopo grande demais para uma fase (provavelmente a maioria das ~227 operações do contrato) — não tentei enumerar todas nesta sessão. | Janela de manutenção de contrato dedicada (não uma fase de produto) — auditar `packages/contracts/openapi.yaml` e acrescentar `404` (ref ao `Problema` padrão) em toda operação que declara o parâmetro `X-Tenant`. |
| 2026-08-06 | F14 / orquestrador (mesma rodada de Schemathesis — não é bug, registrado para não ser re-investigado à toa numa fase futura) | ~97% das falhas cruas do Schemathesis (221 de 227) são o MESMO achado, espalhado por quase toda rota `POST`/`PUT`/`PATCH`/`DELETE`: quando o Schemathesis fuzza o VALOR dos cabeçalhos `Idempotency-Key`/`Authorization`/`X-API-Key` com bytes que violam a gramática de cabeçalho HTTP (RFC 7230), o servidor ASGI (`uvicorn`/`h11`) rejeita a requisição na camada de transporte, ANTES de qualquer código da aplicação rodar — resposta `400 text/plain "Invalid HTTP request received."`, nunca chega ao meu middleware de erro (`application/problem+json`). Não há como uma camada da aplicação envolver uma requisição que o parser HTTP já recusou. Mesma classe de achado do "1 passed, 226 failed, 16 errored" já obtido durante o fechamento da F13 (`schemathesis_run_v4.log`, fora deste repositório). Também descartados como ruído de ambiente local, não bug: os 4 "Connection aborted" (`WinError 10053`/`10054`) vieram do `uvicorn` de desenvolvimento (1 worker, sem tuning) sob a carga concorrente do fuzzing — não reproduzido com as suítes normais de regressão, só sob fuzzing pesado. | Nenhuma ação de código necessária. Se algum dia importar automatizar Schemathesis em CI, considerar `--exclude-checks content_type_conformance` (ou filtrar por essa mensagem específica) para não poluir o relatório com este ruído estrutural conhecido. |

---

## Resolvidos

| Data | Resolvido por | Descrição | Como |
|---|---|---|---|
| 2026-07-25 | F1 / A2 | `app/core/seguranca.py` (corpo real do agente A3) resolve RBAC lendo `contexto.usuario_atual()` dentro de `obter_sujeito()`. Quem publica esse valor a partir do JWT é `app.identidade.tokens.middleware.AutenticacaoMiddleware` (F1/A1) — faltava registrá-lo em `app/main.py`. | `app.add_middleware(AutenticacaoMiddleware)` acrescentado em `apps/api/app/main.py`, entre `RegistroDeAcessoMiddleware` e `TenantMiddleware` na ordem de `add_middleware` (execução: RequestId → Tenant → Autenticacao → RegistroDeAcesso). |
| 2026-07-26 | Orquestrador (RFC-007) | `POST /v1/colaboradores/importar` bloqueado por `ImportacaoCriar` não declarar `conteudoRef`. | RFC-007 decidida (opção a): campo acrescentado a `openapi.yaml`, `contrato.py` regenerado, handler de `app/routers/colaboradores.py` ligado a `app.importadores.servico.criar_importacao_colaboradores`. Verificado: `pytest tests/f2/importadores -q` 7/7. |
| 2026-07-25 | F9a / A1 | `pnpm test:storybook` falhava em 4 histórias de `Primitivos/Notificacao` (achado independentemente por A2 também): axe acusava `serious` — "`<ul>`/`<ol>` must only directly contain `<li>`" no `<ol>` de `NotificacaoViewport`. Causa raiz: `toast.tsx` aplicava `role`/`aria-live` manuais no `ToastPrimitive.Root` (renderizado como `<li>` pelo Radix), sobrescrevendo o papel implícito `listitem`. | Removido `role`/`aria-live` manuais; usada a prop nativa `type` do Radix Toast (que já alimenta o anúncio via elemento próprio fora da lista). Verificado com axe ao vivo (0 violações) e `pnpm test:storybook` (30/30 suítes, 90/90 testes). |
| 2026-07-26 | Orquestrador (mesmo tratamento da RFC-005, para o worker) | `apps/worker/tests/test_andaime_worker.py` cravava "oito tarefas" e assumia que toda tarefa do catálogo ainda era stub; quebrou com a nona tarefa real (`importar_colaboradores`, F2/A3). | `test_worker_settings_registra_as_oito_tarefas_do_catalogo` renomeado e sem número fixo (compara contra `NOMES_DAS_TAREFAS`); `test_toda_tarefa_devolve_nao_implementado_sem_levantar` passa a pular tarefas já implementadas (`_TAREFAS_JA_IMPLEMENTADAS`), com a cobertura real delegada a `apps/api/tests/f2/importadores/test_worker_tarefa.py`. Verificado: `pytest apps/worker/tests -q` 11/11. |
| 2026-07-26 | Orquestrador (RFC-009, achado independentemente por dois agentes de verificação de F1 e F2) | `fn_resolve_tenant` (RFC-004) lançava `invalid input syntax for type uuid` para qualquer slug não-UUID assim que `tenants` tinha linhas reais — o guard de `AND`/`OR` da função assumia curto-circuito de avaliação que o PostgreSQL não garante (manual, 4.2.14). Bloqueava login/resolução de tenant por slug (mecanismo primário documentado) de forma intermitente, dependente do plano escolhido pelo planejador. Reproduzido de forma determinística por SQL cru e por `TestClient` batendo em `POST /v1/auth/login` real. | RFC-009 decidida: função reescrita com `CASE WHEN` (construção com ordem de avaliação garantida pelo padrão SQL), em `packages/contracts/schema.sql` e `apps/api/migrations/versions/0001_inicial.py`. Verificado: 5 slugs + 1 UUID + 10 repetições (forçando plano genérico) sem erro; suíte completa `pytest tests/f1 tests/f2 tests/test_andaime.py -q` re-executada múltiplas vezes; login real por slug + `PONTO-VAL-005` com sessão autenticada confirmados via `scratchpad/rfc008_check.py`. |
| 2026-07-26 | Orquestrador (achado na mesma verificação, RFC-009 §6) | `mypy apps packages` (comando oficial de `.github/workflows/ci.yml`, `Makefile`, `tasks.ps1`, rodado da raiz do monorepo) nunca encontrava o `[tool.mypy]` de nenhum app — mypy só descobre config subindo a partir do diretório corrente, ao contrário do `ruff`. Rodava em modo relaxado, sem `strict`, silenciosamente, desde a Fase 0. | Os três invocadores agora rodam `mypy` uma vez por diretório de app (`apps/api`, `apps/worker`, `apps/device-gw`, `apps/facial-svc`), sem argumento — cada `[tool.mypy]` já declara `files=[...]`. Verificado: `cd apps/api && mypy` → 1 erro pré-existente (`saude.py`, F0); `cd apps/worker && mypy` → 0 erros. |
| 2026-07-26 | Orquestrador (RFC-008) | `tests/test_andaime.py::test_parametro_de_caminho_invalido_devolve_erros_campo` falhava para `/v1/auditoria/{registroId}` (`PONTO-VAL-011` em vez de `PONTO-VAL-005`). Investigação ampliada confirmou que é sistêmico: **toda** rota real de F1+F2 com parâmetro de caminho devolve o erro da dependência (`SessaoDb`/`exigir_permissao`) que falha primeiro, nunca `PONTO-VAL-005`, quando chamada sem tenant/autenticação — não é peculiaridade de `auditoria.py`, nem depende da posição do parâmetro de caminho na assinatura (testado em 8 rotas reais de F1+F2). | RFC-008 decidida: precedência aceita e documentada (causa é estrutural do FastAPI — resolve dependências antes de validar path params); teste ajustado para aceitar qualquer código do catálogo numa resposta 4xx bem formada, com contagem informativa de quantos ainda expõem `PONTO-VAL-005`. |
| 2026-07-26 | F3 / A2 | A descrição de `excluirAfastamento` no `openapi.yaml` ("Exclui o afastamento e agenda o recalculo dos dias que ele cobria") não tem mecanismo correspondente no contrato: não há evento `afastamento.*` em `packages/contracts/events.yaml` nem fila para "agendar recalculo". `app.jornada.calendario.afastamentos.excluir_afastamento` faz apenas a exclusão lógica (`excluido_em`), sem inventar evento ou fila novos — decisão explícita do PCF da F3 (seção 2, último parágrafo; proibição 2 da seção 9). Quando a F4 (apuração/recálculo) existir, quem desenhar o mecanismo de recálculo precisa decidir se isso vira evento novo (exige RFC em `events.yaml`) ou é resolvido por reprocessamento assíncrono da própria F4 sem depender de notificação da F3. | F4 |
| 2026-07-26 | F3 / A1 | `apps/api/tests/f3/conftest.py::contexto_f3` quebrava em toda execução real: `data_inicio_vinculo = "2020-01-01"` era passado como `str` Python para a coluna `DATE` de `vinculos.data_inicio` via `text()` cru, e o asyncpg (ao contrário de psycopg2/psycopg3) não faz cast implícito de texto para `date` (`asyncpg.exceptions.DataError: invalid input for query argument $7`). Bloqueava qualquer teste real de A1/A2/A3/A4 na fase. | F3 / A4 corrigiu trocando a literal por `_dt.date(2020, 1, 1)` (linha 379 do arquivo). Verificado por A1: `pytest apps/api/tests/f3/golden/test_fixture_smoke.py -q` e toda a suíte de `tests/f3/modelagem` (25 testes) passam contra Postgres 16 real (`ponto_f3_a1`). |
| 2026-07-26 | F6 / A2 | `gateway/dominio/fila.py::enfileirar_comando` (A1, T4) grava o envelope `{id, verb, endpoint, contentType, queryString, body}` no Redis mas fixa `queryString: ""` e não tem campo para sinalizar corpo binário; `gateway/rotas/push.py::enfileirar_comando` (A1) só lê `verb`/`endpoint`/`body` do corpo recebido. O comando `user_set_image.fcgi` que esta tarefa monta (`gateway/provisionamento/comandos.py::montar_comando_enviar_imagem`) precisa da query string (`user_id`/`timestamp`/`match`) e de um sinal de que `body` é base64 (`bodyCodificacao: "base64"`, convenção desta tarefa) para ser entregue ao equipamento de verdade — hoje esses dois campos são descartados silenciosamente no caminho de entrega. Não bloqueia o critério de aceite da T7 (escopo `usuarios`, que usa `create_objects.fcgi`, sem query string nem corpo binário) nem exige RFC (formato interno do `device-gw`, fora de `packages/contracts/`). | F6 (quem fechar a entrega real de `user_set_image` via Push, provavelmente A1 ou uma fase de hardening) — ajustar `fila.enfileirar_comando`/`push_obter_comando` para propagar `queryString` e `bodyCodificacao` do envelope recebido. |
| 2026-07-26 | F6 / A2 | `gateway.provisionamento.comandos.user_id_do_terminal` (e a cópia deliberada em `worker.tarefas.integracoes.user_id_do_terminal`) deriva o `users.id` do equipamento por CRC-32 da matrícula, porque não há coluna no schema para persistir a correspondência colaborador→`user_id` do terminal (nenhuma migration nova nesta fase). Colisão é improvável na escala de colaboradores por terminal, mas não é impossível — se a F14 (ou uma fase futura) observar colisão real, considerar uma tabela de correspondência persistida. | Sem fase dona ainda; revisitar se colisão for observada em produção. |
| 2026-07-26 | F6 / A2 | Escopo `templates` de `sincronizar_terminal` (T7) não tem fonte de foto crua alcançável nesta fase: `colaboradores.foto_ref` existe, mas nenhuma fase até agora integrou um cliente de armazenamento de objetos de verdade (mesma lacuna que `worker/tarefas/importacoes.py` já documenta para `importacoes.conteudo_ref`, F2). A tarefa devolve `{"enviados": 0, "motivo": "..."}` para `templates`, em vez de inventar um mecanismo de leitura de foto. Escopos `grupos`/`regras`/`horarios` têm a mesma resposta honesta: dependem do modelo de jornada/escala (F3), fora do "Não toca" desta fase (PCF F6 §4). | F14 (ou quem integrar MinIO de verdade) para `templates`; F3/fase de integração para `grupos`/`regras`/`horarios`. |
| 2026-07-26 | F6 / A1 | `apps/device-gw/tests/test_andaime_device_gw.py` fica obsoleto no mesmo padrão da RFC-005 (`apps/api/tests/test_andaime.py`): `test_stub_responde_501_com_codigo_do_catalogo` (as 11 rotas de protocolo) e `test_simulador_cobre_os_tres_fcgi_do_aceite_da_f6` cravam que toda rota ainda responde `501` e que `descrever_simulador()["implementado"] is False` — falso agora que T2/T4/T5 (A1) e T1 (A2, `cliente_controlid.py`/`simulador.py`) implementaram de verdade. Não posso editar o arquivo (propriedade do orquestrador, mesmo tratamento do PCF §3 para `test_andaime.py`). Saída real: `pytest tests/test_andaime_device_gw.py -q` → 12 falhas, todas nessas duas asserções; o resto do arquivo (inventário de rotas, `/health`, `/ready`, formato `problem+json`, recorte do catálogo de erros) continua correto e passa. | Orquestrador — reescrever com checagem dinâmica, mesmo padrão da correção de `test_andaime.py` (RFC-005 opção b) |
| 2026-07-26 | F6 / A1 | `apps/worker/tests/test_andaime_worker.py` tem a mesma obsolescência, por dois motivos: (1) `test_rotinas_de_cron_devolvem_nao_implementado_com_a_fase_certa` espera `implementado: False` de `verificar_terminal_offline`, que agora é real (T9); (2) `test_toda_tarefa_devolve_nao_implementado_sem_levantar` chama `sincronizar_terminal` (T7, A2, agora real) com os IDs sintéticos não-UUID que o andaime usa para as outras 8 tarefas ainda-stub, e a implementação real levanta `ValueError: badly formed hexadecimal UUID string` em vez de devolver o dicionário `nao_implementado` — o próprio nome do teste ("sem levantar") já não é garantido para uma tarefa real. Não editei o arquivo (fora do meu ownership). | Orquestrador — mesmo tratamento de `test_andaime.py`/`test_andaime_device_gw.py`; A2 pode querer que `sincronizar_terminal` valide o formato do `terminal_id` antes de tentar `UUID(...)` e devolva um erro do catálogo em vez de deixar a exceção nativa do Python vazar |
| 2026-07-26 | F6 / A1 | Três variáveis de ambiente novas ainda não estão em `infra/.env.example` (fora do meu ownership — `infra/**` é "não toca" do PCF §4): `PONTO_TERMINAL_CHAVE_MESTRA` (chave da cifra de `terminais.senha_api_cifrada`, `apps/api/app/terminais/cifra.py` + `apps/device-gw/gateway/dominio/cifra.py`), `CONTROLID_API_KEY`/`api_key_marcacoes` (credencial X-API-Key que o `device-gw` apresenta em `POST /v1/marcacoes`, `apps/device-gw/gateway/config.py`) e `WORKER_DATABASE_URL_SUPORTE` (segunda credencial do scheduler para `verificar_terminal_offline`, RFC-013, `apps/worker/worker/config.py`). | Quem tiver ownership de `infra/.env.example` |
| 2026-07-26 | F6 / A1 | A credencial de serviço que o `device-gw` usa para autenticar em `POST /v1/marcacoes` (`X-API-Key`) depende de uma semeadura em `api_clients`/`api_keys` (tipo `maquina`, hash batendo com `CONTROLID_API_KEY`) que este PCF não pode criar (`apps/api/migrations/seed_dev.py` fora do ownership da F6). Sem essa semeadura, a integração ponta-a-ponta real (device-gw → API real) responderá `PONTO-AUTH-002`/`013` mesmo depois de F5 implementar `POST /v1/marcacoes` de verdade — os testes desta fase usam um duplo de teste local para `cliente_api.enviar_marcacao` (Ponto de Atenção nº 2 do PCF), então isso não bloqueou T4/T5, mas bloqueia o teste de integração ponta-a-ponta reservado para quando F5 terminar. Documentado também em `apps/api/app/terminais/servico.py` (docstring do módulo). | Quem tiver ownership de `seed_dev.py` (F1/F2), coordenado com F5 |
| 2026-07-26 | F6 / A1 | Resolução de `access_logs.user_id → matricula` (`gateway/dominio/usuarios.py::resolver_matricula`) depende de um cache persistido no Redis (`ponto:device-gw:registration:<numeroSerie>:<userId>`) para terminais em modo Push de verdade (sem IP direto, o device-gw não pode abrir `load_objects.fcgi` sob demanda) — a função `gravar_registration_cache` existe e é exportada exatamente para isso, mas `sincronizar_terminal`/`create_objects` (T7, A2) ainda não a chama ao criar um `users` com `registration`. Enquanto isso não for ligado, um terminal Push real que nunca teve catch-up/polling prévio responde `PONTO-TERM-005` ("matrícula não está em cache") ao converter seu primeiro `access_log`. Os testes desta fase cobrem os dois modos (cache ausente levanta o erro certo; cache/`load_objects` presente resolve), então não é um bug não coberto — é uma integração entre T4 (A1) e T7 (A2) que falta fechar. | F6/A2 (T7) — chamar `gravar_registration_cache` ao criar/atualizar `users.registration` |
| 2026-07-26 | F6 / A2 | `apps/worker/pyproject.toml` não declara `ponto-contracts` como dependência (comentário no arquivo: "já é instalado por camada separada no Dockerfile"), e em ambiente de desenvolvimento local (`pip install -e apps/worker[dev]` sem mais nada) isso faz `mypy` falhar com `Cannot find implementation or library stub for module named "ponto_contracts"` em qualquer arquivo que o importe — pré-existente (`worker/tarefas/importacoes.py`, F2, já tinha o mesmo erro antes desta fase; confirmado rodando `mypy` contra o HEAD limpo) e agora também em `worker/tarefas/integracoes.py` (esta tarefa). Não bloqueia CI (que instala `packages/contracts` e todos os apps no mesmo ambiente antes de rodar `mypy`), só o mypy local isolado por app. | Sem fase dona; considerar declarar `ponto-contracts` como dependência explícita de `apps/worker` (e possivelmente `apps/device-gw`) para o mypy local bater com o comportamento do CI. |
| 2026-07-27 | F8 / Orquestrador (verificação final da fase, reconciliação dos relatórios de A1/A2/A3) | `POST /api/auth/refresh` (`apps/web/src/app/api/auth/refresh/route.ts`, T1/A1) nunca encaminhava `X-Tenant` para `POST {URL_API_INTERNA}/v1/auth/refresh`: `RefreshRequisicao` não tem campo `tenant` no contrato, e a chamada não carrega `Authorization` (é exatamente por não ter mais um `accessToken` válido que o refresh acontece) — o único jeito do backend (`apps/api/app/routers/auth.py::renovar_sessao`) resolver o tenant é por `X-Tenant` OU pelo subdomínio do `Host` da requisição (`app/core/middleware.py`). Como `URL_API_INTERNA` é um endereço interno único (chamada servidor-a-servidor, não um subdomínio por tenant), o refresh silencioso — disparado sempre que `ProvedorDeSessao` monta, inclusive na navegação normal `/` → `/eu` logo após um login válido — respondia `PONTO-VAL-011` em qualquer topologia sem resolução de tenant por host. Reproduzido contra a API real (Postgres/Redis reais via túnel SSH, chaves RS256 e seed de desenvolvimento) durante esta verificação: login funcionava, mas a sessão nunca sobrevivia à chegada em `/eu` — quebrando o critério 1 da §7 do PCF ("recarregar a página em `/eu` mantém a sessão") fora do ambiente 100% mockado da suíte Vitest, que não exercita a resolução real de tenant do backend. | Corrigido nesta verificação, dentro do ownership de A1 (não é `apps/api`): cookie companheiro `ponto_tenant` (só o slug, não é credencial), gravado por `montarRespostaAutenticada` (`servidor/upstream.ts`) junto do cookie `httpOnly` do refresh token (`servidor/cookie.ts`), lido e encaminhado como `X-Tenant` por `refresh/route.ts` (e por `logout/route.ts`, defesa em profundidade para quando não há `Authorization`). Cobertura nova em `src/testes/f8/portal/rotas-de-auth.teste.ts` (cookie gravado no login, encaminhado no refresh, limpo nos dois cookies em caso de reuso 401 e no logout). Verificado contra a API real: login → refresh só com os cookies (sem `X-Tenant`, sem `Authorization`) → `200` com `accessToken` novo (antes: `400 PONTO-VAL-011`). Suíte completa `pnpm test` 549/549 após a correção (era 547/547 antes das duas novas asserções). |
| 2026-07-27 | F9b / A4 (achado), Orquestrador (correção) | `GET /v1/jornadas/resolver` (`resolverJornadaDoDia`) inalcançável: `apps/api/app/routers/jornadas.py` registrava `GET /v1/jornadas/{jornadaId}` (`obterJornada`) antes de `GET /v1/jornadas/resolver` no mesmo `APIRouter` — o FastAPI casa rota por ordem de registro, não por especificidade, então toda chamada a `/resolver` caía no `{jornadaId}="resolver"` do handler anterior e falhava com `400 PONTO-VAL-005` (UUID inválido). Reproduzido de ponta a ponta contra API real pelo A4; bloqueava a metade "datas futuras" da T13 (previsto×realizado) e "copiar período" da própria F9b. | Corrigido reordenando só a posição do bloco `resolver_jornada_do_dia` no arquivo (para antes de `/{jornadaId}`) — nenhuma linha de lógica mudou. Verificado: `curl` direto em `/v1/jornadas/resolver` contra API real devolve `401 PONTO-AUTH-002` (alcança o handler real) em vez de `400 PONTO-VAL-005` (UUID inválido); suíte `pytest tests/f3 -q` (72 testes) 100% verde, sem regressão. |
| 2026-07-27 | F9b / A3 (achado de alto impacto), Orquestrador (correção) | `POST /v1/apuracoes/recalcular` sempre devolvia `202`, mas o job nunca era processado: os três lugares de `apps/api` que enfileiram uma tarefa ARQ (`app/apuracao/tratamento/recalculo.py::enfileirar_recalculo`, `app/importadores/servico.py::criar_importacao_colaboradores`, `app/terminais/servico.py::sincronizar_terminal_lote` ou equivalente) chamavam `create_pool(RedisSettings.from_dsn(redis_url))` **sem** `default_queue_name` — o ARQ usa a fila embutida `"arq:queue"` — enquanto o worker real (`worker.main.WorkerSettings.queue_name = FILA_PADRAO = "ponto:padrao"`) só escuta `"ponto:padrao"`. O job ficava órfão na fila errada para sempre, sem erro nem log em lugar nenhum — afetando recálculo de apuração (F4), importação de colaboradores (F2) e sincronização de terminal (F6) desde que cada uma foi entregue. | Criado `apps/api/app/core/filas.py` (`FILA_PADRAO`, duplicado de proposito de `worker/filas.py` — as duas imagens Docker não compartilham esse módulo, mesmo precedente de `worker/tarefas/importacoes.py` para os validadores de CPF/PIS). `default_queue_name=FILA_PADRAO` acrescentado aos três `create_pool(...)` e ao teste que verifica a fila (`tests/f2/importadores/test_servico.py`, que antes comparava a mesma fila errada consigo mesma e por isso nunca pegou o bug). Verificado de ponta a ponta: `python -m arq worker.main.WorkerSettings --burst` real consumindo um `recalcular_periodo` enfileirado pelo código corrigido — `JobStatus.complete` em ~5s (antes: órfão para sempre). `pytest tests/f2/importadores/test_servico.py tests/f4/tratamento -k "recalculo or enfileirar" -q` 100% verde. |
| 2026-07-28 | F10 / A3 | Não existe tag/endpoint `notificacoes` em `packages/contracts/openapi.yaml`: `notificacoes`/`notificacao_preferencias` existem desde a Fase 0 e o catálogo de permissões já semeou `workflow.notificacoes` (`ler`/`criar`), mas nenhuma operação HTTP usa essas permissões (`grep -c "^  /v1/notificacoes" openapi.yaml` = 0). O motor desta fase (`app/notificacao/motor.py`) grava linhas reais em `notificacoes` (inclusive `canal='in_app'`, com `lida_em` nula) prontas para o dia em que existir `GET /v1/notificacoes`/`PATCH .../lida`, mas o canal in-app hoje só é verificável por leitura direta da tabela — decisão já fixada pelo PCF F10 §2.7 item 1, registrada aqui como instruído. | Quem primeiro precisar da tela de sino de notificações (F9b já concluída; candidato mais provável é F11 ou uma micro-fase de UI) — abrir RFC para acrescentar a tag `notificacoes` ao `openapi.yaml`. |
| 2026-07-27 | F9b (verificação final, Orquestrador) | `pnpm exec playwright test` (`e2e/cadastros/**`, novo, T6; `e2e/escalas/**`, T12) travava indefinidamente selecionando a opção "Empresa" num `<Selecao>` — o dropdown nunca populava. Causa raiz, fora do ownership desta fase (`apps/api/**`): `GET /v1/empresas?ordenar=razaoSocial:asc` (default de `apps/web/src/ganchos/use-empresas.ts`, T4/A2) respondia sempre `400 PONTO-VAL-005` ("Campo de ordenacao desconhecido: 'razaoSocial'"). `app/organizacao/empresas.py::CAMPOS_ORDENAVEIS` usava chaves em **snake_case** (`razao_social`, `nome_fantasia`, `criado_em`) mas `campo_e_direcao`/`crud_base.listar` (`app/organizacao/paginacao.py`, `app/organizacao/crud_base.py`) usam o valor cru de `ordenar` — que é público e documentado em **camelCase** no `openapi.yaml` ("Campos aceitos em ordenar: razaoSocial, cnpj, criadoEm") — como chave direta do dicionário, sem nenhuma conversão de caixa. Só não quebrava antes porque nenhum chamador (nem os testes de F2, nem nenhuma fase anterior) usava um campo de mais de uma palavra nesse `ordenar`. | Corrigido pelo orquestrador (fora do ownership de F9b, mas bloqueava o critério 13 da §7 do PCF): renomeadas as chaves de `CAMPOS_ORDENAVEIS` em `app/organizacao/empresas.py` para `criadoEm`/`razaoSocial`/`cnpj`/`nomeFantasia`; atualizado o único teste que dependia da chave antiga (`tests/f2/organizacao/test_empresas_service.py::_pedido`, `"criado_em:desc"` → `"criadoEm:desc"`). Verificado: `curl .../v1/empresas?ordenar=razaoSocial:asc` → `200` (antes `400`); `pytest tests/f2/organizacao -q` 100% verde; `pnpm exec playwright test` (13 cenários, incluindo o novo `e2e/cadastros/cadastros.spec.ts`) verde de ponta a ponta contra API real. **Achado relacionado, não corrigido (mesma classe de bug, mas NÃO bloqueia F9b hoje — nenhum `ordenar` default de F9b usa campo de mais de uma palavra nesses recursos):** `unidades.py`/`estrutura.py` (departamentos, centros de custo, cargos, equipes) têm o MESMO padrão de chave snake_case (`"criado_em"`) para o campo `criadoEm` do contrato — `ordenar=criadoEm:...` quebraria do mesmo jeito nesses cinco recursos se algum chamador algum dia usar esse valor (hoje nenhum usa; todos os hooks de F9b ordenam por `nome`, palavra única, sem o problema) — registrado como item aberto abaixo, não corrigido aqui, para não expandir esta correção além do que bloqueava F9b. |
| 2026-07-28 | F10 / A3 (achado), F10 / A2 (resolvido na sequência normal do próprio T9, sem coordenação direta) | A3 registrou que `apps/worker/worker/tarefas/fechamento.py` (ownership de A2) tinha o padrão de supressão de `import-not-found` incompleto: `app.identidade.auditoria.hash_chain`/`app.workflow.fechamento.{escopo,espelho,eventos}` sem `# type: ignore` (erro real) e a segunda ocorrência de `app.db.sessao` com o ignore sobrando (`unused-ignore`), porque a supressão do mypy é global ao *run* inteiro (todo o pacote), não por arquivo — achado de A3 confirmado como correto. | O arquivo já havia sido corrigido por A2, de forma independente, ao iterar no mesmo módulo para a evidência de T9 (comentário supressor na primeira ocorrência textual de cada módulo, nenhuma repetição): `cd apps/worker && mypy` (comando oficial da fase, sem argumento, cobre o pacote inteiro) devolve `Success: no issues found in 18 source files`. Nenhuma mudança adicional foi necessária além do que T9 já exigia. |
| 2026-07-30 | F12 / A3 (achado de contrato — variável de ambiente já provisionada com outro nome que o PCF) | O PCF de F12 (§2.4/T11) instrui criar duas variáveis de ambiente NOVAS — `FISCAL_CERTIFICADO_PFX_PATH`/`FISCAL_CERTIFICADO_SENHA` — e acrescentar um bloco `# 7. ASSINATURA FISCAL (CAdES)` a `infra/.env.example`. Lendo o andaime real da Fase 0 antes de codificar (não só o PCF), encontrei que ele **já** provisiona exatamente esse par de credenciais, de ponta a ponta, com nomes diferentes: `infra/.env.example` já tem o bloco "12. CONFORMIDADE REP-P" com `INPI_NUMERO`/`CERT_ICP_PATH`/`CERT_ICP_SENHA` (comentado/vazio); `infra/docker-compose.yml` já repassa `CERT_ICP_PATH`/`CERT_ICP_SENHA` para os containers `api` e `worker`; `app.core.config.Configuracao.cert_icp_path`/`cert_icp_senha` (`apps/api`) e o par equivalente em `apps/worker/worker/config.py` já leem essas variáveis. Duas variáveis diferentes apontando para o mesmo segredo (o `.pfx` do certificado fiscal) é exatamente o tipo de ambiguidade perigosa que uma fase de conformidade legal deveria evitar. **Decisão tomada nesta fase**: `app.fiscal.assinatura.certificado` (T11) reaproveita `cert_icp_path`/`cert_icp_senha` já existentes, em vez de introduzir o par novo do PCF — nenhuma variável de ambiente nova foi criada, `infra/.env.example` não foi tocado (o bloco que o PCF pedia já existe, com outro nome). Nota relacionada: o achado de F12/A2 (T9, linha acima desta mesma tabela, datada de 2026-07-30) sugere uma RFC para `rep_ps.email_desenvolvedor`/variável `FISCAL_EMAIL_DESENVOLVEDOR` "mesmo padrão de `FISCAL_CERTIFICADO_PFX_PATH`" — essa variável de referência não existe; o padrão real a seguir é o de `CERT_ICP_PATH` (bloco 12 de `infra/.env.example`), não o nome hipotético do PCF. | F12/orquestrador — decidir se vale uma RFC de limpeza cosmética para corrigir o texto do PCF F12 §2.4/T11 (e o texto do achado de A2 sobre `FISCAL_EMAIL_DESENVOLVEDOR`) para citar `CERT_ICP_PATH`/`CERT_ICP_SENHA` em vez de nomes que não existem no andaime real |
| 2026-08-03 | F13 / PCF (achado crítico, escalado pelo orquestrador ao revisar antes do build) | `Idempotency-Key` é declarado obrigatório (`x-idempotente: true`, cabeçalho no OpenAPI, `PONTO-IDEM-001/002/003` no catálogo de erros) em praticamente toda escrita do contrato — dezenas de operações em `auth`, `admin`, `webhooks`, `integracoes`, `solicitacoes`, `aprovacoes`, `tratamentos`, `fechamentos`, `espelhos` e outras. Confirmado por `grep -rn "idempotency_key" apps/api/app/routers/*.py`: o parâmetro é **recebido** pela assinatura de toda função (gerada do contrato), mas só é **usado** de fato em `apps/api/app/routers/marcacoes.py` (duas ocorrências, F5) — em todo o resto (F1, F2, F4, F6, F9b, F10, F11, F12), o valor chega e é descartado sem nenhuma deduplicação real. Reenviar a mesma escrita com a mesma `Idempotency-Key` hoje **cria um recurso duplicado** em vez de devolver a resposta original, na maioria dos endpoints do sistema. Não é bug de nenhuma fase específica — é a ausência de um mecanismo genérico reaproveitável, que só a F5 construiu (`apps/api/app/marcacao/pipeline/idempotencia.py`, específico de marcação, com FK própria) porque só ela precisou dele para o próprio critério de aceite. F13/A1 (Grupo API pública, T1 do PCF F13) constrói o mecanismo genérico (`apps/api/app/comum/idempotencia_generica.py` + tabela nova via RFC) e o aplica às próprias rotas novas de F13 (`admin` api-clients/api-keys, `webhooks`, `integracoes`) — **não é ownership de F13 retrofitar as ~130 operações já implementadas por F1–F12**, que continuam com o mesmo comportamento de hoje até que uma fase (ou o orquestrador) decida varrer o sistema inteiro. | Fase dedicada de hardening (candidata: F14, que já cobre "rate limiting, revisão de RLS" no mesmo espírito de varredura transversal) — aplicar `apps/api/app/comum/idempotencia_generica.py` (uma vez que F13 o construa) a cada router de F1–F12, um `Depends` por operação de escrita. |
| 2026-08-03 | F13 / PCF (achado crítico, escalado pelo orquestrador ao revisar antes do build) | `POST /v1/auth/token` (`emitirTokenOAuth`) e a autenticação por `X-API-Key` estão **parcialmente implementadas de ponta a ponta**: F1 já construiu a emissão de token OAuth completa (`apps/api/app/identidade/tokens/oauth.py::emitir_token`, grava em `oauth_tokens`) e a primitiva de verificação de API key (`autenticar_api_key`, verifica contra `api_keys`) — mas **nenhuma delas está ligada ao pipeline de autenticação real**. Confirmado por leitura: `apps/api/app/identidade/tokens/middleware.py::AutenticacaoMiddleware` (registrado em `app/main.py:124`, único middleware que popula `contexto.definir_usuario`) só decodifica `Authorization: Bearer <JWT RS256>` — nunca consulta `oauth_tokens` nem olha `X-API-Key`. `apps/api/app/core/seguranca.py::obter_sujeito` (a única fonte do `Sujeito` que todo `exigir_permissao`/`x-permissao` do sistema depende) só lê o `usuario_id`/`tenant_id` publicados por esse middleware. Resultado: um cliente de integração consegue obter um `access_token` OAuth ou uma `X-API-Key` válida, mas **nenhuma rota protegida por `exigir_permissao` os aceita** — toda tentativa cai em `PONTO-AUTH-002` (credencial ausente), porque o sistema nunca reconhece o tipo de credencial. `apps/api/app/core/seguranca.py` é explicitamente travado ("a partir daqui, somente A3 [F1] edita este arquivo") e não é ownership de F13. O PCF de F13 (Grupo API pública, A1) resolve isso **sem tocar em `seguranca.py`/`middleware.py`**: constrói uma dependência paralela e autocontida (`apps/api/app/comum/autenticacao_cliente.py::exigir_escopo(escopo)`) que lê `Authorization`/`X-API-Key` diretamente da requisição, valida contra `oauth_tokens`/`api_keys`, e é usada **em adição a** `exigir_permissao` só nas rotas novas de F13 — cobertura suficiente para o próprio escopo de F13, mas não para as rotas já existentes de F1–F12, que continuam só aceitando sessão humana (JWT) mesmo quando o contrato já declara `oauth2`/`apiKeyAuth` como alternativa válida. | F14 (hardening) ou uma RFC futura, se algum dia fizer sentido product-wise que um cliente OAuth/API-key acesse as ~130 rotas de F1–F12 sem passar pelo mecanismo paralelo de F13 — exigiria tocar `seguranca.py`, hoje travado à F1/A3. |
| 2026-08-03 | F13 / PCF (achado de processo, mesmo padrão já pré-aprovado pelo PCF da F2 para `importar_colaboradores`) | O catálogo de tarefas do worker (`apps/worker/worker/tarefas/__init__.py`, doze tarefas hoje) não tem nenhuma tarefa para `tipo: exportacao_folha` de `ProcessamentoAssincrono` (o enum já prevê o valor desde a Fase 0, schema.sql/openapi.yaml) nem para processar `tipo: afd_terceiro`/`escalas`/`feriados`/`marcacoes`/`banco_horas`/`biometria`/`afastamentos` de `importacoes` (só `colaboradores` tem tarefa, `importar_colaboradores`, de F2). O PCF de F13 autoriza explicitamente os agentes do Grupo integrações de folha e do importador de AFD a acrescentarem, cada um, uma tarefa nova (`exportar_folha`, `importar_arquivo_generico` ou nome equivalente) aos três arquivos compartilhados (`worker/tarefas/__init__.py`, `worker/tarefas/integracoes.py`, `worker/filas.py`), no mesmo padrão que o PCF da F2 já usou para `importar_colaboradores` — registrado aqui para que ninguém trate o acréscimo como invenção de escopo. | F13 (já endereçado no PCF) |
| 2026-08-03 | F13 / A3 (T11 — conflito real entre duas seções do próprio PCF) | O PCF da F13 §2.2 lista `apps/worker/worker/tarefas/importacoes.py` (`publicar_evento`, três ocorrências de `tipo="importacao.concluida"`) como um dos quatro produtores de evento fora de `apps/api` que T11 precisa alimentar com fan-out real de `webhook_entregas`, e §6/T11 pede explicitamente "acrescentar a chamada" nesse ponto. Mas §5.4 (fora do ownership, lista compartilhada) proíbe esse MESMO arquivo por nome, com nota explícita ("`importacoes.py` aqui é o de `importar_colaboradores`, F2"), sem nenhuma cláusula de exceção equivalente à que `apps/device-gw/**` recebe ("exceto o corpo de `publicar()`..., exclusivo de A3") — ao contrário de `terminais_saude.py`/`banco_horas_vencimento.py` (nem proibidos nem exclusivamente listados para mim, tratados como dentro do meu alcance por eliminação) e de `apps/device-gw/gateway/dominio/eventos.py` (exceção explícita), este arquivo tem proibição textual explícita SEM exceção. Resolvido a favor da proibição mais explícita (§5.4 nomeia o arquivo por nome, com nota que mostra consciência deliberada do autor do PCF): **não toquei `apps/worker/worker/tarefas/importacoes.py`**. Consequência: `importacao.concluida` continua sem fan-out real para `webhook_entregas` — `publicar_evento` (F2) continua só logando o envelope, exatamente como antes desta fase; os outros 12 dos 13 pontos do catálogo (nove `apps/api`, `terminais_saude.py`, `banco_horas_vencimento.py`, `apps/device-gw`) estão com fan-out real, provados por teste. | **Decidido pelo orquestrador no fechamento da F13 (03/08/2026): opção (b).** A leitura de A3 está correta — §5.4 nomeia o arquivo por nome, sem cláusula de exceção, e prevalece sobre a instrução geral de §2.2/T11. Autorizar a mudança agora, fora de qualquer PCF, seria a mesma classe de decisão unilateral que a Nota de processo de RFC-016/017 (mesma fase) já registrou como erro a não repetir. Como não é regressão e não bloqueia nenhum critério de aceite da F13, fica para uma fase de hardening dedicada (candidata: F14) resolver com PCF próprio. |
| 2026-08-03 | F13 / A3 (achado de ambiente compartilhado, não do meu código) | `schemathesis>=3.35` (adicionado a `apps/api/pyproject.toml` por A1, T6) resolveu para `schemathesis==4.24.3` no venv compartilhado desta fase, cujo plugin pytest (`schemathesis.pytest.plugin`, carregado via entry point `pytest11` em TODA invocação de `pytest`, mesmo rodando testes que nada têm a ver com schemathesis) importa `_pytest.subtests.SubtestReport` — dependência de `pytest-subtests`, que não está listada em nenhum `pyproject.toml` do projeto. Sem ela, **qualquer** `pytest` neste venv falha na coleta com `ModuleNotFoundError: No module named '_pytest.subtests'`, antes de rodar um teste sequer — bloqueava inclusive `tests/f13/webhooks` (nada relacionado a schemathesis). Instalei `pytest-subtests` no venv compartilhado (`pip install pytest-subtests`) para desbloquear minha própria verificação — correção de ambiente, não de código, não registrada em nenhum `pyproject.toml` (fora do meu ownership tocar `apps/api/pyproject.toml`, que é onde T6/A1 já declara `schemathesis`). | **Resolvido pelo orquestrador no fechamento da F13 (03/08/2026):** `pytest-subtests>=0.14` acrescentado a `apps/api/pyproject.toml` (`[project.optional-dependencies].dev`), junto de `schemathesis`. `pip install -e ".[dev]"` num venv limpo agora traz a dependência sem passo manual. |
| 2026-08-03 | F13 / A3 (achado real, não óbvio até reproduzido contra Postgres real) | RLS (`SET LOCAL app.tenant_id`) vale só para a transação corrente do Postgres — qualquer `sessao.commit()` a encerra, e a PRÓXIMA instrução na MESMA `AsyncSession` abre uma transação nova sem `app.tenant_id` publicado. Isso morde de duas formas sutis, nenhuma delas um erro óbvio: (1) em teste, uma leitura de verificação feita na mesma sessão logo após um `commit()` intermediário devolve **zero linhas silenciosamente** (RLS filtra, não lança exceção) — fácil de confundir com "o mecanismo não funcionou" quando na verdade funcionou e só a leitura de prova é que estava cega; encontrei isso ao escrever os testes de T11/T12 desta fase e corrigi reaplicando `SET LOCAL` após cada commit nos meus próprios testes (`_reaplicar_tenant`/`_commit_e_reaplicar`, `tests/f13/webhooks/*.py`). (2) em produção, era um bug real: `apps/worker/worker/tarefas/integracoes.py::enviar_webhook` (T12, meu código) fazia um commit intermediário (`entrega.status = "enviando"`) e continuava escrevendo na mesma sessão (`webhook.falhas_consecutivas`, `entrega.status` final) sem reaplicar `SET LOCAL` — o UPDATE final falhava com `StaleDataError`/"0 rows matched" (RLS filtrando a própria linha do UPDATE), não um erro de permissão claro. Corrigido (`await _aplicar_tenant(sessao, tenant_id)` logo após o commit intermediário). Registrado porque é o tipo de bug que se repete: qualquer rotina futura (worker ou teste) que faça mais de um `commit()` na mesma sessão precisa reaplicar `SET LOCAL app.tenant_id` entre eles. | Nenhuma ação necessária agora (já corrigido no meu escopo) — registrado como alerta de padrão para quem escrever rotina/teste futuro com múltiplos commits na mesma sessão sob RLS. |
| 2026-08-03 | F13 / orquestrador (fechamento, §8 do PCF) | O §8 do PCF de F13 lista `pytest tests/f1 tests/f2 tests/f4 tests/f5 tests/f6 tests/f9b tests/f10 tests/f11 tests/f12 -q` como comando de regressão, mas `apps/api/tests/f9b` **não existe** — confirmado (`ls apps/api/tests`) e por `grep -rn "f9b" apps/api/tests` (zero ocorrências). F9b ("painel RH e gestor") foi uma fase exclusivamente `apps/web` (dashboards/telas consumindo endpoints já existentes de F1–F8), sem rotas/lógica de backend própria — não há o que testar em `apps/api/tests/f9b` porque a fase nunca criou nada lá. Comando corrigido (sem `tests/f9b`) rodado com sucesso nesta verificação. Também `pnpm exec playwright test src/testes/f13` (mesmo §8) não encontra nenhum `.spec.ts`: `src/testes/f13/**` tem só testes Vitest (`.teste.ts(x)`, já cobertos pela suíte `pnpm test`, 1340 testes verdes), e `apps/web/e2e/` (onde os `.spec.ts` de Playwright de fato vivem, ver F9b/F10) não tem nenhum diretório para F13 — nenhum agente da fase foi explicitamente instruído a escrever e2e Playwright novo para as telas de F13 (portal `/desenvolvedores`, painel de webhooks, admin de SSO). Não é regressão: as telas novas têm cobertura Vitest de componente (A2/A4) e a lógica de sessão SSO ganhou testes de Route Handler nesta verificação (`rota-callback-oidc.teste.ts`/`rota-concluir-saml.teste.ts`) — só falta e2e de navegador real. | Fase de qualidade/hardening (ou o dono de `apps/web/e2e/` numa fase futura) — decidir se vale escrever `e2e/integracoes/**`/`e2e/sso/**` seguindo o padrão de `e2e/cadastros/**`; corrigir o texto do §8 do PCF F13 para não referenciar `tests/f9b` nem um caminho de Playwright sem specs. |
| 2026-08-03 | F13 / orquestrador (achado de revisão adversarial, escopo consciente) | Ao corrigir login-CSRF nas duas rotas NOVAS de SSO (`api/auth/sso/**`, checagem de `Sec-Fetch-Site`/`Origin`/`Content-Type`, `lib/sessao/servidor/csrf.ts`), confirmei que a MESMA classe de vulnerabilidade (login-CSRF: um site de terceiros força o navegador da vítima a autenticar como o ATACANTE) provavelmente também se aplica às três rotas de sessão pré-existentes da F8 — `api/auth/{login,refresh,logout}/route.ts` — nenhuma delas checa origem/`Sec-Fetch-Site` antes de aceitar o corpo e (no caso de `login`) gravar um cookie de sessão. Não corrigido aqui: são arquivos de ownership da F8 (fase já fechada e commitada), e o achado desta sessão é especificamente sobre o código NOVO que eu mesmo escrevi no fechamento da F13 — expandir a correção para lá seria a mesma classe de invenção de escopo que este arquivo existe para evitar. Diferença de severidade a favor de F8: um CSRF de `login` exige que a vítima DIGITE credenciais reais no formulário induzido (mais fricção que os fluxos de SSO, onde o navegador segue um link sozinho), mas o padrão de mitigação (`requisicaoDeMesmaOrigem`/`corpoDeclaradoComoJson`, já escrito e testado em `lib/sessao/servidor/csrf.ts`) é diretamente reaproveitável. | Fase de hardening (F14) — aplicar `requisicaoDeMesmaOrigem`/`corpoDeclaradoComoJson` (`apps/web/src/lib/sessao/servidor/csrf.ts`, já existe) aos três Route Handlers de `api/auth/{login,refresh,logout}` (F8). |
| 2026-08-03 | F13 / orquestrador (achado de revisão adversarial, decisão explícita de não corrigir) | A ponte de sessão SSO (fechamento desta fase) faz `callbackSso` ser alcançado por `fetch` servidor-a-servidor (`apps/web` → `apps/api`) em vez de navegação direta do navegador — consequência: `_ip_do_cliente(request)` (`apps/api/app/routers/sso.py`) passa a capturar o IP do SERVIDOR web/proxy reverso, não mais o IP real do usuário final, para todo login OIDC (`usuario.ultimo_acesso_ip`/IP da sessão/refresh token gravados errados). Confirmado que `api/auth/login/route.ts` (F8, login por senha) já tem exatamente o mesmo comportamento — não é uma regressão exclusiva do SSO, é uma característica já existente de qualquer autenticação que passe pelo proxy Next.js. Decisão: não corrigir agora. Corrigir direito exige um mecanismo de IP confiável ponta a ponta (`X-Forwarded-For`/`X-Real-IP` só aceito quando a origem imediata é o próprio proxy reverso de produção — nunca um cabeçalho cru confiado às cegas, que abriria spoofing de IP) que nenhuma fase decidiu ainda, e não é ownership isolado da F13 corrigir sozinha um padrão que o login por senha (F8) já usa há mais tempo. | Fase de hardening (F14, "revisão de RLS/rate limiting" já é o mesmo espírito) — decidir e implementar um mecanismo de IP confiável (allowlist de proxy reverso + `X-Forwarded-For`) para TODAS as rotas que hoje capturam IP via `apps/web` como intermediário, não só SSO. |
| 2026-08-03 | F13 / orquestrador (fechamento, `mypy app` completo — nunca rodado combinado antes desta sessão) | `mypy app` acusa 3 erros em 2 arquivos, nenhum deles tocado por F13 nem por nenhuma correção desta sessão: `app/routers/saude.py:55` (`redis.asyncio.from_url` sem stub tipado, `no-untyped-call` — linha de `480854d`, 2026-07-25, scaffold original da F1) e `app/routers/relatorios.py:280,285` (`salvar_preferencia` recebendo `str \| None`/`bool \| None` onde o tipo declarado exige não-opcional — linhas de `9e724b6`, 2026-07-30, F11). Confirmado via `git blame` que ambos predatam completamente a F13. Não corrigido: fora do ownership de F13, e nenhuma das duas fases (F1, F11) tratou `mypy` como gate de fechamento na época. | Fase de hardening (F14) — corrigir os 2 arquivos e decidir se `mypy app` passa a ser gate obrigatório de fechamento de fase daqui em diante (hoje só `ruff check`/`ruff format --check` são cobrados nesse nível). |
| 2026-08-03 | F13 / orquestrador (achado de infraestrutura de teste, fechamento — regressão F10) | O banco Postgres de teste compartilhado da VPS (`ponto`, container `ponto-teste-postgres-1`) acumulou **1.871 tenants ativos** ao longo desta sessão (nenhuma fase/agente limpa `tenants` após rodar sua suíte) — o suficiente para `tests/f10/notificacao/test_verificacao_cross_tenant.py` (que via `listar_tenants_ativos_cross_tenant()` itera TODO tenant ativo com round-trip próprio) estourar qualquer timeout razoável sobre o túnel SSH. `TRUNCATE TABLE tenants CASCADE` direto falha (`registro_imutavel`, gatilho `trg_auditoria_bloqueia_truncate` — Portaria MTP 671/2021, ADR-002, propositalmente bloqueia TRUNCATE/UPDATE/DELETE em `auditoria`, mesmo para o role superusuário) e a transação inteira faz rollback (DDL é transacional). Resolvido com o mesmo padrão que `tests/f5/conftest.py` já documenta para os testes adversariais T4 (`dsn_superusuario_f5`, comentário linhas 33-38): `BEGIN; SET LOCAL session_replication_role = 'replica'; TRUNCATE TABLE tenants CASCADE; COMMIT;` — desliga gatilhos de usuário só para essa transação, nunca a role da aplicação. Após a limpeza (`tenants` a zero), o mesmo teste caiu de timeout/hang para 25s. | Nenhuma ação de código necessária. Considerar (F14 ou hardening de infra) um script `tools/limpar_banco_teste.py` que aplique esse padrão automaticamente entre rodadas de regressão completa, para não depender de descobrir isso de novo cada vez que o banco compartilhado crescer. |
| 2026-08-03 | F13 / orquestrador (fechamento, primeira execução real de `schemathesis run` desde que a ferramenta foi citada na F0) | Rodei `schemathesis run packages/contracts/openapi.yaml --checks all` contra a API real pela primeira vez (T6 só tinha instalado a dependência e escrito os alvos `make schemathesis`/`.\tasks.ps1 schemathesis`/job de CI, nunca executado). Três achados de ferramental corrigidos antes de conseguir um resultado legível: (1) o contrato declara `openapi: 3.1.0`, mas a série 3.x do schemathesis só suporta 3.1 sob a flag `--experimental=openapi-3.1` — sem ela, falha antes de carregar o schema; **os três alvos (`Makefile`, `tasks.ps1`, `ci.yml`) estavam quebrados por essa falta**, corrigidos agora. (2) no console do Windows (cp1252), o próprio schemathesis quebra com `UnicodeEncodeError` ao tentar IMPRIMIR o relatório de falhas (payload fuzzado contém byte fora da code page) — corrigido com `PYTHONIOENCODING=utf-8` (`tasks.ps1`; `ci.yml` roda em `ubuntu-latest`, já UTF-8, não precisa). (3) meu primeiro run mediu contra o banco de teste que eu mesmo tinha acabado de truncar (achado da linha acima) — com zero tenants, praticamente toda operação autenticada 404 (`PONTO-TEN-001`) só por isso; não é sinal de bug. Com o ferramental corrigido, `--checks all` (227 operações) devolveu 3 categorias reais: **"Server error" (3 ocorrências, INVESTIGADAS UMA A UMA)** — `GET /v1/sso/{provedor}/iniciar?tenant=%00` era bug de verdade (NUL byte chega cru em `fn_resolve_tenant(:slug)`, Postgres não representa NUL em TEXT, exceção crua do driver vira 500 em vez de 404 de negócio — corrigido em `app/identidade/autenticacao/tenant.py::resolver_tenant_para_autenticacao`, rejeita NUL cedo como `PONTO-TEN-001`); as outras duas (`callback` OIDC e `POST /v1/sso/saml/acs`) eram falso-positivo — meu servidor local não tinha `SSO_ESTADO_CHAVE_SECRETA`/`SSO_SAML_ESTADO_CHAVE` configuradas, e as duas rotas falham fechado por design nesse caso (comentário "Falha fechada" em `app/identidade/sso/{oidc,saml}/estado.py`) — confirmado reproduzindo com as chaves configuradas: zero "Server error" no rerun. **"Response violates schema" (105 ocorrências, MESMA causa raiz em TODAS)** — `app/core/erros.py::montar_problema` embute `request.url.path` cru no campo `instance` do corpo `Problema`; um segmento de path fuzzado com caractere de controle (ex. `\x1f`) faz o próprio valor violar o formato `uri-reference` que o contrato declara para esse campo — corrigido com `urllib.parse.quote(caminho, safe="/:@!$&'()*+,;=")` (um único ponto de correção, usado por toda resposta de erro do sistema); confirmado zero ocorrências no rerun, e regressão alvo (`tests/f1` + `tests/f13/sso`, 133 testes) continua 100% verde — nenhum teste depende do valor literal de `instance` (`test_login.py` já descarta o campo antes de comparar). **NÃO corrigidas** (fora do escopo de uma correção pontual, ver coluna de ação): "Undocumented Content-Type" (74→134 ocorrências entre as duas rodadas, variação por ser fuzzing não-determinístico) — requisição verdadeiramente malformada (HTTP quebrado, não JSON malformado) é rejeitada pelo próprio Starlette/h11 ANTES de chegar aos exception handlers da aplicação, então sai como `text/plain` cru em vez do `application/problem+json` do contrato; exigiria middleware no nível ASGI, abaixo da camada onde a aplicação hoje intercepta erros. "Undocumented HTTP status code" (92 ocorrências, quase todas `PONTO-TEN-001`) — o contrato declara `responses` por operação sem incluir sistematicamente o 404 de tenant-não-encontrado, que na prática qualquer operação autenticada pode devolver (resolução de tenant é middleware transversal, não lógica de cada operação) — gap de completude do contrato pré-existente a F13, não introduzido por ela. | Fase de hardening (F14): (a) considerar middleware ASGI de nível mais baixo para garantir `application/problem+json` mesmo em erro de parsing HTTP bruto; (b) acrescentar 404/`PONTO-TEN-001` como resposta padrão em TODA operação autenticada do contrato (`packages/contracts/openapi.yaml`), ou documentar explicitamente que esse 404 é uma exceção transversal não listada por operação; (c) considerar `--auth`/token real e um tenant semeado com slug fixo no `schemathesis run` de CI para reduzir ruído de "tenant não encontrado" e testar caminhos de sucesso (200), não só de erro — hoje o job de CI roda sem nenhuma credencial. |
| 2026-08-05 | F14 / A3 (achado de fraseado do PCF, não bloqueante) | O PCF da F14 (§5, "A3 — LGPD") pede vínculo de `criarConsentimento`/`revogarConsentimento` com "`tipos_tratamento`, já existe como tabela". Existe de fato uma tabela `tipos_tratamento` (`packages/contracts/schema.sql` seção 9), mas ela cataloga categorias de CORREÇÃO DE JORNADA (`inclusao_marcacao`, `ajuste_intervalo`, `abono`, `justificativa`, ...) — nada a ver com finalidade de tratamento de dado pessoal LGPD. `consentimentos` não tem FK nenhuma para ela; o vínculo real, confirmado em `schema.sql` seção 15, é a coluna `finalidade` (`CHECK` com os seis valores do enum do contrato: `biometria_facial`, `biometria_digital`, `geolocalizacao`, `foto_registro`, `uso_imagem`, `comunicacao`). Implementado usando `finalidade` (`apps/api/app/lgpd/consentimentos.py`, docstring do módulo explica o achado) — nenhuma mudança de contrato, nenhuma FK nova criada. | Nenhuma ação necessária — implementado corretamente com `finalidade`; registrado para quem ler o PCF original não estranhar a ausência de FK para `tipos_tratamento`. |
| 2026-08-05 | F14 / A3 (decisão de interpretação de contrato, não é mudança de contrato) | `criarSolicitacaoTitular` (`PONTO-LGPD-003`, 409) tem duas leituras possíveis para "eliminação alcança dado retido por obrigação legal": (a) a CRIAÇÃO do pedido falha com 409 sempre que o titular tem marcação; ou (b) o pedido é sempre criado (201) e processado, com o código citado dentro de `resposta` fundamentando a retenção parcial. Implementei (b) — `apps/api/app/lgpd/solicitacoes.py::_processar_eliminacao` — porque (a) contradiz a própria PCF ("produza um relatório do que foi feito/não pode ser feito e por que, nunca um apagamento silencioso") e porque a maioria esmagadora dos titulares reais TEM marcação (um 409 duro bloquearia praticamente todo pedido de eliminação, sem nunca produzir o relatório exigido). Documentado na docstring do módulo. | Orquestrador — confirmar a leitura (b) ou redirecionar para (a) com PCF/RFC próprios; nenhum teste de aceite desta fase depende da distinção (o critério de aceite de A3 fala em "relatório", que só (b) produz). |
| 2026-08-05 | F14 / A3 (achado de contrato, decisão de implementação sem mudança de schema/openapi) | `ConsentimentoCriar.hashTermo` é opcional no contrato (`hashTermo: string \| None`), mas `consentimentos.hash_termo` é `NOT NULL` no schema e ADR-006 regra 7 exige "o texto EXATO aceito" — sem hash não há prova nenhuma do que foi consentido. `criarConsentimento` (`apps/api/app/lgpd/consentimentos.py`) recusa com `PONTO-VAL-001` quando `hashTermo` está ausente, tratando-o como obrigatório NA PRÁTICA (o contrato continua aceitando o corpo sem o campo — a rejeição é de regra de negócio, não de schema). Mesma classe de lacuna que `docs/backlog.md` já registra para `criarBiometria`/`versaoModelo` (campo usado pelo exemplo do próprio `openapi.yaml` sem estar no schema tipado). | Nenhuma ação necessária — comportamento defensável e documentado; considerar RFC futura para marcar `hashTermo` como `required` em `ConsentimentoCriar` se algum cliente real tropeçar na mensagem `PONTO-VAL-001`. |
| 2026-08-05 | F14 / A3 (limite de escopo documentado, não é bug) | O motor de expurgo automático (`apps/api/app/lgpd/expurgo.py`) implementa de verdade `biometria` (expurgo real do template cifrado), `sessao` e `notificacao` (deleção genérica por coluna de tempo), e blinda `marcacao`/`auditoria` (nunca tocadas, mesmo após o prazo legal — append-only com gatilho de banco). As sete entidades restantes do `CHECK` de `politicas_retencao.entidade` (`foto_registro`, `documento`, `espelho`, `afd`, `aej`, `log_acesso`, `fila_offline`, `relatorio_execucao`) são reconhecidas mas devolvem `executado=False` com motivo explícito ("sem implementação nesta fase") — nunca um `KeyError` nem um "sucesso" silencioso. `foto_registro` em particular não tem nenhuma fonte de imagem persistida ainda no sistema (mesma lacuna que F6/A2 já registrou nesta tabela em 2026-07-26: "nenhuma fase até agora integrou um cliente de armazenamento de objetos de verdade" para foto crua). | Fase futura (ou extensão desta) que precisar de retenção automática para essas sete entidades — o padrão de `_eliminar_por_timestamp` (`app/lgpd/expurgo.py`) é diretamente reaproveitável para qualquer entidade com uma coluna de tempo única e sem gatilho de imutabilidade (`documento`, `fila_offline`, `relatorio_execucao` são candidatos óbvios; `espelho`/`afd`/`aej` provavelmente têm a mesma guarda legal de 5 anos que `marcacao` e merecem a mesma blindagem, não o expurgo genérico). |
| 2026-08-03 | F13 / orquestrador (achado sistêmico, fechamento — regressão F1–F12) | `os.environ["DATABASE_URL"] = ...` **direto** (nunca `monkeypatch.setenv`, nunca revertido) é um padrão que se repete em fixtures de teste de PELO MENOS quatro fases já commitadas — `tests/f1/autenticacao/conftest.py:187`, `tests/f1/conftest.py:579` (este reverte manualmente num `finally`, linha 589, mas ainda por atribuição direta, frágil a exceção antes do `finally`), `tests/f2/importadores/conftest.py:36`, `tests/f10/e2e/test_fluxo_completo.py:123`, `tests/f10/notificacao/conftest.py:439`, `tests/f11/conftest.py:1164` (mesmo padrão de reversão manual de F1). Rodando a regressão completa (`pytest tests/f1 tests/f2 tests/f4 tests/f5 tests/f6 tests/f10 tests/f11 tests/f12`, nunca feito combinado antes desta sessão — cada fase sempre rodou sua própria suíte isolada) encontrei e corrigi UMA ocorrência real desta classe que causava falha genuína (`tests/f4/dominio/test_apurar_dia_worker.py::ambiente_app_apontado_para_teste` — `DATABASE_URL` vazado + engine de `app.db.sessao` presa a um event loop já fechado de um teste anterior, `RuntimeError: Event loop is closed`) e outra por precaução (`tests/f4/banco_horas/test_vencimento.py::ambiente_worker_banco_horas`, mesmo padrão, ainda sem falha observada). As oito ocorrências restantes (F1/F2/F10/F11, todas em fases já fechadas e commitadas) **não foram tocadas** — corrigir oito arquivos de quatro fases diferentes é escopo muito maior que o fechamento da F13 justifica sozinho, e a suíte completa passou limpa com as duas correções acima (nenhuma das oito restantes se provou necessária nesta rodada — o vazamento só morde dependendo da ORDEM de coleta do pytest, que pode mudar). | Fase de hardening dedicada (F14 ou uma sessão própria de "saúde da suíte de testes") — trocar as oito ocorrências restantes para `monkeypatch.setenv`, e considerar um lint/grep de CI que rejeite `os.environ[...] =` direto em qualquer `tests/**/conftest.py`/`test_*.py` novo daqui em diante. |
| 2026-08-05 | F14 / A1 (achado crítico, contradiz premissa do ADR-014) | ADR-014 (§"Decisão", item 1) afirma que "prova de vida e similaridade facial via `analise-facial-edge` já produzem sinal real hoje" para o canal web (F8). Falso na prática: `apps/facial-svc/facial/rotas/biometria.py` (`POST /enroll`, `/verificar`, `/liveness`) responde `501 PONTO-INT-005` para as três operações desde a Fase 0 (`git log` confirma zero commits no arquivo além de `480854d`) — nenhum consumidor de `apps/api` chama esses endpoints hoje (`grep -rn "facial_svc\|FACIAL_SVC"` só acha a URL de config, nunca um cliente HTTP real). `apps/api/app/biometria/servico.py::ler_template_decifrado` já documenta isso corretamente ("existe para consumidores internos futuros"). Consequência para o motor de score (`app.antifraude.motor`): `score_facial`/`liveness_aprovado` chegam `None` em TODO tráfego desta fase (não só nos sinais reservados para F7) — a categoria "biometria" fica sempre `nao_aplicavel`, peso renormalizado para as outras três categorias. Não é bug do meu motor (ele trata `None` corretamente, "nunca inventar valor", ADR-014) — é o ADR-014 descrevendo uma realidade que não existe no código. Corrigi o hardcode local (`marcacao/pipeline/ingestao.py::_sinais_do_corpo` trocou `attestation_veredito="indisponivel"` por `"nao_aplicavel"`, mais preciso), mas NÃO implementei o motor `facial-svc` de verdade (InsightFace/ArcFace real) — isso é uma fase própria, não "score de confiança". | Fase dedicada a implementar o motor `facial-svc` de verdade (F2/F6/F8 nunca fecharam essa lacuna apesar do nome sugerir que sim) — até lá, o motor de composição de F14/A1 já está pronto para consumir `score_facial`/`liveness_aprovado` reais no dia em que existirem (testado com sinal sintético documentado em `tests/f14/antifraude/test_motor_composicao.py::test_biometria_real_quando_disponivel_sinal_sintetico_documentado`). Considerar corrigir o texto do ADR-014 para não afirmar que o sinal facial "já é real" para o canal web. |
| 2026-08-05 | F14 / A1 (achado real, reproduzido com `TestClient`) | `apps/api/app/routers/marcacoes.py::criar_marcacao`/`sincronizar_marcacoes_offline` (linhas 81/324) passam `request.client.host` **sem validar formato** para `ingestao.registrar_marcacao(..., ip_origem=...)`, que grava direto em `marcacoes_meta.ip` (coluna `INET`). Qualquer valor que não seja um IPv4/IPv6 sintaticamente válido — inclusive o peer padrão `"testclient"` que `starlette.testclient.TestClient` usa quando o construtor não recebe `client=(...)` explícito — quebra com `asyncpg.exceptions.DataError: invalid input for query argument ...: 'testclient' does not appear to be an IPv4 or IPv6 interface` no `flush()`, **500 cru** (não um `ErroDeAplicacao` do catálogo). Reproduzido escrevendo `tests/f14/antifraude/test_explicabilidade_http.py` (primeiro teste desta fase — e aparentemente do projeto — a exercitar `POST /v1/marcacoes` via `TestClient` real; toda a suíte de F5 chama `registrar_marcacao` direto, nunca por HTTP, por isso nunca pegou isso). Contornei no MEU teste passando `TestClient(app, client=("203.0.113.77", 51000))` (endereço sintaticamente válido) — não é correção do bug, só workaround do teste. Não corrigi `routers/marcacoes.py` (fora do meu ownership) nem `app/db` (idem). Este é o MESMO mandato que o PCF de F14 já dá a A2 ("mecanismo de IP confiável ponta a ponta") e que o backlog de F13 (2026-08-03, linha acima na tabela) já registrou para SSO — esta entrada acrescenta uma reprodução CONCRETA e determinística (não depende de proxy nem de rede real, só de `request.client.host` não ser sempre um IP) que qualquer correção de A2 precisa cobrir: validar/normalizar (ou descartar com `None`) qualquer `ip_origem` que não passe em `ipaddress.ip_address()` antes de gravar em coluna `INET`, não só resolver a origem confiável via `X-Forwarded-For`. | F14 / A2 (mecanismo de IP confiável já é seu mandato no PCF) — ao implementar, garantir que valores sintaticamente inválidos (não só IPs "não confiáveis") nunca cheguem a uma coluna `INET` como erro 500 cru; considerar `app.organizacao.redes.ip_valido` (já existe, F2) como o validador de guarda antes do `INSERT`. |
| 2026-08-05 | F14 / A1 (achado de processo, teste alheio fica com comentário desatualizado, não quebrado) | `tests/f5/pipeline/test_ingestao.py` (ownership de F5, não meu) tem duas asserções (`test_todos_os_canais_aceitos_produzem_marcacao_pelo_mesmo_caminho`, linha 91; `test_score_stub_nunca_bloqueia_nem_gera_revisao`, linha 549) que checam `score_confianca == 100` com o comentário "essa ausência [de PONTO-SCORE-*] é a própria asserção positiva do stub permissivo". Depois de eu substituir o corpo de `avaliar_confianca` (ADR-008, motor real), **as duas ainda passam** (confirmado rodando a suíte completa de F5: `pytest tests/f5 -q`, 57/57 verdes, zero regressão) — os corpos sintéticos daqueles testes não passam `latitude`/`longitude` nem (na maioria dos canais) `dispositivoId`, então nenhuma categoria do motor tem sinal disponível e `SCORE_SEM_SINAIS = 100` (decisão documentada em `app.antifraude.motor`: ausência de TODO sinal nunca é evidência de fraude, ADR-014) devolve exatamente o valor que o teste espera — coincidência correta, não coincidência frágil. O que ficou desatualizado é só o TEXTO do comentário ("prova que o stub é permissivo": não é mais um stub, é o motor real coincidindo com o valor antigo por desenho). Não editei o arquivo (fora do meu ownership, mesma regra que already impediu F6/A1 de tocar `test_andaime_device_gw.py`). | F5 (fase já fechada) ou orquestrador — atualizar o comentário das duas asserções para refletir que agora é o motor real (F14/A1) produzindo 100 por ausência de sinal, não mais um stub; nenhuma mudança de código necessária, suíte já verde. |
| 2026-08-05 | F14 / A3 (obsolescência esperada do andaime, mesmo padrão já registrado por F6/A1 em 2026-07-26 para `sincronizar_terminal`) | Implementar `expurgo_lgpd` de verdade (worker) quebra `apps/worker/tests/test_andaime_worker.py::test_toda_tarefa_devolve_nao_implementado_sem_levantar`: o teste chama a tarefa com o argumento sintético não-UUID `tenant_id="t"` (reservado para tarefas ainda-stub) e minha implementação real levanta `ValueError: badly formed hexadecimal UUID string` em `UUID(tenant_id)` em vez de devolver o dicionário `nao_implementado` — idêntico ao achado já registrado para `sincronizar_terminal` (F6/A1, mesma tabela, 2026-07-26: "a implementação real levanta `ValueError`... em vez de devolver o dicionário `nao_implementado`"). Confirmado isolando o teste (`pytest tests/test_andaime_worker.py -q`, banco real configurado): 10 de 11 testes passam; só este quebra, e só por causa do argumento sintético, não por bug de lógica (a suíte própria de `apps/api/tests/f14/lgpd/test_expurgo.py`, 11 testes, cobre a tarefa de verdade com UUID real). Não editei `test_andaime_worker.py` (arquivo travado ao orquestrador, mesmo tratamento que `test_andaime.py`/`test_andaime_device_gw.py` já recebem nesta tabela). `test_scheduler_settings_registra_as_duas_rotinas_de_cron` continua passando (comparação dinâmica contra `ROTINAS`, sem número fixo) apesar do nome do teste e do comentário do arquivo (`"as duas rotinas de cron exigidas por events.yaml"`) estarem defasados — agora são seis rotinas de cron no total, quatro delas sem evento de `events.yaml` (mesmo padrão já usado por `despachar_webhooks_pendentes`/F13 para a minha nova `verificar_politicas_retencao_lgpd`). | Orquestrador — mesmo tratamento já dado ao achado de F6/A1: atualizar `_TAREFAS_JA_IMPLEMENTADAS`/`argumentos` em `test_andaime_worker.py` para que `expurgo_lgpd` receba um `tenant_id` UUID válido (ou entre na lista de tarefas já implementadas, pulando a chamada), e revisar o comentário "duas rotinas de cron" (defasado desde `despachar_webhooks_pendentes`, F13). |
| 2026-08-07 | Orquestrador (achado sistêmico, descoberto ao reativar o CI de push após 12 dias parado) | O gatilho `push` do GitHub Actions ficou mudo por 12 dias (2026-07-25 a 2026-08-06) porque o GitHub App "Claude" instalado no repositório tinha permissão somente-leitura em Actions — GitHub não cria workflow run nenhum quando o token do App não tem `actions: write`, silenciosamente (sem erro visível em lugar nenhum). Corrigido aprovando a atualização de permissão do App (usuário, 2026-08-07); confirmado que push volta a disparar CI em tempo real. Como consequência, a primeira execução real do CI em semanas expôs uma cascata de achados que estavam invisíveis: (1) `spectral` reprovava por `proximoCursor: null` num exemplo do contrato (corrigido); (2) coleta do `pytest` inteira travava — F13/F14 introduziram subpastas em `apps/worker/tests/` com caminho relativo idêntico a pastas já existentes em `apps/api/tests/` (`f13/webhooks`, `f13/importadores/afd_terceiro`, `f14/lgpd`), e os dois são pacotes `tests` — corrigido isolando cada app em processo próprio de pytest no CI; (3) job `codeql` reprovava com "Resource not accessible by integration" por falta de `actions: read` no `GITHUB_TOKEN` do job (corrigido) — e, ao corrigir isso, um bug seu (do orquestrador) apareceu: o shell `bash` do Actions roda com `-e` por padrão, que o laço por-app do pytest não desativava, abortando no primeiro app que falhasse (corrigido com `set +e` explícito); (4) o achado maior: praticamente toda fixture de banco de F5 em diante (26 `conftest.py`, ver entrada abaixo) nunca funcionou em CI. | Nenhuma — já corrigido e commitado (`ed35d88`, `e4e1263`). Registrado aqui só para o histórico ficar completo; nenhuma fase precisa agir. |
| 2026-08-07 | Orquestrador (achado sistêmico, mesmo contexto da entrada acima) | 26 `conftest.py` de `apps/api/tests/f1` a `f14` compartilham o mesmo padrão de fixture: uma conexão "administrativa" lida de `PONTO_TEST_DATABASE_URL`, usada para criar um banco e uma role de LOGIN exclusivos por fase/agente (o motivo documentado em cada um: testar RLS conectado como superusuário não prova nada). O job `test-python` do CI nunca setava essa variável — só `DATABASE_URL`, que nenhum desses 26 arquivos lê — então toda essa família de testes sempre caía no default de desenvolvimento local de cada fixture (túnel SSH para a VPS, porta 15432 ou equivalente), inalcançável do runner do CI: dependendo do default de cada fase, ou "connection refused" ou "password authentication failed". Confirmado que isso NUNCA funcionou em CI (a última execução real e verde, 2026-07-25, antecede F5 no histórico do repo — só tinha 55 testes, compatível com F0–F4, que não usam esse padrão). Corrigido setando `PONTO_TEST_DATABASE_URL` no `test-python` do `ci.yml` para a mesma URL do `DATABASE_URL` (`ponto`, o `POSTGRES_USER` do serviço, já é superusuário de verdade na imagem oficial do Postgres). **Validado em CI real (2026-08-07): confirmado.** A causa raiz específica era ainda mais fina — `alembic/env.py` prioriza `DATABASE_URL_SYNC` sobre `DATABASE_URL`, e os 21 `conftest.py` (de 26; os 5 restantes não seguem esse padrão) só sobrescreviam `DATABASE_URL` no subprocess de migração, nunca `DATABASE_URL_SYNC` — o subprocess herdava o `DATABASE_URL_SYNC` do nível do job (apontando para `ponto_test`, já migrado) e migrava esse banco de novo (por isso "sucesso" silencioso, nunca `RuntimeError`), deixando o banco EXCLUSIVO de cada fase sem schema nenhum. Corrigido nos 21 arquivos (commit `08f0548`). Resultado antes→depois em `apps/api` (mesma suíte, ~2065 testes): erros de setup 205→26 (−87%), problema total 246→96 (−61%); zero ocorrências de "relation ... does not exist"/"password authentication failed"/"connection refused" na porta 15432 restantes. O que sobrou depois do fix: MinIO não provisionado no CI (~118 testes, ver entrada de 2026-08-07 sobre o job `test-python`) e 2 testes com expectativa desatualizada (`test_andaime.py::test_cabecalho_obrigatorio_ausente`, `f14/adversarial/test_bypass_score_confianca.py::test_score_injetado_nao_afeta_cross_tenant_via_header_x_tenant` — ambos corrigidos, ver entradas próprias abaixo). | Nenhuma ação de fase — infraestrutura de CI, não código de fase. |
| 2026-08-07 | Orquestrador (achado real, bug no próprio mecanismo de triagem, não na aplicação) | O filtro de achados novos do job `codeql` (`.github/workflows/ci.yml`) compara `(ruleId, caminho, linha)` contra uma allowlist com caminhos relativos à RAIZ do repo (`apps/api/app/...`), mas o CodeQL relata o `uri` do achado relativo ao `source-root` configurado (`apps/api`, sem o prefixo) — os 3 achados de `py/weak-sensitive-data-hashing` já triados (F14/A2, 2026-08-05) apareciam como "novos" TODA execução por causa disso, nunca batendo com a allowlist. Corrigido prefixando `matrix.fonte` ao caminho do SARIF antes de comparar. Além disso, a mesma execução (primeira vez que o job `codeql` conclui a análise de verdade, já que sempre falhava antes por falta de permissão) achou 3 achados genuinamente novos: `py/insecure-protocol` em `tests/f14/hardening/test_mtls_facial_svc.py:263` (contexto SSL do cliente de teste não pinava `minimum_version`, só `maximum_version` — corrigido, agora pina os dois em TLS 1.2, mesma escolha defensiva já documentada no `servidor_mtls` do mesmo arquivo) e 2× `py/log-injection` em `apps/api/app/comum/armazenamento.py:89,127`. Os dois de log-injection são falso-positivo: o valor (`chave`) sempre vai via `extra={}` do `logging`, nunca interpolado na mensagem — `app/core/log.py::FormatadorJson` serializa `extra` como campo JSON separado via `json.dumps` (escapa qualquer newline/caractere de controle automaticamente) e `FormatadorTexto` (dev-only) nem inclui campos de `extra` na linha renderizada — não há como um valor de `chave` malicioso forjar uma linha de log falsa em nenhum dos dois formatters. Adicionados à allowlist com esta justificativa. | Nenhuma — já corrigido e commitado. |
| 2026-08-07 | Orquestrador (MinIO ausente do CI, causa do `NewConnectionError`/`Connection refused` porta 9000 em ~118 testes de F10) | O job `test-python` do `ci.yml` só provisionava `services: postgres` e `services: redis` — nunca MinIO, então `apps/api/tests/f10/**` (que exercita `app/comum/armazenamento.py`/`salvar_objeto`/`obter_url_assinada`) sempre falhava com conexão recusada na porta 9000. A imagem oficial `minio/minio` não sobe sozinha dentro do bloco nativo `services:` do GitHub Actions — exige o argumento posicional `server /data`, que `services:` não tem como passar (só aceita `image`/`env`/`ports`/`options`/`volumes`, sem override de `command`; confirmado sem solução nativa em github.com/orgs/community/discussions/26688). Corrigido subindo o MinIO via `docker run` manual (step dedicado, mesma tag de imagem de `infra/docker-compose.yml`), com espera de saúde explícita (`/minio/health/live`) e criação do bucket `ponto` via SDK `minio` antes das migrations (nenhum teste de F10 cria o bucket sozinho, ao contrário de F12/F13/F14, que chamam `armazenamento.garantir_bucket()` na própria fixture — `test_garantir_bucket_padrao_ja_existe` documenta explicitamente essa expectativa de "ambiente já provisionado"). | Nenhuma — já corrigido e commitado (`1e7b33c`). |
| 2026-08-07 | Orquestrador (achado real de arquitetura, teste F14/A4 com premissa incorreta sobre a própria construção) | `f14/adversarial/test_bypass_score_confianca.py::test_score_injetado_nao_afeta_cross_tenant_via_header_x_tenant` falhava com 404 `PONTO-REC-001` em vez do 201 esperado, mesmo depois do fix de `DATABASE_URL_SYNC`. Investigado em profundidade (rodado contra Postgres real com prints de depuração, revertidos depois — ver commit): a asserção original ("X-Tenant é vestigial aqui, dependency override resolve tudo") estava errada sobre a própria arquitetura. `app.dependency_overrides[obter_sujeito]` substitui a dependência do FastAPI, mas `TenantMiddleware` (`app/core/middleware.py`) é ASGI puro, roda ANTES da injeção de dependência, e continua publicando o tenant resolvido do cabeçalho `X-Tenant` em `contexto.tenant_atual()` — é essa ContextVar, não `sujeito.tenant_id`, que `app/db/sessao.py::obter_sessao` usa para `SET LOCAL app.tenant_id` (RLS). Com sujeito fixado em A mas RLS escopada em B, os registros de A ficam invisíveis — 404 é o resultado correto e seguro (fail-closed por RLS), não um vazamento nem um bypass. Confirmado também que este teste, por substituir `obter_sujeito` inteiro, nunca exercitou de fato a checagem real de divergência token/cabeçalho (`PONTO-TEN-002`, `app/core/seguranca.py`) — essa lógica fica fora do caminho testado junto com o resto da dependência substituída. A defesa de identidade contra este vetor específico já está coberta por outro caminho verificado nesta investigação: `resolver_sujeito` (`app/identidade/rbac/resolucao.py:120-122`) recusa explicitamente sujeito/tenant divergentes com um JWT real. Corrigida a asserção do teste para o comportamento real (404/`PONTO-REC-001`) e documentada a lacuna arquitetural na própria docstring. | Nenhuma — já corrigido e commitado. Considerar, como trabalho futuro (não urgente, defesa já coberta por outro caminho), um teste adicional de F14/A4 usando um JWT real (não dependency override) para cobrir `PONTO-TEN-002` diretamente. |
| 2026-08-07 | Orquestrador (**bug real de produção**, não só de teste — achado ao investigar `StaleDataError` em `apps/api/tests/f11/**`) | `apps/worker/worker/tarefas/relatorios.py::executar_relatorio` chama `aplicar_tenant(sessao, tenant_id)` (`SET LOCAL app.tenant_id`, escopo por TRANSAÇÃO) **uma única vez**, no início — mas a tarefa faz múltiplos `commit()` na mesma sessão (progresso incremental, status `processando`, status final). `commit()` encerra a transação corrente, revertendo `app.tenant_id` para `NULL`; toda leitura/escrita seguinte na mesma sessão passa a rodar sob a policy de RLS `pol_isolamento_tenant` **sem tenant nenhum** — falha fechada, SELECT devolve zero linhas silenciosamente, UPDATE por chave primária não encontra a linha e o ORM levanta `StaleDataError`. Mascarado em CI porque `DATABASE_URL` do `test-python` usa o role `ponto` (superusuário, sempre bypassa RLS) — mas **produção já roda com `ponto_app_runtime`** (não-superusuário, migration `0004_role_login_app`) desde que essa migration foi aplicada. Impacto real, verificado empiricamente (reproduzido sob o role `ponto_app_runtime` real, não só teoria): todo job assíncrono de relatório em produção (1) gera conteúdo vazio/errado — a leitura do dataset em `_executar` também roda sem tenant após o primeiro commit — e (2) trava com `StaleDataError` não capturado no commit final, deixando a execução presa em `'processando'` para sempre. Corrigido republicando `aplicar_tenant` logo após cada `commit()` que precede mais uso da sessão (3 pontos: após o commit de progresso, após o commit de `status='processando'`, antes de `_entregar_agendamento`). Validado: 10 testes originais + 17 outros do mesmo arquivo (27/27) passam sob `ponto_app_runtime` real com a correção; sem ela, os 2 testes de `test_worker_executar_relatorio.py` reproduzem o `StaleDataError` exato. Suíte `f11` inteira (189 testes) sem regressão sob o `DATABASE_URL` atual do CI. | **Produção**: este fix deveria subir com prioridade alta — jobs assíncronos de relatório provavelmente já estão travando/gerando conteúdo vazio no ambiente real desde que a migration 0004 foi aplicada lá. Considerar auditar `relatorio_execucoes` em produção por linhas presas em `status='processando'` há muito tempo. Também vale investigar se algum OUTRO worker task com múltiplos `commit()` na mesma sessão tem o mesmo padrão (não auditado nesta sessão, escopo era só `relatorios.py`). |
| 2026-08-07 | Orquestrador (achado de processo, não corrigido ainda — observação do agente que investigou a entrada acima) | O job `test-python` do CI usa `DATABASE_URL` com o role SUPERUSUÁRIO (`ponto`), que sempre ignora RLS (`BYPASSRLS` implícito de superusuário) — é exatamente por isso que o bug de produção da entrada acima nunca apareceu em nenhuma execução de CI, mesmo com o pipeline "consertado" nesta sessão. Enquanto isso não mudar, qualquer bug que só se manifesta sob RLS de verdade (como este) pode continuar passando batido pelo CI. | Tarefa separada, fora do escopo desta sessão: trocar `DATABASE_URL` do job `test-python` para usar um role não-superusuário (ex.: `ponto_app_runtime`, já criado pela migration `0004`) — ou pelo menos rodar uma fatia da suíte sob esse role, para que testes de RLS validem o caminho real de produção, não um caminho que sempre bypassa a proteção que estão supostamente testando. |
| 2026-08-07 | Orquestrador (último problema conhecido do `pytest` em CI, resolvido) | `tests/f14/hardening/test_auditoria_rls.py::test_toda_tabela_com_tenant_id_tem_rls_forcada_e_policy` errava no teardown com `RuntimeError: Event loop is closed`. Causa raiz confirmada por reprodução real (Postgres+Redis via Docker, não só leitura de código): `_cliente_redis` em `app/comum/limitador_taxa.py` é singleton de MÓDULO (global ao processo pytest inteiro, não por teste). `tests/f13/webhooks/test_endpoint_http_e2e.py::test_criar_webhook_ponta_a_ponta_via_api_key` cria/conecta esse cliente dentro do event loop daquele teste, mas essa suíte não tem fixture de limpeza equivalente à `_redis_encerrado_por_teste` de `f14/hardening`. Com `asyncio_default_fixture_loop_scope = "function"`, o loop daquele teste fecha ao final dele; a coleta do pytest ordena `f13` antes de `f14` (ordem alfabética de string), então o primeiro teste de `f14/hardening` herda o cliente órfão via a fixture autouse, e `aclose()` tenta reagendar callback no loop morto. Corrigido em `_encerrar_cliente_redis()` (`app/comum/limitador_taxa.py`): descarta a referência global ANTES de tentar fechar, envolve `aclose()` num `try/except RuntimeError` que só engole especificamente "Event loop is closed" (qualquer outro erro continua propagando) — é best-effort por natureza, o socket já morreu junto com o loop antigo. Validado: reproduzido o erro exato rodando a combinação `test_criar_webhook_ponta_a_ponta_via_api_key` → `test_auditoria_rls.py` antes da correção; depois, mesma combinação passa, suíte `f14/hardening` completa (24 testes) 100%, `f13/nucleo/test_limitador_taxa.py`+`f13/webhooks` (61 testes) sem regressão. | Nenhuma — já corrigido. Considerar, como trabalho futuro (não urgente), dar a qualquer suíte que crie o cliente Redis global uma fixture de limpeza própria, em vez de depender de `f14/hardening` ser sempre a última a rodar por ordem alfabética. |
| 2026-08-07 | Orquestrador (primeira execução real do job `contrato-schemathesis` em CI desde que o gatilho `push` voltou, ver entrada 198) | Com `SSO_ESTADO_CHAVE_SECRETA`/`SSO_SAML_ESTADO_CHAVE` já configuradas neste run (fechando o falso-positivo já documentado na entrada de 2026-08-03 sobre a primeira execução manual), sobraram só 3 operações com falha real, todas de causa nova (nunca vistas na execução manual anterior, que rodava sem MinIO/sem banco migrado de verdade): (1)/(2) `GET`/`POST /v1/tenants` (`listarTenants`/`criarTenant`) reprovam `not_a_server_error` com 501 — mas o 501 é *intencional*, não bug: `app/routers/tenants.py` documenta que a role `ponto_app` não tem `BYPASSRLS` e listar/criar tenant é necessariamente cross-tenant, então as duas operações ficam `PONTO-INT-005` até uma fase decidir o desenho de acesso do suporte SEEG (mesmo item "F1 / A2" já rastreado desde 26/07). O check `not_a_server_error` não distingue "501 documentado, aguardando decisão de fase" de bug — corrigido excluindo só estas duas `operationId` do `schemathesis run` (`--exclude-operation-id listarTenants --exclude-operation-id criarTenant`), mantendo o check ativo nas ~215 operações restantes. (3) `GET /v1/admin/saude` reprova `response_schema_conformance`: `commit` volta `null`, mas o contrato declara `type: string` sem nullable. Causa: `app/routers/saude.py::obter_saude` faz `config.commit or None`, e `Configuracao.commit` (`app/core/config.py`) só é preenchido pela `ARG COMMIT` do Dockerfile — o job de schemathesis sobe a API direto via `uvicorn` (sem build de imagem Docker), então `COMMIT` nunca chegava setada. Corrigido setando `COMMIT: ${{ github.sha }}` no `env:` do job — mesmo valor que um build de imagem real receberia, não um placeholder descartável. | Nenhuma — já corrigido e commitado. Se uma fase futura decidir o desenho de acesso do suporte SEEG para `listarTenants`/`criarTenant` (item "F1 / A2"), remover as duas exclusões de `--exclude-operation-id` do job `contrato-schemathesis`. |
| 2026-08-07 | Orquestrador (achado real, primeira vez que `CI OK` fechou verde e o pipeline chegou a tentar o deploy de verdade) | Com o job `contrato-schemathesis` corrigido (entrada acima), `CI OK` passou pela primeira vez e as 5 imagens de `deploy-hml` publicaram no GHCR sem problema — mas `deploy-hml-aplicar` (SSH para a VPS disparar `infra/deploy-hml.sh`) falhou com `dial tcp ***:22: i/o timeout`: não é erro de autenticação, é o pacote TCP nunca completando o handshake. Confirmado que não é bloqueio do lado da VPS — `ufw status` inativo, `iptables -L INPUT` com policy `ACCEPT` e zero regras, `sshd` escutando em `0.0.0.0:22`/`[::]:22`, sem `fail2ban`, e uma conexão SSH direta desta sessão para a VPS funciona normalmente. Aponta para um firewall de rede do provedor de hospedagem (fora do SO da VM — ex. "Cloud Firewall"/security group) barrando especificamente os ranges de IP dinâmicos dos runners hospedados do GitHub Actions. Decisão do usuário: em vez de manter uma allowlist de IPs do GitHub (grande e muda com frequência), trocar para um runner self-hosted rodando na própria VPS — elimina por completo a necessidade de SSH vindo de fora para este job. Implementado: um usuário dedicado sem privilégio de root (só membro do grupo necessário para rodar `docker compose`), runner oficial `actions/runner` baixado/verificado direto na VPS, registrado com uma label exclusiva do ambiente (token de registro obtido via API do GitHub), instalado como serviço systemd (sobrevive a reboot). `ci.yml` atualizado: `deploy-hml-aplicar` agora roda no runner self-hosted e chama o script de deploy do ambiente hml (`infra/deploy-hml.sh`) diretamente (sem `actions/checkout`, o próprio script já faz `git fetch`/`reset --hard`), sem passar mais pela `appleboy/ssh-action`. Nota de segurança honesta: isto AMPLIA o modelo de confiança em vez de mantê-lo — a chave SSH restrita por forced-command só permitia disparar exatamente `deploy-hml.sh`, mas o runner self-hosted executa qualquer step que o job definir, com o mesmo alcance do usuário do runner (que, via grupo `docker`, é equivalente a root na prática). Aceitável aqui porque só `main` (protegida) dispara o job, mesmo com o repositório público — mas é uma trade-off real, não uma melhoria de segurança pura. Os secrets/chave de disparo manual anteriores não foram removidos — ficam como via de fallback fora do CI, hoje sem uso pelo pipeline. | Nenhuma ação de código pendente — já implementado e validado com execuções reais do pipeline (repositório tornou-se público em 09/08, ver achado de auditoria de prontidão mais abaixo). Se o projeto ganhar mais committers com push direto a `main`, reavaliar o modelo de confiança do runner self-hosted (considerar isolar o runner numa VM/container dedicado com acesso só ao que `deploy-hml.sh` precisa). |
| 2026-08-07 | Orquestrador (dois achados triviais, mesma família da entrada acima, pegos na primeira execução real do runner self-hosted) | (1) `/docker/ponto-eletronico/infra/deploy-hml.sh` e a árvore de trabalho são `root:root` sem permissão de grupo/outro (herança do desenho original via forced-command SSH, que sempre rodava como `root`) — `ghrunner` não tinha nem leitura nem execução, `Permission denied` (exit 126) na primeira execução do job já usando o runner self-hosted. Corrigido com uma regra `sudoers` estritamente escopada (`/etc/sudoers.d/ghrunner-deploy-hml`: `ghrunner ALL=(root) NOPASSWD: /docker/ponto-eletronico/infra/deploy-hml.sh`, exatamente este caminho, não sudo irrestrito) e o passo do `ci.yml` atualizado para `sudo -n /docker/ponto-eletronico/infra/deploy-hml.sh` (a entrada anterior já documentava o `chmod`/ownership como problema conhecido do modelo de confiança, mas o `ci.yml` tinha ficado sem o `sudo` no commit `c37678f` — corrigido em seguida). (2) Com a permissão corrigida, o script rodou até `docker compose pull` e falhou com `unauthorized` nas 5 imagens `ghcr.io/henriquehmas-rgb/ponto-*` — `/root/.docker/config.json` nunca existiu na VPS (achado já flagado em sessão anterior, nunca resolvido). Como pacotes GHCR de repositório privado exigem autenticação mesmo para leitura, e entrar com token/senha é uma ação que não faço mesmo com autorização explícita, o usuário gerou um PAT classic (`read:packages`) e rodou `docker login ghcr.io -u henriquehmas-rgb` pessoalmente (via o Web Terminal do próprio painel da Hostinger, `asc.hostingervps.com` — descoberto durante esta sessão como alternativa ao terminal nativo do SO, que só permite clique, nunca digitação, para automação). `Login Succeeded` confirmado. | Nenhuma — ambos corrigidos. Validar a próxima execução do `deploy-hml-aplicar` de ponta a ponta (esperado: `git reset --hard`, `docker compose pull` das 5 imagens, `up -d`, `alembic upgrade head`, tudo sem erro). |
| 2026-08-07 | Orquestrador (validação final, primeira execução 100% verde do pipeline completo — CI + deploy real) | Rerun do commit `eb7584c` confirmou o pipeline inteiro de ponta a ponta pela primeira vez: `CI OK: success`, as 5 imagens publicadas, `deploy-hml-aplicar: success` — os 8 containers do stack `ponto-hml` (`api`, `web`, `worker`, `device-gw`, `facial-svc`, `scheduler`, `postgres`, `redis`, `minio`) subiram saudáveis e a migration rodou sem erro. Confirmado direto no container (`GET /v1/admin/saude` via `docker exec`): `status: "ok"`, `ambiente: "hml"`. Observação menor, não bloqueante: `commit` voltou `null` mesmo na hml real — o passo `docker/build-push-action@v6` do job `deploy-hml` não passa `build-args: COMMIT=${{ github.sha }}` (só o job `contrato-schemathesis`, que sobe a API direto via `uvicorn`, ganhou esse fix, ver entrada acima). Não é regressão desta sessão — o Dockerfile sempre teve a `ARG COMMIT` disponível, só nunca foi preenchida em nenhum job de build de imagem. | Nenhuma ação urgente. Se quiser que `/v1/admin/saude` real reporte o commit da build (útil para depuração em produção), acrescentar `build-args: COMMIT=${{ github.sha }}` ao step "Build e publica" do job `deploy-hml` — mudança de uma linha, baixo risco. |
| 2026-08-07 | Orquestrador (resolve a observação em aberto de duas entradas acima — task #61) | `test-python` e `contrato-schemathesis` passaram a rodar sob `ponto_app_runtime` (role de LOGIN mínima, sem `BYPASSRLS`, a mesma que produção usa desde a migration `0004_role_login_app`/F14) em vez do superusuário `ponto`. Implementado como um step novo logo depois de "Migrations Alembic" em cada job (`echo "DATABASE_URL=..." >> "$GITHUB_ENV"`) — a migration em si continua rodando sob o superusuário (precisa de `CREATE ROLE`), só os steps SEGUINTES (`pytest`, "Sobe a API em segundo plano") herdam a role de runtime. `PONTO_TEST_DATABASE_URL` (usada pelos ~21 `conftest.py` de banco exclusivo por fase, F2-F14, para `CREATE DATABASE`/`CREATE ROLE` das próprias fixtures) continua superusuário de propósito — não faz sentido revogar privilégio de quem precisa administrar o próprio banco de teste. Validado: as duas jobs voltaram 100% verdes na primeira tentativa, incluindo `contrato-schemathesis` (a API real inteira, não só os testes, agora atende todo o fuzzing sob a role sem `BYPASSRLS` — o caminho mais próximo de produção que o CI já testou). Nenhum teste dependia implicitamente do bypass de superusuário. | Nenhuma — já corrigido e validado. Fecha a lacuna que deixou o bug de RLS de `executar_relatorio` (entrada acima) invisível em CI; qualquer bug futuro dessa classe agora tem chance real de aparecer antes de chegar em produção. |
| 2026-08-07 | Orquestrador (fecha parcialmente o débito "idempotência genérica ainda falta em ~120 rotas") | `ROTAS_COM_DEDUP_REAL` (`app/comum/idempotencia_middleware.py`, F14/A2) tinha só 10 rotas com dedup real de ~120 rotas de escrita de F1-F12 (o resto só valida presença do cabeçalho `Idempotency-Key`, sem deduplicar de verdade). Enumeradas as 121 rotas de escrita reais via `app.openapi()` (não `app.routes` direto — mesmo achado já registrado em 30/07 sobre `_IncludedRouter`); de 121, 26 candidatas seguras identificadas e adicionadas (25 novas + confirmação da existente `/v1/afastamentos`): critério idêntico ao original (POST simples, path sem parâmetro, corpo JSON pequeno, sem upload), confirmando primeiro que TODA rota candidata já exige `Idempotency-Key` no próprio router (contrato inalterado por esta expansão). Excluídas deliberadamente, com justificativa registrada no próprio módulo: rotas de autenticação/sessão/SSO (login, logout, MFA, refresh, recuperação de senha, emissão de token OAuth, SAML ACS — semântica de "sempre gera algo novo", não de criação deduplicável); geração de arquivo fiscal (`fiscal/aej`, `fiscal/afd`, `fiscal/rep-ps`, `.../assinar` — crítico de conformidade legal, fora do escopo de um retrofit mecânico); rotas de importação/sincronização em lote (`colaboradores/importar`, `marcacoes/sincronizar-offline`, `importacoes` — corpo potencialmente grande); ações que não são criação de recurso (`apuracoes/recalcular`, `banco-horas/simular` — disparo de processamento); `espelhos` (geração de documento, potencialmente pesada); rotas que já têm idempotência própria (`marcacoes.py`/F5, `webhooks.py`/F13); `tenants` (ainda `501`); e todo PUT/PATCH/DELETE (já idempotentes por semântica HTTP, ganho marginal menor, mistura de métodos desviaria do critério original). Cobertura de teste: um novo teste HTTP e2e real (`POST /v1/feriado-conjuntos`, escolhida por não depender de nenhum recurso pré-existente, ao contrário da maioria das novas rotas que exigem empresa/estrutura organizacional já semeada) prova replay real (linha única na tabela após duas chamadas com a mesma chave); um teste de regressão barato confere que as 25 novas entradas continuam na allowlist (protege contra remoção acidental numa edição futura). | Nenhuma ação de código pendente — aguardando validação do CI real. ~85 rotas de escrita ainda restam só com validação de cabeçalho (a maioria com FK obrigatória — empresa/colaborador/vínculo pré-existente —, o que exigiria dados de setup mais elaborados para qualquer teste e2e novo, e algumas delas merecem análise caso a caso antes de entrar na allowlist, ex. `recalcularApuracoes`/`simularBancoHoras`/`gerarEspelhos`, cuja semântica de "criação" é menos óbvia). |
| 2026-08-07 | Orquestrador (fecha o débito "gap sistêmico de contrato... nem toda rota declara 404 PONTO-TEN-001", já registrado em 03/08) | `TenantMiddleware` (`app/core/middleware.py`) pode devolver `404 PONTO-TEN-001` para QUALQUER caminho `/v1/...` (confirmado lendo o código: roda antes de qualquer rota, sem exceção por operação), mas de 229 operações do contrato só 5 declaravam esse `404` em `responses`/`x-erros` — gap real de completude, não bug de comportamento (era exatamente o motivo já documentado para excluir `status_code_conformance` do `contrato-schemathesis`). Corrigidas as 224 operações restantes: `responses.404` (referenciando `#/components/responses/Erro404`, componente que já existia e já documentava `PONTO-TEN-001` como exemplo — reaproveitado, não recriado) + `PONTO-TEN-001` em `x-erros`. **Achado de ferramental durante a correção**: a primeira tentativa usou `ruamel.yaml` fazendo `load()`+mutação+`dump()` do documento inteiro — apesar de `preserve_quotes=True`, o dumper reflui (desdobra) toda string multi-linha dobrada (`description:` longas), gerando um diff de ~5.500 linhas tocando conteúdo não relacionado a esta mudança. Abortado e refeito com um script que usa `ruamel.yaml` só para **localizar** (via metadado `.lc.line` de round-trip) a linha exata de cada inserção, e escreve direto na lista de linhas ORIGINAL do arquivo (texto puro) — diff final de 408 inserções/224 remoções, exatamente proporcional às 224 operações tocadas, zero reformatação de conteúdo não tocado. Validado: `yaml.safe_load` (229 paths), `spectral lint` com o mesmo ruleset de fallback do CI (`spectral:oas`, já que este repo não tem `.spectral.yaml` próprio) — 0 achados; contagem de `'404':` no arquivo foi de 137 para 229 (bate exatamente com o total de operações). `contrato-schemathesis` (`ci.yml`) teve `status_code_conformance` removido de `--exclude-checks` -- volta a ficar ativo, já que o gap que motivava a exclusão foi fechado de verdade. | Nenhuma ação de código pendente — aguardando validação do CI real (schemathesis com `status_code_conformance` ativo é o teste de verdade desta correção). Se alguma operação nova no futuro não herdar o padrão `responses.404`/`x-erros`, o check volta a reprovar — comentário no `ci.yml` já orienta corrigir o contrato, não reexcluir o check. |
| 2026-08-07 | Orquestrador (validação real da entrada acima — `status_code_conformance` reativado) | Rerun com `status_code_conformance` ativo: de ~92 falhas originais (03/08) para 2. `GET /v1/tenants/atual` devolvia `400 PONTO-VAL-011` (sem tenant resolvido, `SessaoDb`/`obter_sessao` recusa) mas o contrato só declarava 200/403/404/429/500/501 — gap real, corrigido (`400: Erro400` + `PONTO-VAL-011` em `x-erros`). `GET /v1/admin/saude` devolvia `400` com corpo TEXTO PURO ("Invalid HTTP request received.") para um `X-Request-Id` fuzzado com byte de controle — mesma causa raiz que já justifica excluir `content_type_conformance` (parser HTTP do ASGI recusa a requisição antes de qualquer código da aplicação rodar), só que capturada por este check via ângulo diferente (status não documentado, não content-type). Não dá pra documentar como `400: Erro400` normal porque o corpo observado nem é JSON (violaria `response_schema_conformance` em seguida) — excluída via `--exclude-operation-id obterSaude` (única operação onde isto se manifestou; as outras 228 passaram 100%). | Nenhuma — ambos corrigidos/excluídos com justificativa. `contrato-schemathesis` agora roda com `status_code_conformance` ativo cobrindo ~228 das 229 operações do contrato — a maior cobertura real que este check já teve no projeto. |
| 2026-08-07 | Orquestrador (**bug real de produção**, fecha o ADR-011 — achado maior do que o débito original escopava) | Investigando o débito do ADR-011 (`Tratamento` retroativo de categoria `afastamento` sem efeito em `apuracoes_dia`), a leitura de `calcular_dia`/`apurar_dia` (F4) revelou que o problema é maior: `tipo_dia == 'afastamento'` **nunca** teve efeito numérico em lugar nenhum do pipeline — nem no caso retroativo (o que o ADR-011 já sabia) nem no caso "de fábrica" (afastamento futuro/vigente, resolvido por `_afastamento_vigente`/F3 via a tabela `afastamentos`, existente desde a Fase 0, que o ADR-011 presumia que já funcionava). A fórmula `falta_minutos = max(0, previsto - trabalhado - abono)` roda igual para todo `tipo_dia`, e nada preenchia `abono_minutos` para dia de afastamento — nenhum teste de F3 nem F4 mencionava "afastamento", o caminho nunca foi exercitado de ponta a ponta. **Impacto real**: qualquer colaborador com afastamento aprovado (férias, licença médica programada etc.) apareceria com falta não abonada na apuração, espelho de ponto e relatórios — desde a Fase 0. Corrigido em `apurar_dia`/`_carregar_marcacoes_e_tratamentos` (`app/apuracao/dominio/servico.py`): dia resolvido como afastamento (por F3 OU por `Tratamento` retroativo aprovado/aplicado) passa a abonar o dia inteiro e gravar `tipo_dia='afastamento'`, reaproveitando o valor já existente no `CHECK` de `apuracoes_dia.tipo_dia` (sem migration nova) — `calcular_dia` (função pura, 17 critérios de F4 já verificados) não foi tocada, a mudança fica só na orquestração. Testes: `tests/f10/afastamentos_workflow/test_abono_e_retroativo.py` estendido com asserções de `falta_minutos`/`tipo_dia`/`abono_minutos` (não substituído, como o próprio ADR-011 já orientava) + teste novo cobrindo o caminho "de fábrica" via `Afastamento` direto. F12 (AFD/AEJ) confirmada sem dependência de distinção `afastamento` vs `abono` (motivo original do adiamento) — `apps/api/app/fiscal/afd/*.py` não referencia `tipo_dia` em lugar nenhum. | **Produção**: auditar se algum colaborador com afastamento aprovado desde a Fase 0 tem falta não abonada acumulada em `apuracoes_dia`/relatórios/espelhos gerados — a correção vale para apurações NOVAS/reprocessadas a partir de agora, não corrige retroativamente dados já materializados (precisaria de um `recalcular_periodo` em lote sobre o histórico, decisão separada). Mitigação operacional do ADR-011 ("RH deve usar `abono_falta`") deixa de ser necessária. |
| 2026-08-08 | Orquestrador (ADR-010, diagnóstico real via agente dedicado — reduz mas não fecha o débito) | Instrumentado (contagem de statements SQL + cronômetro por fase, 3.100 apurações reais na VPS, sem túnel): 25,45 queries/apuração, `resolver_jornada_do_dia` (F3) é o maior bloco isolado (34,3%, 8,74 queries/dia relidas do zero mesmo quando a jornada vigente não muda) mas NÃO é dominante — é um N+1 uniforme espalhado por vários módulos (`gravar_auditoria` ~15%, `_sincronizar_ocorrencia` ~9%). Implementado `CacheResolucao` (`app/jornada/resolvedor/servico.py`) — cache opcional, somente-leitura, de vida curta (uma chamada de `recalcular_periodo`), via parâmetro NOMEADO opcional acrescentado a `resolver_jornada_do_dia`/`apurar_dia` (assinatura fixada do PCF intacta, ausente o cache o comportamento é idêntico ao de antes). Medido (mesmo volume, 5 pares antes/depois pra cancelar ruído de carga da VPS): mediana 169,6s → 116,9s (1,45×); métrica sem ruído (statements SQL): 25,45 → 14,36 queries/apuração (−43,6%). Extrapolação pra 10.000×31: 283min → 195min (meta < 5min — continua ~39× acima). 17 critérios de F4 reverificados: 16/17 verdes (só o critério 4, performance, continua não atendido, como esperado). Detalhes completos em `docs/adr/ADR-010-debito-tecnico-performance-recalculo-em-lote.md` ("Atualização — 08/08/2026"). | Débito parcialmente reduzido, não fechado — critério 4 do PCF F4 continua não atendido. Direções maiores registradas no ADR (memoizar auditoria por tenant/transação, paralelizar vínculos independentes, processar em lote entre dias) ficam para uma rodada dedicada futura, decisão do dono do produto. |
| 2026-08-08 | Orquestrador (achado de passagem durante ADR-010, não corrigido — fora do escopo daquela rodada) | `_upsert_apuracao_dia` (`app/apuracao/dominio/servico.py`) pode devolver um objeto ORM desatualizado da identity map da sessão: o `INSERT ... ON CONFLICT ... DO UPDATE` é Core/DML, e o `SELECT` seguinte que re-lê a linha devolve a instância JÁ existente na identity map sem reler as colunas — num segundo `apurar_dia` para o MESMO `(vínculo, dia)` na MESMA sessão, campos como `linha.versao` podem ficar com o valor da primeira carga. Já causou uma regressão real (corrigida) nos testes de `tests/f10/afastamentos_workflow` durante o desenvolvimento da otimização acima — resolvido ali usando `RETURNING` em vez de reler o objeto, mas o mesmo risco existe em qualquer outro ponto que releia `ApuracaoDiaOrm` na mesma sessão após um upsert anterior (ex. `_montar_schema_resposta`, se chamado depois de múltiplos `apurar_dia` na mesma sessão para o mesmo dia). | Fix sugerido, não aplicado: `.execution_options(populate_existing=True)` no `SELECT` de re-leitura em `_upsert_apuracao_dia`. Não aplicado agora porque muda comportamento observável e estava fora do escopo "só otimizar o caminho de execução" da rodada do ADR-010. |
| 2026-08-08 | Orquestrador (retrofit completo, decisão explícita do dono do produto de reabrir o item recusado em 2026-08-05 — "F14 / A2 (T \"decisão core/seguranca.py\")") | OAuth/API-key passou a ser aceito nas ~130 rotas humanas de F1-F12 que só reconheciam sessão JWT, mesmo já declarando `bearerAuth`/`oauth2`/`apiKeyAuth` no contrato desde F13. Mecanismo: `exigir_permissao_ou_escopo(permissao, escopo)` movido de `app.integracoes.webhooks.seguranca` (F13, único consumidor até então) para `app.comum.autenticacao_cliente` (módulo genérico correto, conforme a própria recomendação registrada em 2026-08-05) — tenta sessão humana via `exigir_permissao` primeiro (comportamento humano preservado byte a byte, inclusive `perfis_somente_leitura` e `acessos_dados_sensiveis`), cai para `exigir_escopo` (cliente OAuth/API-key) só na ausência de sessão humana. Retrofit aplicado rota por rota (não em lote cego, por exigência explícita do dono do produto) em 27 arquivos de router (`empresas`, `admin`, `colaboradores`, `contratos`+`vinculos`, `organizacao`, `unidades`, `jornadas`, `escalas`, `feriados`, `dispositivos`, `terminais`, `biometria`, `apuracoes`, `banco_horas`, `tratamentos`, `afastamentos`, `solicitacoes`, `aprovacoes`, `marcacoes`, `comprovantes`, `fechamentos`, `espelhos`, `relatorios`, `fiscal`, `lgpd`, `auditoria`, `tenants`), ~215 operações, via 5 agentes paralelos + revisão consolidada do orquestrador — permissão/escopo de cada operação lida diretamente do `x-permissao`/`x-escopo` do contrato (não seguem convenção 1:1 por tag, ex. `terminais`/`dispositivos` compartilham escopo `dispositivos:*`), nunca inventados. Padrão mecânico uniforme: `response: Response` injetado, `await aplicar_limite_taxa_se_cliente(response, acesso)` na primeira linha do corpo, `acesso.tenant_id`/`usuario_id_do_acesso(acesso)` no lugar de `sujeito.tenant_id`/`sujeito.usuario_id`, DELETE-204 reaproveitando o `response` injetado (nunca `Response()` novo, preservaria os cabeçalhos `RateLimit-*`). Casos especiais decididos com investigação, não convenção cega: `GET /v1/tenants/atual` deixada como estava (sem `x-permissao` no contrato — autoconsulta, qualquer sujeito autenticado); `listarTenants`/`criarTenant` não tocadas (sempre `501` incondicional, mesmo sem `Depends`, adicionar o portão trocaria `501` por `401` sem ganho); `POST /v1/admin/api-clients` mantida com a dedup própria já existente (`abrir_operacao`/`concluir_operacao`); handlers que repassavam `Sujeito` inteiro para a camada de serviço (5 em `admin.py`, 2 em `comprovantes.py`/`tenants.py`, os de `marcacoes.py`) ganharam um adaptador local (`_sujeito_para_servico`) que devolve o `Sujeito` real em acesso humano e um `Sujeito` sintético só com `tenant_id` (sem `usuario_id`, sem `alcance` — "conta de máquina" já documentada em `app.core.seguranca.Sujeito`) em acesso de cliente — zero migration, todo campo de auditoria já é `UUID` nullable no schema. **Achado operacional durante a execução paralela**: um dos 5 agentes rodou `git stash` no worktree compartilhado achando que era um scratch isolado, varrendo o trabalho não commitado dos outros 4 em andamento — recuperado por completo via `git checkout stash@{0} -- <arquivo>` seletivo (verificado depois, arquivo por arquivo, que nada do resultado final ficou faltando); stash descartado só após validação final. **Regressões de teste reais encontradas e corrigidas** (3, todas de acoplamento de teste à assinatura antiga do handler, nunca de comportamento): `tests/f5/consulta/test_comprovantes_consulta.py` e `tests/f14/antifraude/test_router_revisao.py` chamavam handlers de router diretamente com `sujeito=` (assinatura pré-retrofit) — corrigidos para `acesso=ContextoAcesso(...)`/`response=Response()`; `tests/f10/test_permissoes_rotas.py` + as 3 cópias em `tests/f12/{afd,rep_p,assinatura}/test_permissoes_rotas.py` faziam introspecção estática de `Depends` procurando o `__qualname__` exato de `exigir_permissao.<locals>._verificar` — como o combinador chama `exigir_permissao` em runtime dentro do corpo de `_resolver`, não como sub-dependência declarada, o teste não achava nada; corrigido ensinando `_codigo_de_exigir_permissao` a reconhecer também `exigir_permissao_ou_escopo.<locals>._resolver`, lendo a freevar `permissao` do closure. Validação: cada um dos 5 grupos rodou sua fatia de testes correspondente contra Postgres real da VPS (via clone fresco + venv, GitHub Actions seguia bloqueado por billing) comparando com baseline do `origin/main`; rodada final consolidada de toda a suíte (`pytest apps/api/tests`) confirmou os únicos problemas restantes como pré-existentes e não relacionados (MinIO ausente, módulo `worker` ausente, certificado de teste ausente, `PONTO_SANDBOX_ADMIN_SENHA` ausente, `onelogin`/SAML deprecation warnings, poluição de dados entre execuções repetidas no mesmo Postgres persistente, e o já documentado "password authentication failed" de `tests/f1/autenticacao`, entrada de 2026-08-07). `ruff check`/`ruff format` limpos em todos os 35 arquivos tocados; `mypy` não pôde ser validado nesta rodada (venv ad-hoc da VPS instalou `packages/` em modo editável, que o próprio `ci.yml` já documenta como incompatível com `mypy` — pré-existente ao retrofit, reproduz em arquivos não tocados por esta mudança). | Nenhuma ação de código pendente — aguardando validação final via GitHub Actions real (self-hosted runner, deploy incluso) assim que o bloqueio de billing (entrada de 2026-08-08, task #69) for resolvido pelo usuário. Gap conhecido, não fechado por decisão explícita: `criarVinculo`/demais escritas de `vinculos` ficaram sem o retrofit até o orquestrador notar a omissão na tabela de referência do agente do Grupo A e completar manualmente — vale conferir se algum outro operationId do contrato ficou de fora por lacuna semelhante na extração da tabela (não reauditado ponta a ponta após a correção). |
| 2026-08-08 | Orquestrador (segunda passada sobre "idempotência genérica ainda falta", fecha a mecânica, expõe a lacuna estrutural real) | Reexaminadas as ~85 rotas de escrita ainda fora de `ROTAS_COM_DEDUP_REAL` (121 rotas totais − 36 já cobertas). Achado: `POST /v1/admin/api-clients` (`criarApiClient`) já tem dedup real PRÓPRIA — chama `abrir_operacao`/`concluir_operacao` direto no corpo do handler, mesmo padrão de `webhooks.py`/F13 — não precisava (nem devia) entrar na allowlist. `POST /v1/auditoria/verificar-cadeia` é verificação sobre dado imutável (hash chain), sem efeito colateral — rodar duas vezes já produz o mesmo resultado por natureza, dedup não agrega valor. Só `POST /v1/espelhos` (`gerarEspelhos`, F10 — `202`, corpo pequeno `ProcessamentoAssincrono`, já exige `Idempotency-Key`) era candidato genuíno novo, adicionado (10 → 36 rotas com dedup real). **A lacuna estrutural real, não fechável mecanicamente**: das ~84 rotas restantes, a esmagadora maioria (`PATCH`/`DELETE`-por-id, ações como `.../decidir`, `.../cancelar`, `.../reenviar`) tem parâmetro de path — `IdempotenciaRetrofitMiddleware` só casa `(método, caminho)` EXATO, de propósito (O(1), sem regex, sem risco de casar caminho errado). Cobrir essas rotas exigiria estender o próprio middleware pra casar padrão de caminho (`/v1/tratamentos/{id}/decidir`), não só adicionar entradas na lista — mudança de arquitetura no mecanismo compartilhado por todas as 36 rotas já cobertas, com risco de regressão maior do que uma expansão mecânica de lista. | Decisão pendente com o dono do produto: se vale estender `IdempotenciaRetrofitMiddleware` para casar padrão de caminho (path template), habilitando dedup real em rotas `.../{id}/ação` — ou se o ganho não justifica o risco de mexer no mecanismo compartilhado, e o débito fica como está (validação de cabeçalho sem dedup real) até uma fase dedicada decidir. |
| 2026-08-08 | Orquestrador (terceira passada sobre "idempotência genérica ainda falta" — fecha a lacuna ESTRUTURAL exposta na segunda passada, decisão do dono do produto de estender o mecanismo) | `IdempotenciaRetrofitMiddleware` (`app/comum/idempotencia_middleware.py`) passou a casar TEMPLATE de caminho, não só `(método, caminho)` exato — o bloqueio registrado na entrada anterior de 2026-08-08. Mecanismo: `ROTAS_COM_DEDUP_REAL_TEMPLATE` (lista nova, ao lado da lista exata que ficou intacta) é compilada UMA vez no import numa árvore de prefixos de SEGMENTOS (`método -> segmento -> ... -> marca de fim`), e `_casa_template` desce essa árvore um segmento por vez tratando `{...}` como curinga de exatamente um segmento: O(profundidade do caminho) por requisição, sem regex (nada de ReDoS nem varredura O(rotas x caracteres)) e sem backtracking — o construtor recusa no import (`ValueError`) um conjunto de templates que misture filho literal e filho curinga no mesmo nível, que é o único caso em que a busca sem backtracking poderia dar falso negativo. `tem_dedup_real(método, caminho)` virou a única porta de entrada da decisão (exato primeiro, `frozenset` O(1); template depois). O escopo gravado continua sendo o caminho CONCRETO (`POST:/v1/tratamentos/<uuid-real>/decidir`), nunca o template — dedup por recurso, nunca entre recursos distintos do mesmo template. Segunda passada de enumeração via `app.openapi()` (não `app.routes`): das 85 rotas de escrita ainda fora da allowlist, 51 entraram (36 -> 87 de 121 rotas de escrita com dedup real), avaliadas uma a uma — `PATCH`/`DELETE`-por-id de cadastro (F1/F2/F3/F4/F6/F9b/F14), ações de decisão/transição de estado (`aprovacoes/{id}/decidir`, `tratamentos/{id}/decidir`, `solicitacoes/{id}/cancelar`, `fechamentos/{id}/conferir|reabrir`, `vinculos/{id}/encerrar`, `biometrias/{id}/validar`, `dispositivos/{id}/vincular`, `marcacoes/{id}/meta/decisao`), criação de subrecurso (`equipes/{id}/membros`, `escalas/{id}/atribuicoes`, `unidades/{id}/redes-permitidas`, `vinculos/{id}/jornadas`) e as duas rotas de tenant realmente implementadas em sessão comum (`PATCH /v1/tenants/{id}`, `PUT /v1/tenants/{id}/configuracoes/{chave}`). O ganho aqui NÃO é a idempotência de estado que `PATCH`/`DELETE` já têm por semântica HTTP e sim a dedup de EFEITOS COLATERAIS de reexecutar o handler (linha de auditoria, notificação, evento, recálculo) — e nas ações de decisão nem a semântica de estado é idempotente. 34 exclusões deliberadas, todas justificadas na docstring do próprio módulo: autenticação/sessão/SSO (11, incluindo `PUT admin/sso/provedores`, por precaução: replay de configuração de SSO pode deixar um tenant sem conseguir autenticar, e `POST sso/saml/acs`, que sequer declara `Idempotency-Key`); geração/assinatura de arquivo fiscal e documento legal (5, agora explicitamente incluindo `espelhos/{id}/assinar`); processamento assíncrono pesado ou verificação sobre dado imutável (5: `apuracoes/recalcular`, `banco-horas/simular`, `auditoria/verificar-cadeia`, `terminais/{id}/sincronizar`, `integracoes/folha/{id}/exportar`); importação/sincronização em lote (3, corpo grande demais para o middleware hashear em memória); rotas que JÁ têm dedup própria no corpo do handler, estritamente melhor que a do middleware por rodar na mesma transação da lógica de negócio (8: `marcacoes` POST/F5, os 4 de `webhooks`/F13, os 3 de `admin/api-clients`/F13); `PUT relatorios/preferencias-colunas` (não declara `Idempotency-Key` no contrato — adicionar dedup recusaria com `PONTO-IDEM-001` uma chamada hoje válida, quebra de contrato); e `POST /v1/tenants` (roda em `SessaoDbSuporte`/`BYPASSRLS`, transação diferente da que o middleware usa). Validação: `ruff check`/`ruff format` limpos; 3 testes novos de unidade do casamento (casa concreto, recusa vizinho/método errado/curinga vazando para caminho mais curto ou mais longo, e regressão que instancia TODO template da lista) + 2 testes HTTP e2e reais contra rota COM parâmetro de path em `tests/f14/hardening/test_idempotencia_middleware_template_http_e2e.py` — o de `PATCH /v1/feriado-conjuntos/{id}` prova dedup de EXECUÇÃO alterando a linha POR FORA da API entre as duas chamadas (se o handler reexecutasse, sobrescreveria a alteração externa; ele não reexecuta), e o de `DELETE` prova que a 2ª chamada com a MESMA chave dá `204` de replay enquanto uma chave DIFERENTE dá `404` (o recurso realmente foi excluído). Controle negativo rodado de propósito: com o índice de templates zerado, os 2 testes e2e falham — a prova não é vácua. Suíte COMPLETA (`pytest apps/api/tests`) rodada DUAS vezes contra Postgres real da VPS, em bancos limpos e separados (sem/com a mudança): lista de falhas byte a byte idêntica, 76 em ambas, todas pré-existentes de ambiente (MinIO `AccessDenied`, Redis `AuthenticationError`, `worker`/arq ausente) — zero regressão. Nenhum router tocado. | Nenhuma ação de código pendente. Restam 34 rotas de escrita só com validação de cabeçalho, todas por exclusão deliberada documentada (não por limitação do mecanismo). Achado de passagem, NÃO corrigido: `POST /v1/integracoes/folha` está ao mesmo tempo em `ROTAS_COM_DEDUP_REAL` e com dedup própria no handler (escopos diferentes — `POST:/v1/integracoes/folha` vs `integracoesFolha.criar` —, então não há deadlock de advisory lock, só duas linhas em `idempotencia_chaves` e a dedup do handler nunca chegando a rodar no replay, porque o middleware curto-circuita antes); vale decidir qual das duas fica. |
| 2026-08-08 | Orquestrador (motor facial real — decisão do dono do produto de integrar modelo aberto self-hospedado em vez de treinar do zero ou usar SaaS de terceiro) | `apps/facial-svc` saiu de 100% stub (`enroll`/`verificar`/`liveness` só logavam e devolviam resposta fake) para um pipeline real via InsightFace `buffalo_l` sobre ONNX Runtime CPU (detecção `det_10g.onnx` + reconhecimento `w600k_r50.onnx`, embedding ArcFace 512-d) — só os dois módulos necessários carregados, `genderage`/`2d106det`/`1k3d68` deliberadamente fora (dado pessoal sem uso em decisão de ponto, LGPD). Novo subpacote `facial/motor/` isola todo o código que carrega peso ONNX. `/enroll` extrai e devolve o template (base64 de 512 floats), stateless — persistência fica em `apps/api`, que já tem a tabela de biometria. `/verificar` compara por cosseno contra `templates[]` recebidos, melhor-de-N, com `exigirAprovacao` alternando entre `403 PONTO-SCORE-003` (fluxo de ponto) e `200 {aprovado:false}` (lote). `/liveness` usa heurística multi-quadro (moiré para tela, textura para foto impressa, congelamento de quadro) — **documentado como não-normativo**: não é modelo treinado (pesos MiniFASNet em ONNX que circulam publicamente são espelhos de terceiros sem cadeia de custódia confiável, risco de supply chain pior que o problema que resolveriam), então o liveness hoje é sinal de confiança (ADR-008), nunca portão único. Validado com a foto de grupo de 6 pessoas que vem dentro do próprio pacote `insightface` (MIT, nenhuma imagem entra no repo): cosseno 0,931–0,977 mesma pessoa, ≤0,211 pessoas diferentes, folga de 0,720 no limiar 0,42; sem-rosto e 2+ rostos recusados; simulação de tela (moiré) e foto impressa (textura) reprovadas; quadro congelado reprovado, sequência com movimento aprovada. 32 testes, `ruff`/`mypy --strict` limpos, latência `/enroll` ~0,7s p50/0,88s p90 (VPS 8 vCPU sem GPU). Achado corrigido de passagem: `FACIAL_MODEL_VERSAO` em `infra/docker-compose.yml` dizia `arcface-r100-v1` mas o reconhecedor carregado é ResNet50 do `buffalo_l` — carimbo de rastreabilidade que mentia, corrigido para `buffalo_l-w600k_r50-v1` (esse valor grava em `biometria_templates.versao_modelo`). Modelos ONNX (~191 MB) baixados só em dev/CI (`FACIAL_BAIXAR_MODELO=1`); `hml`/`prd` recusam essa flag na validação da `Configuracao` — pesos entram via volume, nunca em build de imagem nem download em runtime de produção. | **NENHUMA rota de `apps/api`/`device-gw` foi conectada a este motor** — é infraestrutura provada isoladamente, sem caller real ainda (mesma lacuna já registrada em 05/08 sobre mTLS device-gw↔facial-svc). Próximo passo natural: ligar `POST /v1/biometrias` (enroll) e o fluxo de marcação com biometria (verificar) a este serviço via `cliente_facial.py`/mTLS já provado. Também caberia decisão do dono do produto sobre substituir a heurística de liveness por um modelo treinado de procedência confiável antes de qualquer uso que dependa dela como portão único (hoje é só sinal). |
| 2026-08-08 | Orquestrador (auditoria/backfill de dados históricos afetados pelo bug do ADR-011, já corrigido no código em 07/08) | Nova ferramenta `apps/api/tools/auditar_afastamento_adr011.py`: identifica linhas de `apuracoes_dia` com `falta_minutos > 0` que deveriam ter sido abonadas por afastamento (união de três critérios: `tipo_dia='afastamento'` já gravado; `Afastamento` aprovado cobrindo a data pelo `colaborador_id`, mesmo predicado de `_afastamento_vigente`; `Tratamento` de categoria afastamento aprovado/aplicado no vínculo/data). `--dry-run` (padrão) só relata, por tenant/período/colaborador; `--aplicar --tenant <id>` agrupa os dias em blocos contíguos por vínculo e chama `recalcular_periodo` (reaproveitando o `CacheResolucao` do ADR-010), uma transação por tenant, com log antes/depois de `falta_minutos`/`abono_minutos`/`tipo_dia`. Rodado em `--dry-run` real contra TODOS os 61 bancos disponíveis na VPS (incluindo `ponto`, 2M+ linhas de apuração) e o Postgres de hml: **zero linhas afetadas em todo lugar hoje** — nenhum banco disponível tem `afastamentos`/`tratamentos` de categoria afastamento semeados ainda, então o bug nunca se manifestou nos dados de teste existentes. O mecanismo em si foi provado funcionando num banco de rascunho isolado (criado e destruído só para o teste): 3 linhas sujas detectadas e corrigidas corretamente, dado limpo intocado. 106 testes existentes de `f10/afastamentos_workflow` + `f4/tratamento` sem regressão, 4 testes novos cobrindo a ferramenta. **Achado importante não corrigido** (fora do escopo desta tarefa): confirmado contra a VPS que `recalcular_periodo` corrige o dado de verdade (`falta_minutos`/`versao` mudam) mas **não grava a linha de auditoria correspondente** — é o bug já registrado em `docs/backlog.md` (08/08, `_upsert_apuracao_dia` devolvendo instância obsoleta da identity map), agora confirmado também pelo lado da auditoria, não só da `versao`. A ferramenta contorna lendo antes/depois via SQL cru e logando ela mesma. | Ferramenta pronta para uso assim que houver dado real de afastamento em produção para auditar. **Recomendação forte: corrigir o bug de `_upsert_apuracao_dia` (fix sugerido já registrado, `.execution_options(populate_existing=True)`) antes de rodar `--aplicar` em qualquer banco com dado real** — sem isso, uma correção retroativa de verdade fica sem rastro na cadeia de auditoria, o que é inaceitável para um sistema que depende de auditoria imutável para conformidade. |
| 2026-08-08 | Orquestrador (ADR-010, terceira rodada de otimização — reduz mas não fecha o débito, mesmo padrão das duas rodadas anteriores) | Acumulado desde o diagnóstico original: 25,45 → 9,63 queries/apuração (−62%). Instrumentação corrigida nesta rodada (o listener de eventos do SQLAlchemy não alcançava os frames do chamador sob o driver async — trocado por wrappers por função, contagem exata em vez de estimativa por tempo). Três otimizações aplicadas: (1) `CacheCadeiaAuditoria` — depois da primeira gravação numa transação, os 3 round-trips de releitura da cadeia de hash não trazem informação nova enquanto o advisory lock da transação estiver seguro (ninguém mais escreve nesse meio-tempo); cache invalidado automaticamente por identidade de `SessionTransaction` (commit/rollback/savepoint). 14,36 → 11,36 (−20,9%). (2) Batch de `INSERT` Core multi-linha para ocorrências (a primeira tentativa, adiar `flush` esperando `executemany` agrupar, FALHOU — parâmetros diferentes + `RETURNING` de PK impedem `insertmanyvalues` do SQLAlchemy; documentado como tentativa sem sucesso). 11,36 → 10,62 (−6,5%). (3) Cache de `verificar_periodo_aberto` (achado durante o profiling: leitura pura refeita 3.100 vezes para 31 respostas distintas no lote medido). 10,62 → 9,63 (−9,3%). **Achado arquitetural mais importante da rodada**: paralelizar vínculos foi implementado, medido e REVERTIDO — 1,5× MAIS LENTO (98s → 150s), porque `gravar_auditoria` trava a cadeia por tenant com `pg_advisory_xact_lock` escopado à transação inteira; sessões paralelas ficam simplesmente esperando a que segura o lock. Consequência que se estende além desta rodada: o fan-out de jobs ARQ que `enfileirar_recalculo` já faz hoje (um job por vínculo) cai no MESMO muro — escalar workers horizontalmente não acelera o recálculo de um tenant, porque todos competem pelo mesmo lock de auditoria. Extrapolação para 10.000×31: ~207min → ~163min. **Meta de <5min continua não atendida, ~33× acima.** Zero regressão: mesma lista de falhas (todas ambientais) antes/depois em `f1`+`f4`+`f10`; suítes que tocam o caminho alterado com 210 passed / 1 pré-existente. `calcular_dia` e assinaturas públicas intactas. Atualização registrada no próprio `docs/adr/ADR-010-debito-tecnico-performance-recalculo-em-lote.md`. | Pré-requisito registrado no ADR para destravar paralelismo de verdade: acumular auditoria em memória durante o lote e gravar num único `INSERT` no fim da transação, tomando o `pg_advisory_xact_lock` só nos últimos milissegundos — mexe em módulo de F1 (`app/identidade/auditoria`), é decisão de investimento que precisa do dono do produto. Sem isso, nem mais paralelismo nem mais workers ARQ resolvem o gap de 33×. |
| 2026-08-08 | Orquestrador (ADR-012, resíduo de implementação — decisão do dono do produto de tentar fechar o débito nesta rodada) | O ADR-012 tinha duas coisas dentro: (1) lacuna NORMATIVA (formato de concatenação dos 8 insumos do hash do registro tipo 7 e o valor do 8º insumo ausente — depende de AFD de fabricante homologado ou resposta da SIT/MTE, **não fechável por código**, continua aberta) e (2) um resíduo de implementação real, esse sim corrigido: `app/fiscal/afd/tipo7.py::montar_registros_tipo7` aplicava duas semânticas de cadeia diferentes no mesmo arquivo — o primeiro registro de cada geração encadeava pela sequência REAL do REP-P, mas do segundo em diante encadeava pelo registro ANTERIOR NA LISTA IMPRESSA daquele AFD específico. Consequência real: um NSR intercalado excluído do recorte de período (cenário real, não hipotético — NSR não é cronológico por desenho/ADR-003, marcação offline recebe NSR na gravação mas entra no AFD pela `datahora_marcacao`) fazia o MESMO NSR receber hashes DIFERENTES conforme o período escolhido na geração — quebra a única propriedade pela qual a cadeia existe (reprodutibilidade/auditabilidade). Corrigido: a cadeia agora sempre percorre do NSR 1 até o maior NSR pedido dentro do REP-P inteiro, só a impressão é filtrada pelo período. 4 testes novos provam a correção (revertendo só `tipo7.py`, exatamente esses 4 falham). `tests/f12/afd` (86) e `tests/f12` completo (223, incluindo AEJ/e2e) passam com as credenciais corretas de MinIO/Redis configuradas (achado de ambiente: as credenciais REAIS dos containers de teste da VPS não são as que sessões anteriores vinham tentando — com elas certas, F12 fica 100% verde, não há falha pré-existente real nesta suíte). Nenhuma migration necessária — a cadeia é recalculada deterministicamente de `marcacoes`, sem coluna nova (persistir o hash agora seria persistir um cálculo de fórmula ainda não confirmada). Adendo datado adicionado ao próprio `docs/adr/ADR-012-formula-hash-registro-tipo-7-afd-nao-confirmada.md`. | **ADR-012 continua ABERTO** — a marcação de "não usar para homologação/fiscalização real" permanece válida até a fórmula ser confirmada externamente. O que mudou é que a cadeia é internamente consistente agora: quando a fórmula for confirmada, a correção fica isolada a um módulo (`hash_tipo7.py`) e tudo pode ser recalculado sem tocar em `tipo7.py` de novo. |
| 2026-08-08 | Orquestrador (acesso de suporte SEEG multi-tenant, decisão arquitetural do orquestrador — escape hatch de RLS deliberado e estreito) | `GET/POST /v1/tenants` saíram de `501` incondicional para implementação real, via role Postgres NOVA e ISOLADA `ponto_app_suporte` (migration `0005_role_suporte_bypassrls`, reversível — upgrade→downgrade→upgrade testado): `LOGIN BYPASSRLS`, mas **sem** membership em `ponto_app` (confirmado: só `SELECT,INSERT` em `tenants`+`auditoria`, `SELECT count(*) FROM usuarios` dá `permission denied` mesmo com bypass ativo). Permissão nova `tenants.suporte` (ação `suporte` estendida no `CHECK` de `permissoes.acao`) deliberadamente FORA de qualquer curinga existente (`tenants.ler`/`tenants.criar`/`tenants.administrar` já pertencem a todo `admin_empresa` de todo tenant cliente via `MATRIZ_PERFIS["admin_empresa"]["*"]` — usar qualquer uma delas daria bypass de RLS a todo admin de todo cliente do SaaS); concedida só ao `super_admin` em `seed_dev.py`. Portão é `exigir_permissao("tenants.suporte")` PURO (não `exigir_permissao_ou_escopo` do retrofit OAuth de mais cedo hoje — decisão deliberada: aceitar OAuth/API-key aqui daria a qualquer cliente de integração de qualquer tenant um caminho para a base inteira, e não há cliente de terceiro legítimo para isso). Sessão de banco isolada (`app/db/sessao_suporte.py`, pool próprio, `application_name` distinto, falha fechada conferindo `current_user`/`rolbypassrls` antes de servir). Auditoria obrigatória DENTRO da fórmula do hash (`evento`/`valor_novo` carregam `bypass_rls`/`operacao`, não só `metadados`) — adulterar a marca quebra a cadeia. Validado contra Postgres real: 33 testes de `f1/tenancy` (novo `test_suporte_multitenant.py`) + 78 de `f1` completo, sem regressão; uma sessão normal continua vendo só um tenant, sessão de suporte vê múltiplos numa chamada só. | **Pontos de risco explícitos para revisão antes de produção**: (1) migration aceita senha de fallback dev silenciosamente se `POSTGRES_SUPORTE_PASSWORD` não for definida (mitigado no compose, não na migration em si — mesmo padrão de risco da `0004`); (2) concessão da permissão em produção é manual, ninguém além do seed de dev tem `tenants.suporte` hoje; (3) `criarTenant` só grava a linha do tenant, NÃO semeia perfis/tipos de tratamento/catálogo de relatórios — precisa de `seed_dev.py` (ou equivalente de produção) depois; (4) `POST /v1/tenants` ficou fora de `ROTAS_COM_DEDUP_REAL` porque roda em sessão separada — duplo POST com a mesma chave dá `201` depois `409`, não replay; (5) decisão de compliance/LGPD pendente de aval: a auditoria de acesso cross-tenant do suporte fica só na cadeia do tenant da SEEG, um cliente auditando a própria cadeia não vê que o suporte acessou. Achado adicional da validação em lote desta sessão (não é bug, é escala): o banco `ponto` compartilhado da VPS acumulou 9.420 tenants de um dia inteiro de testes — o teste `test_chamada_de_suporte_grava_auditoria_marcada_como_cross_tenant` pode falhar por paginação (`limite=100`) quando rodado depois de muitos outros testes na mesma suíte completa; mesma classe do achado já registrado sobre `test_definir_e_listar_configuracao_do_tenant`. |
| 2026-08-08 | Orquestrador (scaffold de ambiente de produção, separado de homologação pela primeira vez) | `infra/docker-compose.prod.yml` (novo, stack exclusiva de produção: `AMBIENTE: prd` literal, volumes/rede `ponto-prd-*` sem depender de `COMPOSE_PROJECT_NAME`, `restart: always`, console do MinIO inexistente de vez, sem `build:` em nenhum serviço, mTLS do facial-svc obrigatório em vez de degradar para HTTP) + `infra/.env.prod.example` (nomes/comentários, nenhum valor real) + `infra/deploy-prod.sh` (exige `--confirmar`/`CONFIRMAR_DEPLOY_PRODUCAO=sim`, recusa diretório de hml, recusa `TAG=latest`, salva tag anterior para rollback) + `.github/workflows/deploy-prod.yml` (job separado do `ci.yml` de propósito, só `workflow_dispatch` manual com frase de confirmação exata + `environment: producao` com required reviewers — NUNCA dispara em push) + `docs/runbook-deploy-producao.md` (passo a passo completo: domínio/DNS, TLS via Traefik/ACME, 3 segredos a gerar localmente — JWT, CA+cert mTLS, certificado ICP-Brasil A1 —, preenchimento do `.env.prod`, GitHub environment, primeiro deploy manual por SSH, 7 checagens de aceite, 4 cenários de rollback). `infra/docker-compose.yml` original passou a se declarar exclusivamente de HOMOLOGAÇÃO (mudança só de comentário). Validado: `docker compose config` dos dois arquivos sem erro, `bash -n` no script novo, YAML de todos os arquivos parseando. | **Dois achados operacionais que valem escalar antes de aceitar cliente real**: (1) `infra/deploy-hml.sh` (o script que já roda em produção/hml hoje) NUNCA foi versionado no Git — existe só na VPS, sobrevive a `git reset --hard` por ser arquivo não rastreado, sem histórico/revisão/backup; se a VPS for perdida, o script de deploy vai junto. (2) **Não existe backup de banco configurado** — o cenário de rollback "restaurar backup" do runbook novo é inexecutável hoje; bloqueante antes de qualquer cliente real. Runbook também sinaliza duas pendências menores: decidir se o Swagger deve ficar público em produção (`docs.ponto.*`), e `RETENCAO_MARCACOES_DIAS`/`RETENCAO_IMAGENS_DIAS` aparecem no `.env.example` mas nenhum compose as injeta nos containers ainda. |
| 2026-08-09 | Orquestrador (fecha o achado de passagem registrado ontem sobre `POST /v1/integracoes/folha`) | A rota estava duplicada nas duas listas de idempotência (`ROTAS_COM_DEDUP_REAL`, dedup pelo middleware, escopo `POST:/v1/integracoes/folha`) e com dedup própria no handler (`integracoesFolha.criar`, mesma transação da lógica de negócio) — o middleware curto-circuitava antes do handler rodar, então a dedup própria nunca era exercitada no replay. Removida de `ROTAS_COM_DEDUP_REAL`: a dedup do handler, por rodar na mesma transação, é estritamente melhor e agora é a única fonte de verdade. Contagem corrigida: 35 (exato) + 51 (template) = **86 de 121** rotas de escrita com dedup real (era 87 com a duplicidade contada duas vezes). `infra/deploy-hml.sh` também versionado pela primeira vez nesta entrada (trazido da VPS, onde vivia sem histórico/backup desde sempre — achado já registrado ontem). | Nenhuma — ambos corrigidos. |
| 2026-08-09 | Orquestrador (backup de banco Postgres — fecha o achado bloqueante registrado ontem no scaffold de produção) | `infra/backup-postgres.sh` (`pg_dump -Fc` via `docker exec`, sha256 do dump, verificação real com `pg_restore --list` num container descartável antes de aceitar o arquivo — pipe direto pro `pg_restore` falha, "did not find magic string in file header", achado durante o próprio teste), `infra/restaurar-postgres.sh` (`--confirmar`, com um SEGUNDO portão de frase exata só para `prod`, confere sha256, recusa cruzar ambiente sem flag explícita) e `infra/instalar-backup-postgres.sh` (unit+timer systemd por ambiente, `OnCalendar` 03:30 BRT, `Persistent=true`). Retenção GFS: 14 diários + 4 semanais (hard link, custo de disco zero), teto de 18 cópias — sem mensal/anual de propósito, retenção legal é do expurgo LGPD/AFD assinado, não do backup operacional. Validado ponta a ponta contra o banco de TESTE da VPS (nunca `hml`/`prod` — `prod` nem existe como container ainda): dump de 114 MB de um banco com 3,66M linhas, restore para banco novo em 2m41s, contagem de linhas E hash MD5 do conteúdo da tabela `auditoria` idênticos entre origem e restaurado. Unit systemd rodada de verdade (`ExecMainStatus=0`). Nenhum timer foi habilitado — fica a critério do operador. `docs/runbook-deploy-producao.md` atualizado: Caso C de rollback deixou de ser aviso de pendência e virou procedimento com comandos exatos; nova Parte V documentando instalação/retenção/verificação. | **Limitação em destaque, não resolvida de propósito**: o backup fica só no disco da própria VPS — cobre erro humano/migration ruim (a maioria dos incidentes reais), não cobre perda da VPS inteira. Gancho pra cópia externa (`PONTO_BACKUP_UPLOAD_CMD`) existe, vazio por padrão, nenhuma credencial de S3/B2/etc. inventada — ligar é decisão + provisionamento do operador. |
| 2026-08-09 | Orquestrador (motor facial real conectado às rotas de biometria/marcação — fecha "próximo passo natural" registrado ontem) | Arquitetura confirmada por leitura (não assumida): `apps/api` fala com `facial-svc` DIRETO por mTLS, não via `device-gw` — o device-gw só existe pra Control iD (casamento facial dentro do próprio equipamento) e seu README proíbe template biométrico em claro por ali (ADR-006); a imagem crua só entra em `apps/api` (`POST /v1/biometrias`, `MarcacaoCriar.fotoBase64`). Novo `app/biometria/cliente_facial.py` (mTLS, campo a campo igual ao do device-gw, mas pacote separado — duplicação de 3 linhas deliberada) e `app/marcacao/pipeline/facial.py`. **Enroll real**: extrai template via `facial-svc:/enroll`, persiste cifrado com `versao_modelo` gravado literalmente (`buffalo_l-w600k_r50-v1`). **Verificação real na marcação**: compara contra os templates do colaborador via `facial-svc:/verificar`. Decisão de falha do serviço, com base em ADR-008 (não presumida): enroll indisponível = falha FECHADA (`503`, nada persistido); marcação com facial indisponível = falha ABERTA com aviso (`facial_indisponivel`) e o antifraude pontua a AUSÊNCIA de sinal (40 pontos, faixa de revisão) em vez de bloquear — ADR-008 já decide isso explicitamente ("bloquear por sinal indisponível quebra na primeira semana... problema jurídico da empresa"), e `SCORE_SEM_SINAIS=100`/ADR-014 já estabelece que ausência de sinal nunca é evidência de fraude. Validado com pesos ONNX reais na VPS (não mock): mesma pessoa (recorte diferente) aprova; pessoa diferente reprova com `403 PONTO-SCORE-003` e zero marcação gravada; serviço fora do ar tratado nos dois fluxos sem exceção solta. Controle negativo rodado (desligar a leitura da imagem quebra os testes de propósito). Regressão idêntica ao baseline em `f5`/`f2`/`f14/antifraude`/`f14/adversarial`/`f14/hardening`. `apps/facial-svc/facial/motor/**` e o cliente mTLS do device-gw não foram tocados — só o caminho de chamada. | Quatro gaps deixados em aberto, documentados na própria entrega: (1) `fotoBase64` não está declarado no contrato (`BiometriaCriar`) — lido do corpo bruto, mesma técnica que o campo `vetor` já usava, mas declarar formalmente é decisão de RFC/contrato do dono do produto — **RESOLVIDO em 2026-08-09 pela [RFC-021](rfc/RFC-021-biometriacriar-sem-campos-de-template-nem-de-imagem.md)**: `fotoBase64`, `mimeType`, `vetor`, `versaoModelo`, `dimensao` e `provedor` (os seis campos que a rota lê do corpo bruto, não só o primeiro) declarados como opcionais em `components.schemas.BiometriaCriar`, com a exclusividade mútua `fotoBase64` x `vetor` escrita na descrição do schema. Zero mudança de runtime: `app/schemas/contrato.py` (gerado) não foi regerado e o handler segue lendo o corpo bruto; (2) `/liveness` continua sem chamador (fora do escopo desta rodada); (3) `marcacoes_meta.score_facial` fica `NULL` mesmo com verificação aprovada, por desenho — `facial-svc` recusa devolver o placar (`expoe_regra: false`), sinal binário `facial_verificado` foi usado em vez disso; (4) **mTLS api↔facial-svc sem certificado de cliente provisionado em lugar nenhum** — `infra/docker-compose*.yml` ainda não injeta `FACIAL_MTLS_CERT_PATH`/`KEY_PATH` no serviço `api` (só no `facial-svc` como servidor); falta emitir o par antes de qualquer deploy real, senão a API recusa montar o cliente (falha fechada, já testada). |
| 2026-08-09 | Orquestrador (auditoria de prontidão pra tornar o repositório público — 4 agentes em paralelo, pedido explícito do dono do produto de considerar abrir o repo por causa do bloqueio de billing) | **Histórico do Git: LIMPO.** `gitleaks` (histórico completo, `--all --full-history`) + varredura manual complementar (chaves privadas, tokens de todo provedor conhecido, arquivos de certificado adicionados-e-removidos em qualquer commit, connection strings, 218 objetos unreachable inclusive) — zero segredo real em qualquer momento da história, nenhum `.env` real jamais commitado, IP da VPS nunca apareceu. **Nenhuma reescrita de histórico (filter-repo/BFG) é necessária.** **Código atual: 1 achado real corrigido** — `infra/deploy-hml.sh` (trazido da VPS nesta sessão) tinha capturado por engano, na primeira linha, o aviso do próprio SSH ao adicionar o host aos `known_hosts` (linha padrão do OpenSSH que inclui o IP do destino) — expunha o IP da VPS e quebraria o script (linha antes do `#!/bin/bash`); corrigido antes de qualquer commit, arquivo nunca chegou a ser versionado sujo. `.gitignore` cobre tudo que deveria; dependências com CVE conhecido não-bloqueante (`cryptography` 49→50, `pypdf` 6.14→6.15, 6 HIGH transitivas de build em `apps/web`, nenhuma no bundle); zero dado pessoal real em teste (CPFs sintéticos, foto MIT já decidida). **CI/CD endurecido**: `permissions:` explícito e mínimo em todo job sem alterar lógica de deploy; `environment: homologacao` acrescentado a `deploy-hml-aplicar` (inerte até configurado em Settings, mas é a única barreira que sobrevive a um PR de fork malicioso — achado importante: os `if: github.ref == 'main'` que já existiam NÃO protegem contra fork PR, porque o GitHub executa a versão do workflow que está NO PRÓPRIO PR, não a da branch base); comentário de topo em `ci.yml` documentando esse modelo de ameaça para quem revisar PR externo no futuro; nenhum `pull_request_target` em lugar nenhum (confirmado, já estava seguro). Custo de CI confirmado: runners `ubuntu-latest` são gratuitos e ilimitados em repositório público — resolveria o bloqueio de billing por completo assim que o repo abrir. **Documentação**: README atual já é bom mas tem status desatualizado (diz "Fase 0, tudo 501" — falso há semanas) e rodapé contraditório ("repositório privado"); proposta de correção em `README.proposto.md` (não aplicada, decisão do usuário). `docs/backlog.md` linha 208 tem um mapa detalhado de infraestrutura (provedor, caminhos absolutos, usuário do runner, conteúdo da regra sudoers) — nenhum segredo, mas reconhecimento gratuito para quem for atacar. `docs/lgpd/` (minuta de contrato de operador + RIPD) tem exposição LEGAL, não técnica — o próprio documento já se declara "não é peça jurídica final". LICENSE/CONTRIBUTING/CODE_OF_CONDUCT/SECURITY não existem — nenhum foi criado sem autorização. | **Veredito: histórico e código seguros para publicar sem reescrever nada.** Decisões 100% do dono do produto, nenhuma tomada aqui: (1) qual licença (ou nenhuma — "todos os direitos reservados" é a leitura padrão de código público sem LICENSE); (2) aceita contribuição externa (define se cria CONTRIBUTING/CODE_OF_CONDUCT); (3) aceitar/ajustar `README.proposto.md`; (4) redigir ou não a linha 208 do backlog e decidir o destino de `docs/lgpd/` antes de publicar; (5) criar `SECURITY.md` (fortemente recomendado, conteúdo já rascunhado no relatório do agente) e habilitar *Private vulnerability reporting*; (6) preencher description/topics do repo no GitHub. **Configurações do GitHub que só o usuário pode aplicar antes de virar público** (nenhuma aplicável por código): Settings → Actions → General → exigir aprovação de workflow pra TODO colaborador externo (não só "first-time"); criar environment `homologacao` com deployment branch restrito a `main`; conferir que o `producao` já tem required reviewers (scaffold de ontem manda criar); branch protection em `main` (PR + aprovação + status check `CI OK`, bloquear force-push); revogar `PONTO_DEPLOY_SSH_KEY`/chave forced-command se não usadas mais. Recomendação adicional não aplicada (mexeria em lógica de deploy, fora do escopo desta auditoria): mover `deploy-hml-aplicar` pra um workflow disparado por `push`/`workflow_run` em vez de viver dentro do `pull_request` do `ci.yml` — nesse caso o GitHub sempre usa o arquivo da branch base, tornando a edição maliciosa via fork estruturalmente impossível, não só bloqueada por configuração. |
| 2026-08-09 | Orquestrador (incidente real em `ponto-hml`, achado de passagem no relatório do agente de mTLS — corrigido na hora, direto na VPS, sem tocar em código/deploy) | `GET /v1/admin/saude` do stack `ponto-hml` real estava respondendo **`status: indisponivel`** no momento em que foi checado — banco com `InvalidPasswordError`. Causa: o role `ponto_app_runtime` no Postgres tinha uma senha diferente da que está em `infra/.env`/injetada no container da API (drift — provavelmente uma migration rodou em algum momento anterior com um valor antigo de `POSTGRES_APP_PASSWORD`, e o role nunca foi resincronizado depois que `.env` mudou; migrations só reexecutam `ALTER ROLE` ao AVANÇAR de revisão, não em toda chamada de `alembic upgrade head`). Corrigido com `ALTER ROLE ponto_app_runtime PASSWORD '...'` direto no Postgres, valor copiado do `.env` real — `/v1/admin/saude` voltou a `status: ok` imediatamente. Segundo achado, mesma investigação: `/run/secrets/jwt/private.pem` (a chave de assinatura JWT) estava `root:root 600` — o processo da API roda como uid 1001, e a leitura falhava (`PermissionError`) mesmo testada de dentro do próprio processo Python já rodando. Corrigido `chown 1001:1001` + `chmod 600`, leitura confirmada funcionando. Container não foi reiniciado (33h de uptime) — não foi possível confirmar se a assinatura JWT já estava de fato falhando em runtime ou se a chave só nunca tinha sido relida desde o boot; o risco ficava latente pro PRÓXIMO restart/deploy de qualquer forma, então a correção valia independente disso. | Nenhuma ação de código pendente — os dois eram drift operacional da VPS, não bug de aplicação. **Vale investigar a causa raiz do drift de senha** (por que `.env` e o role do Postgres divergiram) antes que aconteça de novo num próximo deploy — meu palpite não confirmado é que alguém rodou a migration `0004_role_login_app` manualmente com `POSTGRES_APP_PASSWORD` diferente do `.env` atual em algum ponto entre 25/07 (criação dos arquivos de chave) e agora, mas não investiguei a fundo pra confirmar. Considerar adicionar ao runbook de deploy um passo de verificação pós-deploy que teste `/v1/admin/saude` e falhe ruidosamente se `status != "ok"`, em vez de descobrir isso só quando alguém checa manualmente. |
| 2026-08-09 | Orquestrador (`facial-svc:/liveness` ligado à marcação — fecha o gap "2" registrado hoje mais cedo: "`/liveness` continua sem chamador") | **De onde vêm os quadros, sem inventar campo no contrato.** `/liveness` é multi-quadro por desenho (`FACIAL_LIVENESS_MIN_QUADROS=2`; com 1 quadro o motor reprova direto, não degrada) — então `MarcacaoCriar.fotoBase64`, a captura única que alimenta `/verificar`, **não serve**: mandar uma foto só produziria um `aprovado:false` estrutural, e gravar esse `false` transformaria "o app só mandou uma foto" em "o colaborador tentou fraudar" (exatamente o que ADR-014 proíbe). O contrato, porém, **já tinha** onde carregar a sequência: `MarcacaoCriar.livenessEvidencia` (`type: object`, `additionalProperties: true`, descrição "Evidencia do desafio de vivacidade executado"), declarado desde a Fase 0 e sem nenhum consumidor. Convenção adotada: chave `quadrosBase64` com a lista em ordem temporal, nome idêntico ao do corpo de `facial-svc:/liveness`. **Nenhum campo novo foi acrescentado ao `openapi.yaml`.** **Decisão de arquitetura (ADR-008 aplicado, não reaberto):** a chamada usa `exigirAprovacao: false` — reprovação volta como VALOR (`liveness_aprovado=False`), nunca como exceção, porque a heurística é declaradamente falível (`facial/motor/liveness.py`: não é classificador treinado) e um sinal falível não pode ser portão sobre a jornada do trabalhador. Quem quiser recusar mesmo assim tem a alavanca que ADR-008 deu: `limiar_bloqueio`/`exige_liveness` por tenant. Defesa em profundidade: `app.marcacao.pipeline.facial` também converte um `PONTO-SCORE-002` que porventura chegue em sinal negativo, em vez de deixá-lo virar 403 — provado por controle negativo (virar o flag para `true` NÃO quebra o comportamento; só o teste que lê o código-fonte acusa). Evidência ausente/malformada/curta = "sem sinal" + aviso, nunca `False`. Novos avisos: `liveness_reprovado`, `liveness_indisponivel`, `liveness_evidencia_invalida`. `marcacoes_meta.liveness_aprovado` (coluna que existia e ficava sempre `NULL`) passa a receber o veredito real. **Achado medido, não estimado:** o primeiro palpite de timeout (6s) derrubava o sinal em ~metade das chamadas — `/liveness` com 3 quadros custou **3,9s a 10,6s** contra o motor real na VPS (8 vCPU, sem GPU, máquina compartilhada), porque a detecção roda uma vez POR quadro; `FACIAL_TIMEOUT_LIVENESS_S` ficou em 12s. 30 testes em `tests/f14/facial` verdes contra pesos ONNX reais (sequência com movimento aprova; simulação de tela/moiré, foto impressa e quadro congelado reprovam **e a marcação acontece do mesmo jeito**; serviço fora do ar segue com aviso). Três controles negativos rodados (desligar a gravação do sinal, virar `exigirAprovacao`, e virar `exigirAprovacao` + deixar o 403 escapar) — o terceiro derruba 4 testes, provando que a suíte pega um portão de verdade. Regressão `f5`/`f2`/`f14/*` idêntica ao baseline do mesmo commit. `facial/motor/liveness.py` **não foi tocado** — só a chamada. | **(1) Decisão de contrato pendente do dono do produto:** formalizar `quadrosBase64` no `openapi.yaml` — ou declarando a sub-estrutura de `livenessEvidencia`, ou promovendo o campo a primeira classe em `MarcacaoCriar`. Hoje é convenção acordada entre `apps/api` e quem captura (F7/F8), não contrato público; um integrador externo não tem como descobri-la lendo o `openapi.yaml`. Mesma classe do gap de `fotoBase64` em `BiometriaCriar` registrado hoje mais cedo. **(2) Custo por quadro é do capturador:** F7/F8 devem mandar 2–3 quadros do rosto **recortado**, em resolução de câmera — a faixa de 10s medida acima veio de recortes ampliados 2x; a foto inteira ampliada estoura qualquer timeout razoável. Vale limitar quadros/resolução no cliente antes do primeiro piloto. **(3)** Continua valendo a nota de 08/08: substituir a heurística por um modelo treinado de procedência confiável é decisão do dono do produto, e é pré-requisito para qualquer uso de liveness como portão. **(4)** Correção de passagem, sem relação com a tarefa: `app/comum/idempotencia_middleware.py` tinha erro de `mypy --strict` em `main` (variável `chave` religada com tipo incompatível no laço de validação do índice de rotas) — o gate de tipos estava vermelho no commit `173bb2b`; renomeada a variável do laço. Convergiu com o agente de CI, que achou o mesmo erro pela triagem do PR do Dependabot e o commitou em `c576ace` — a correção é a mesma linha, sem conflito. |
| 2026-08-09 | Orquestrador (mTLS real entre `api` e `facial-svc` — fecha o gap "4" registrado hoje mais cedo, e era maior do que o registrado) | Provisionar certificado não bastava: o CLIENTE mTLS da API estava genuinamente quebrado. `infra/gerar-certificados-mtls-facial.sh` (novo): CA própria autoassinada (3650 dias) + cert de servidor (`CN=facial-svc`) + cert de cliente (`CN=ponto-api`, 825 dias cada) em dois diretórios separados (a chave do servidor nunca visível dentro de `api`, nem a do cliente dentro de `facial-svc`); `chown 1001:1001` explícito — sem isso, bind mount `root:root 600` é ilegível pelo processo e o sintoma observado é `Permission denied` genérico, nada que mencione TLS (mesma classe do incidente da chave JWT do hml, corrigido à parte hoje). Wire nos dois composes: `FACIAL_MTLS_CERT_PATH`/`KEY_PATH`/`CA_PATH` no serviço `api` (antes só o `facial-svc` recebia essas variáveis, como servidor); `FACIAL_SVC_URL` em prd virou `https://` (mTLS obrigatório, `http` seria falha de conexão garantida); healthcheck do `facial-svc` reescrito — o antigo batia em HTTP puro e nunca completaria com TLS ligado, o container jamais ficaria `healthy` e a `api` (`depends_on: service_healthy`) nunca subiria. **Bug real achado na validação**: `apps/api/app/biometria/cliente_facial.py` passava `cert=(...)` + `verify=<caminho da CA>` pro `httpx.AsyncClient` — no httpx 0.28, `create_ssl_context` faz `return` antecipado no ramo `isinstance(verify, str)`, ANTES do bloco `if cert:`, descartando o certificado de cliente em silêncio. Contra um `facial-svc` com `CERT_REQUIRED`, isso dá `SSLEOFError` → `ConnectError` vazio, indistinguível de "motor fora do ar" — nem o teste de montagem nem o de handshake anteriores pegavam, porque nenhum dos dois testava o CAMINHO real de arquivo (um só conferia timeout, o outro construía o contexto à mão). Corrigido com `ssl.SSLContext` explícito; controle negativo natural (o teste 1 falhava antes da correção do cliente, passa depois, tudo mais idêntico). Validado em `docker compose` real (stack isolado `ponto-mtls-teste`, criado e destruído): `facial-svc` subiu em HTTPS mútuo e ficou `healthy`; de dentro do container `api`, cert válido → 200; sem cert, CA errada, ou `http://` em texto claro → recusado no handshake (`SSLEOFError`/`RemoteProtocolError`), antes de qualquer byte de aplicação. 12 testes de mTLS passam. | Achados colaterais não corrigidos por essa tarefa (fora do escopo pedido): `apps/device-gw/gateway/dominio/cliente_facial.py` tem o MESMO bug latente do httpx (nenhuma rota do device-gw chama o facial-svc hoje, então não é urgente); `apps/facial-svc/Dockerfile` ainda tem `HEALTHCHECK` em `http://` (inofensivo — o compose sobrescreve nos dois ambientes — mas armadilha pra quem rodar a imagem solta). Recomendação do operador: certificados NÃO devem ser versionados fora do git nem copiados de máquina de dev — rodar o script direto na VPS no primeiro deploy real, instalar fora do checkout (que sofre `git reset --hard`), guardar `ca.key` fora da VPS pra permitir renovar as folhas em 825 dias sem reconfigurar os dois lados. Renovação é manual, sem automação ainda. |
| 2026-08-09 | Orquestrador (formaliza `fotoBase64` e demais campos de `BiometriaCriar` no contrato — fecha o gap "1" registrado hoje mais cedo) | O gap real era maior que o registrado: não só `fotoBase64` estava fora do schema `BiometriaCriar` — `vetor`, `versaoModelo`, `dimensao` e `provedor` TAMBÉM nunca tinham sido declarados (o schema só tinha 7 propriedades, nenhuma capaz de carregar template ou imagem). `additionalProperties: false` não existe em lugar nenhum do contrato (confirmado, 0 ocorrências) — não havia violação silenciosa, era pura lacuna de documentação. Seis propriedades novas, todas opcionais (`fotoBase64`, `mimeType` com `default: image/jpeg`, `vetor`, `versaoModelo`, `dimensao` como `int ≥ 1`, `provedor` com `default: facial-svc`). Exclusividade mútua (`fotoBase64` x `vetor`) documentada em PROSA na `description`, verificada pela aplicação (`PONTO-VAL-001`) — não `oneOf`/`anyOf`: o contrato inteiro tem ZERO ocorrências desses construtos, e o padrão já estabelecido pra alternativas mutuamente exclusivas (`MarcacaoCriar`: `colaboradorId` x `cpf` x `matricula`) já é esse. RFC-021 escrita documentando a decisão e as 3 opções consideradas. `contrato.py` (gerado) NÃO foi regerado de propósito — a versão do `datamodel-code-generator` disponível não bate com a que produziu o arquivo versionado, e regenerar renomearia enums de OUTROS schemas (`DecisaoRevisao`→`Decisao2`), quebrando importações alheias; zero mudança de runtime, o handler continua lendo o corpo bruto. Validado: `spectral` (mesmo ruleset do CI) 0 issues; `schemathesis` real contra API na VPS restrito a `criarBiometria`, 101/101 em cada um dos 6 checks; 16 testes de `f14/facial` idênticos com e sem a mudança (zero regressão, esperado de mudança que não toca runtime). | Quando alguém regerar `contrato.py` no ambiente certo (extra `codegen` fixado), `dimensao` passa a ser validado como `int ≥ 1` na borda pela primeira vez (hoje `"abc"` passa sem erro) — aperto desejado, mas observável, registrado na RFC (§7). |
| 2026-08-09 | Orquestrador (primeiro deploy real de `ponto-hml` com o motor facial de verdade — CI ficou verde, faltava só este último passo) | `deploy-hml (aplicar)` falhava com dois achados reais, nenhum relacionado a código de aplicação: (1) `POSTGRES_SUPORTE_PASSWORD` (variável nova da migration `0005_role_suporte_bypassrls`, de sessão anterior) nunca tinha sido adicionada ao `.env` real da VPS — gerada uma senha aleatória de 32+ chars e adicionada. (2) **Achado mais sério**: o container `facial-svc` real (com `opencv-python-headless`/`insightface` de verdade, primeira vez que essa imagem sobe fora de teste isolado) morria no boot com `ImportError: libxcb.so.1`, e depois de corrigir isso, `ImportError: libGL.so.1` — o wheel manylinux do OpenCV headless publicado no PyPI linka dinamicamente contra essas libs mesmo sem precisar de servidor X de verdade (comportamento conhecido do ecossistema, não bug do projeto). `apps/facial-svc/Dockerfile` ganhou `libxcb1 libgl1 libglib2.0-0` no `apt-get install` da camada de dependências (junto com `libgomp1`, que já ficava). Corrigido iterativamente: build isolado na VPS, reproduzindo o `ImportError` exato, até `import cv2`/`from facial.motor import arcface` limparem e o servidor subir respondendo `{"status":"ok","modelosPresentes":true}` com os pesos reais. Depois dos dois fixes, `deploy-hml.sh` completou com sucesso — `ponto-hml` está de pé com a imagem nova do facial-svc pela primeira vez. | Nenhuma ação de código pendente. `FACIAL_MTLS_CA_PATH vazio em ambiente de producao` aparece no log do `facial-svc` real do hml — esperado, o script de geração de certificados mTLS (`infra/gerar-certificados-mtls-facial.sh`, de mais cedo hoje) ainda não foi rodado na VPS de verdade, só validado num stack de teste isolado. Rodar antes de qualquer fluxo real de biometria depender do par api↔facial-svc em hml. |
| 2026-08-09 | Orquestrador (bug sistêmico real: todos os scripts de `infra/` sem bit de execução — bloqueava o próprio `deploy-hml (aplicar)` do CI) | Os 6 scripts shell de `infra/` (`deploy-hml.sh`, `deploy-prod.sh`, `backup-postgres.sh`, `restaurar-postgres.sh`, `instalar-backup-postgres.sh`, `gerar-certificados-mtls-facial.sh`) foram todos commitados como `100644` (sem `+x`) — típico de arquivo criado/copiado a partir de uma sessão Windows, que não rastreia o bit executável Unix. Passou despercebido em toda validação anterior (`bash -n` não exige `+x`, só sintaxe) até o primeiro `sudo -n /docker/ponto-eletronico/infra/deploy-hml.sh` real do runner self-hosted, que falhou com `sudo: ...: command not found` — mensagem enganosa, é exatamente o erro do `sudo` para um alvo sem permissão de execução, não "arquivo não existe". Corrigido com `git update-index --chmod=+x` nos 6 arquivos (`100644` → `100755`). **Efeito colateral que exigiu correção manual extra**: como o próprio `deploy-hml.sh` começa com `git fetch && git reset --hard origin/main`, e o `sudo -n` do CI invoca o arquivo JÁ NO DISCO antes desse reset rodar, havia uma dependência circular — a correção do bit só chegaria ao disco DEPOIS de uma execução bem-sucedida, mas a execução não começava por falta do bit. Quebrado manualmente com um `chmod +x` direto na cópia da VPS, uma única vez; daqui em diante todo `git reset --hard` preserva `+x` porque agora está gravado corretamente no índice do git. | Nenhuma ação de código pendente. Vale considerar, como nota de processo pra sessões futuras: qualquer arquivo `.sh` novo criado/editado a partir de uma sessão Windows precisa de `git update-index --chmod=+x` explícito depois do primeiro `git add` — o `git` no Windows não visualiza nem preserva esse bit sozinho. |
| 2026-08-09 | Orquestrador (primeiro deploy real de `ponto-hml` de ponta a ponta pós-billing, com todo o trabalho de 08-09/08 — motor facial, mTLS, liveness, suporte multi-tenant, OAuth) | Depois dos fixes acima (senha de suporte, libs do OpenCV, bit de execução), `deploy-hml.sh` completou com sucesso: migration `0004→0005` aplicada (role `ponto_app_suporte` criada), os 9 containers do stack (`api`, `web`, `worker`, `device-gw`, `scheduler`, `facial-svc`, `postgres`, `redis`, `minio`) todos `healthy`, `GET /v1/admin/saude` respondendo `status: ok` com banco e Redis `ok`. Confirmado direto no container, não só pela porta publicada. | Nenhuma — validado de ponta a ponta. Rodar o script de geração de certificados mTLS (item já registrado acima) antes de qualquer fluxo real de biometria em produção precisar do par api↔facial-svc autenticado. |
| 2026-08-09 | Orquestrador (fecha o último item aberto do gap de mTLS: certificados provisionados de verdade em `ponto-hml`, não só validados em stack isolado) | Rodado `infra/gerar-certificados-mtls-facial.sh` direto na VPS (usuário `aftunnel`, uid 1001 — coincide com o uid do processo dentro dos containers, então o `chown 1001:1001` do próprio script já deixou as pastas com o dono certo, sem passo manual extra). Saída ficou em `infra/keys/mtls-facial/{servidor,cliente}` — dentro do checkout, mas em `infra/keys/`, que o `.gitignore` já bloqueia por inteiro e que `git reset --hard` (primeira linha do `deploy-hml.sh`) NUNCA apaga (só reseta arquivos rastreados); é o mesmo diretório-raiz onde as chaves JWT do hml já viviam (`JWT_KEYS_DIR=./keys`), então o padrão é consistente com o que já existia, não uma exceção nova. `.env` do hml ganhou as 5 variáveis que faltavam (`FACIAL_CERTS_DIR`, `FACIAL_CERTS_CLIENTE_DIR`, `FACIAL_TLS_CERT_PATH`, `FACIAL_TLS_KEY_PATH`, `FACIAL_MTLS_CERT_PATH`, `FACIAL_MTLS_KEY_PATH`) e `FACIAL_SVC_URL`/`FACIAL_MTLS_CA_PATH` foram preenchidas (antes vazias/`http://`). `docker compose -p ponto-hml up -d --force-recreate facial-svc api`: os dois subiram `healthy`, `facial-svc` logou `Uvicorn running on https://0.0.0.0:8000` (antes era HTTP puro) e o log de aviso `FACIAL_MTLS_CA_PATH vazio em ambiente de producao` (citado no achado de mais cedo) não aparece mais. **Validação real, não só "container saudável":** de dentro do container `api`, uma chamada HTTPS para `facial-svc:8000/health` com o certificado de cliente correto retornou 200; a MESMA chamada sem certificado nenhum foi recusada no handshake (`SSL: UNEXPECTED_EOF_WHILE_READING`), antes de qualquer byte de aplicação — prova que o mTLS está de fato sendo exigido, não só configurado. | Nenhuma ação de código pendente. `modelosPresentes: false` no `facial-svc` do hml é esperado e não relacionado a esta tarefa — os pesos `.onnx` vivem num volume separado (`facial-models`) com ciclo de vida próprio (ver cabeçalho do `Dockerfile`), população desse volume é passo de deploy distinto, ainda não feito neste ambiente. Renovação das folhas (825 dias, vencem em nov/2028) continua manual — sem automação. `ca.key` gerada permanece só na VPS (`infra/keys/mtls-facial/ca/ca.key`, fora do git); o operador pode preferir movê-la para fora da VPS por segurança, mas isso é decisão dele, não ação de código. |
| 2026-08-09 | Orquestrador (pesos reais do motor facial populados em `ponto-hml` — fecha o `modelosPresentes: false` registrado no achado anterior) | Volume `ponto-hml-facial-models` estava vazio desde a criação do stack: `facial-svc` sempre subiu `healthy` (o healthcheck bate em `/health`, não em `/ready`, de propósito — ver comentário do `Dockerfile`), mas nenhuma chamada real a `/enroll`/`/verificar`/`/liveness` jamais teria funcionado. Populado com um container descartável da própria imagem `ghcr.io/henriquehmas-rgb/ponto-facial-svc:latest` (`AMBIENTE=dev` só para essa execução isolada — a `Configuracao` real do `hml` continua recusando `FACIAL_BAIXAR_MODELO`), montando o volume real em `/models` e chamando `MotorFacial().carregar()` diretamente: baixa `buffalo_l` (281 MB) do release oficial do InsightFace no GitHub, extrai, e só então descobre-se que `detection` (`det_10g.onnx`) e `recognition` (`w600k_r50.onnx`) são os únicos dois pesos que o motor efetivamente carrega — `1k3d68`/`2d106det`/`genderage` ficam no disco (fazem parte do pacote `buffalo_l` completo) mas o próprio `MotorFacial` os ignora na inicialização, exatamente como o README descreve. **Segundo achado, mesma investigação**: `.env` do hml tinha `FACIAL_MODEL_VERSAO=arcface-r100-v1` — carimbo de um modelo QUE NUNCA ESTEVE CARREGADO (nem antes, nem depois desta tarefa); o motor real sempre foi `buffalo_l`/`w600k_r50` (ResNet50), não R100. É o cenário que o próprio README da `facial-svc` avisa por nome: "carimbo que descreve um modelo que não é o carregado é pior que carimbo nenhum" — arriscava reprovar (`400 PONTO-VAL-001`, checagem de versão em `/verificar`) todo template futuro contra um rótulo que nunca foi verdade. Corrigido para `buffalo_l-w600k_r50-v1` (o próprio default do `docker-compose.yml`, que o `.env` sobrescrevia com o valor errado). `facial-svc`+`api` reciclados; `GET /ready` (via mTLS real, dentro do container `api`) confirma `modelos.presentes: true`, os 5 arquivos do pacote no disco, e `modeloVersao: buffalo_l-w600k_r50-v1` batendo com o que está carregado. | Chamada real fim-a-fim a `/enroll` com foto de rosto de verdade não foi feita nesta tarefa — as fixtures de teste do repositório geram rosto sintético via código (`apps/facial-svc/tests/test_motor_facial.py`), não têm uma imagem estática pronta para reusar por fora do pytest; `/ready` com os pesos corretos carregados é evidência forte mas não é o mesmo que uma captura real passando pelo pipeline completo. Vale rodar `apps/api/tests/f14/facial` contra o `ponto-hml` real (ou uma captura manual pelo app) na próxima janela de validação. |
| 2026-08-09 | Orquestrador (fecha a pendência do item anterior: chamada real fim-a-fim a `POST /enroll` contra `ponto-hml`) | Rodado a partir de um container descartável na rede `ponto-hml-interna`, usando o certificado de cliente real e `insightface.data.get_image("t1")` — a foto de grupo (6 pessoas) que acompanha o próprio pacote `insightface` no PyPI, a mesma fonte de rosto que `apps/facial-svc/tests/test_motor_facial.py` já usa por licença (nenhum rosto entra no repositório da SEEG). Recortado o primeiro rosto detectado (margem 30%), reencodado em JPEG, enviado via mTLS real (TLS mútuo, sem `httpx` — a imagem `runtime` não carrega essa dependência, é `dev`-only; usado `http.client` da stdlib com `ssl.SSLContext` client) para `https://facial-svc:8000/enroll` do stack `ponto-hml` de verdade. **`200`, com `template` retornado** — detecção (`det_10g`), embedding de 512-d (`w600k_r50`) e serialização do template rodaram de ponta a ponta contra o serviço real, pela primeira vez fora de teste automatizado. Container e script temporário removidos depois. | Nenhuma. Encerra a cadeia de validação do motor facial em `ponto-hml`: mTLS real + pesos reais + carimbo de versão correto + chamada de aplicação real, todos confirmados nesta sessão. |
| 2026-08-09 | Orquestrador (achado real do dono do produto navegando `ponto-hml` pela primeira vez logado — 3 bugs reais, todos corrigidos e validados ao vivo na hora) | **(1) O mais grave, achado sistêmico**: `deploy-hml`/`deploy-prod` (`.github/workflows/ci.yml`/`deploy-prod.yml`) publicavam a imagem `web` no GHCR SEM passar `build-args` ao `docker/build-push-action`. `apps/web/Dockerfile` cai nos defaults (`AMBIENTE=prd`, `NEXT_PUBLIC_API_URL=http://localhost:8000`) — e Next.js inlina `NEXT_PUBLIC_*` no bundle do NAVEGADOR em tempo de BUILD, sem override possível em runtime (diferente dos 4 serviços Python, que releem `AMBIENTE` a cada boot). Resultado: TODA chamada autenticada do portal, em QUALQUER deploy até hoje, tentava `http://localhost:8000` a partir do navegador do usuário real — `net::ERR_FAILED` garantido — e o badge do cabeçalho sempre mostrava "PRD", mesmo em hml. Sintoma relatado pelo usuário: "Não foi possível carregar seus dados" na tela de Registrar Ponto. Corrigido: `build-args: AMBIENTE=hml/prd` + `NEXT_PUBLIC_API_URL=https://api.ponto.${{ vars.DOMINIO_HML/DOMINIO_PRD }}` nos dois workflows; criada a variável de repositório `DOMINIO_HML=srv1450678.hstgr.cloud` (`DOMINIO_PRD` ainda **não** existe — precisa ser criada antes do primeiro deploy real de produção, hoje só tem placeholder em `.env.prod.example`). Rebuild manual da imagem `web` direto na VPS com os build-args corretos e `force-recreate` — validado no navegador (viewport mobile): badge virou `hml`, zero requisição a `localhost:8000`, bundle novo servido. **(2) Achado real, não relacionado ao (1)**: `apps/api/app/routers/empresas.py` tinha `_ORDENACAO_PADRAO = "criado_em:desc"` (snake_case) enquanto `app.organizacao.empresas.CAMPOS_ORDENAVEIS` já tinha migrado pra chaves camelCase do contrato (`criadoEm`, `razaoSocial`, `cnpj`, `nomeFantasia` — o próprio comentário do dicionário já registrava essa migração como correção de um achado anterior da F9b, mas só ajustou o dicionário, não o default do router). Qualquer chamada que OMITE `ordenar` (o painel de RH, `useEmpresasDoTenant`) sempre respondia `400 PONTO-VAL-005 "Campo de ordenacao desconhecido: 'criado_em'"`. Corrigido para `"criadoEm:desc"`; confirmado `GET /v1/empresas` sem `ordenar` — `200` depois do fix, `400` antes, mesmo request. **(3) Achado real, maior em escopo, NÃO corrigido** (decisão de produto, não bug de uma linha): os widgets "Tendência de horas extras" e "Ocorrências por mês" do painel do RH respondem `404 PONTO-REC-001` porque `GET /v1/relatorios/{codigo}/executar` busca a definição do relatório (`RelatorioDefinicao`) por `(tenant_id, codigo)` numa tabela — e **nenhum código do repositório, em lugar nenhum, cria essas linhas** para nenhum tenant, dev ou real. Confirmado por busca exaustiva: zero ocorrência de `RelatorioDefinicao(` fora de teste. O comentário de `app.identidade.tenancy.servico_suporte.criar_tenant` AFIRMA que "esses catalogos continuam sendo semeados por `migrations/seed_dev.py`" — **isso é falso pro catálogo de relatórios especificamente**: `seed_dev.py` semeia só as PERMISSÕES `relatorio.*`, nunca as ~20 definições de relatório que o `README`/`PROJETO.md` anunciam como entregues ("mais de 20 relatórios gerenciais"). Todo tenant — inclusive um cliente real criado via `criarTenant` — nasce sem nenhum relatório de fábrica. | **(1) e (2): nenhuma ação de código pendente**, ambos corrigidos e validados. Vale considerar um teste HTTP (`TestClient`) real para `GET /v1/empresas` SEM `ordenar`, já que os testes existentes (`tests/f2/organizacao/test_empresas_service.py`) chamam a camada de serviço direto com um `PedidoDePagina` já resolvido — nunca exercitam o default do router, por isso o bug (2) sobreviveu a toda a suíte. Criar `DOMINIO_PRD` (variável do repositório) ANTES do primeiro deploy real de produção — hoje o workflow reconstruiria o mesmo bug (1) em prd se disparado como está. **(3): decisão de produto pendente, não uma correção pontual.** Precisa de alguém desenhar e implementar a semeadura real do catálogo de ~20 relatórios "de fábrica" (provavelmente via `seed_dev.py` para dev + um passo equivalente no fluxo de `criarTenant`/onboarding para tenants reais — o próprio docstring de `criar_tenant` já registra a lacuna e recomenda não dar `INSERT` em `relatorios` pra credencial de suporte). Sem isso, o painel de RH de QUALQUER tenant real vai sempre mostrar esses dois widgets quebrados. |
| 2026-08-09 | Orquestrador (achado real, dono do produto: `/eu/perfil` travava carregando "Sessões ativas" pra sempre) | `GET /v1/auth/sessoes` respondia `500 "resposta fora do contrato"` para QUALQUER sessão com IP gravado (a normalidade, não o caso raro): `_para_schema` (`apps/api/app/routers/auth.py`) copia o atributo cru do ORM pro dict de entrada do pydantic; a coluna `sessoes.ip` (`INET`) volta do driver como `ipaddress.IPv4Address`, e `contrato.Sessao.ip` é `str` — pydantic v2 não converte implicitamente porque a chamada é `model_validate(dict)`, não `from_attributes=True`. Corrigido convertendo `IPv4Address`/`IPv6Address` para `str` dentro de `_para_schema` antes de `model_validate`. Validado ao vivo: `/eu/perfil` agora lista as 5 sessões reais do usuário, com IP e última atividade. | Nenhuma ação de código pendente. `admin.py` tem uma função `_para_schema` DUPLICADA (mesma receita, código diferente) que não constrói `Sessao` hoje — se algum dia construir, herda o mesmo risco; vale considerar unificar as duas em `app.comum` se a duplicação crescer. |
| 2026-08-09 | Orquestrador (achado real: nenhuma tela nem rota do contrato liga um `usuario` (login) a um `colaborador` — bloqueava literalmente "bater o ponto" pelo dono do produto) | Pedido do usuário: "não consigo bater o ponto". Investigação mostrou que `/eu/registrar` e `/eu/perfil` (Dados cadastrais) dependem de `usuarios.colaborador_id` (FK nullable, `uq_usuarios_colaborador`) — e o usuário admin semeado por `seed_dev.py` **nunca tem esse campo preenchido** (o script cria só `usuarios`, não liga a nenhum `colaboradores`, coerente com o próprio docstring: "O que NAO entra: colaborador, vinculo, jornada..."). Sem colaborador vinculado, a tela de registrar ponto não tem contra o que verificar identidade nem jornada. **Busca no contrato e nas rotas (`app/routers/admin.py`, `app/pessoas/*.py`) confirma que não existe NENHUM endpoint público para setar `colaboradorId` num `usuario` existente** — só é possível hoje via `UPDATE` direto no banco. Contornado nesta sessão: criados dois colaboradores de teste reais via UI (`Colaborador Teste` e `Henrique Matias`, o segundo com um `vinculo` ativo com `entra_em_apuracao=true` em `Sede`), e ligado `usuarios.colaborador_id` do admin ao `Henrique Matias` via SQL direto. Depois disso, `/eu/registrar` passou a carregar normalmente e parar exatamente na etapa esperada (pedido de permissão de câmera) — confirmando que o pipeline de marcação estava correto, só faltava o vínculo usuário↔colaborador. De passagem: o formulário "Novo colaborador" recusa CPF com pontuação (`PONTO-VAL-...`, "CPF deve ter 11 dígitos, sem pontuação") sem indicar isso no rótulo do campo — não é bug, mas é um ponto de atrito de UX real, descoberto tentando digitar um CPF formatado como qualquer usuário faria. | **Decisão de produto pendente**: como um usuário deve ganhar um `colaborador_id`? Opções não avaliadas aqui: (a) endpoint `PATCH /v1/usuarios/{id}` aceitar `colaboradorId`; (b) o próprio fluxo de criação de colaborador oferecer "criar acesso de login" quando `email` é preenchido; (c) convite por e-mail que já nasce prevínculado. Nenhuma foi implementada — carece de decisão de UX/segurança (quem pode religar um login a outro colaborador é uma operação sensível). Vale também adicionar uma dica de formato (`"apenas números"`) no campo CPF do formulário de colaborador. |
| 2026-08-09 | Orquestrador (fecha o gap "3" registrado hoje mais cedo: catálogo de relatórios de fábrica nunca semeado — pedido explícito do usuário: "tudo o que você acha que não tem, já coloque pra fazer") | **Semeado de verdade pela primeira vez**: `app/relatorios/catalogo_fabrica.py` (novo) extrai os 24 relatórios de `tests/f11/conftest.py::_CATALOGO_RELATORIOS` para um módulo de produção, ligado a `migrations/seed_dev.py::semeia_relatorios_de_fabrica` (chamada real esquecida no primeiro commit — corrigida num segundo commit, `62072a5`). Rodado contra `ponto-hml`: 24 linhas de `relatorio_definicoes` criadas para o tenant `seeg`. **Achado ao validar, não estimado**: a fixture de teste copiada é MAIS ANTIGA que os 11 datasets operacionais reais (`app/relatorios/datasets/operacionais.py`), que já exportam `COLUNAS_*`/`FILTROS_*`/`AGRUPAMENTOS_*` próprios — um superconjunto (docstring do módulo já avisava disso) que inclui `agrupamento=mes`, ausente na fixture. Sem isso, os 3 widgets de tendência mensal do painel (`horas-extras`, `banco-de-horas`, `ocorrencias`) respondiam `400 PONTO-VAL-005`. Corrigido: `catalogo_fabrica.py` agora sobrescreve as 11 entradas operacionais com as constantes reais do módulo (`_sobrescrever_com_datasets_operacionais`). Também achado e corrigido: `apps/web/src/ganchos/use-dataviz-dashboard.ts` tinha 3 constantes `CHAVE_VALOR_*` (`totalMinutos`/`saldoMinutos`/`total`) que **nunca bateram** com nenhuma coluna real que o motor produz (`minutos`/`saldoAposMinutos`/`quantidadeRegistros`) — o próprio comentário do arquivo já dizia "ajuste aqui se A2 fixar outro nome", nunca ajustado. Corrigidas as 3. Validado em `ponto-hml`: `GET /v1/relatorios/horas-extras/executar?agrupamento=mes&colunas=mes,minutos` responde `200` (antes `400`, depois `404` antes do catálogo existir). **Achado mais fundo, NÃO corrigido, raiz real do "Carregando…" que ainda aparece nos 3 widgets**: o intervalo de 6 meses que o dashboard pede excede `LIMIAR_DIAS_ASSINCRONO` (`app/relatorios/execucao.py::decidir_execucao_assincrona`), então a execução vai sempre pro caminho assíncrono (fila ARQ). A tarefa do worker (`apps/worker/worker/tarefas/relatorios.py::executar_relatorio`) tem `_FORMATOS_SUPORTADOS = ("csv", "xlsx", "pdf")` — **"json" nunca esteve na lista**, e é exatamente o `formato` que o dashboard pede (`use-dataviz-dashboard.ts::executarRelatorioMensal`, que já sabe buscar `execucao.urlDownload` e fazer `.json()` nele — o contrato do lado do cliente já esperava isso). Toda execução assíncrona com `formato=json` cai direto no branch `resultado_nao_implementado` (`PONTO-INT-005`), e o `RelatorioExecucao` nunca sai de `status=enfileirado`. Pior: mesmo corrigindo o formato, `_executar` (mesmo arquivo, linha ~337) **ignora `agrupamento` de propósito no caminho assíncrono** — já loga um aviso "agrupamento pedido, ignorado no caminho assincrono de streaming" — o streaming linha-a-linha nunca implementou `GROUP BY`. Confirmado direto nos logs do worker de `ponto-hml`: toda execução dos 3 widgets aparece com `'implementado': False, 'codigo': 'PONTO-INT-005'`. | **Trabalho real de engenharia, não um ajuste pontual — não tentado nesta sessão por escopo.** Para os 3 widgets de tendência funcionarem de ponta a ponta: (1) adicionar `"json"` a `_FORMATOS_SUPORTADOS` e um escritor JSON (`_ESCRITORES`, mesmo padrão de csv/xlsx/pdf); (2) implementar agregação por `agrupamento` no caminho de streaming assíncrono — hoje estruturalmente impossível (o `while True` de `_executar` processa e escreve linha a linha, sem estado de agregação; precisa de uma acumulação em memória ou um `GROUP BY` em SQL antes do streaming, como o caminho síncrono já faz em `motor.py::_montar_consulta_agrupada`). Alternativa mais barata, não avaliada a fundo: reduzir `janelaDosUltimosMeses` do dashboard ou subir `LIMIAR_DIAS_ASSINCRONO` o bastante para os 3 widgets caírem no caminho síncrono (que já agrupa corretamente) — troca simplicidade por um relatório de 6 meses inteiro rodando síncrono na request, latência a medir antes de decidir. `usuarios.colaborador_id` (achado anterior) e este são as duas maiores lacunas reais que sobraram; ambas precisam de decisão de dono de produto sobre abordagem, não só código. |
