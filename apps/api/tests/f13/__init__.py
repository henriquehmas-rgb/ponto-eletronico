"""Testes da F13 (API publica, webhooks e integracoes). Pacote raiz
necessario para o modo de import padrao do pytest resolver cada subpacote de
agente por caminho qualificado (mesmo padrao de `tests/f10/__init__.py` etc.)
-- sem logica, criado pelo primeiro agente que precisou dele para rodar
testes localmente (A6); qualquer outro agente da fase pode deixar como
esta."""
